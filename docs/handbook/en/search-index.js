window.HANDBOOK_INDEX=[{"p":"Delonix Runtime — Contributor Handbook","h":"Delonix Runtime — Contributor Handbook","u":"index.html#delonix-runtime-contributor-handbook","t":"This handbook is for people who want to change the engine : you cloned the repository today and want to send a first pull request without breaking your host or the engine. If you only want to use Delonix, start with the README and the user documentation site instead. The workspace has 21 crates and ships 4 binaries ( delonix , delonix-cri , delonix-mcp , delonix-mgmt )."},{"p":"Delonix Runtime — Contributor Handbook","h":"What the engine is — and what it is not","u":"index.html#what-the-engine-is-and-what-it-is-not","t":"Delonix Runtime is an execution abstraction for one node : it runs containers and microVMs and manages the networking and storage they need. It is declarative (its own Kinds, grouped by apiVersion — see delonix api-resources ), and it talks to providers (the Linux kernel, libvirt, Cloud Hypervisor, Proxmox VE, the Kubernetes CRI) only through ports, never through if provider == … branches spread across the code. Three principles shape almost every review comment you will get: Cloud native — plan / apply / drift, API-first (the CLI, the node API, the CRI and the MCP server expose the same operations), observable through open standards. Daemonless — no resident process by default. What must persist belongs to systemd or to a per-workload process with a clear owner. A new daemon needs an ADR. Rootless-first — the normal path runs without root; privilege is an explicit opt-in. And one boundary that is enforced by a CI gate: the engine knows no consumer. It does not know who calls it, and it has no notion of tenant, account, plan or billing. A requirement coming from a consumer enters as a generic engine capability, or it does not enter. The canonical text is the section «Identidade e fronteira do motor» at the top of AGENTS.md ."},{"p":"Delonix Runtime — Contributor Handbook","h":"Two halves: generated facts and narrative","u":"index.html#two-halves-generated-facts-and-narrative","t":"Pages in this handbook mix two kinds of content: Facts — which crates exist, their layer, who depends on whom, the binaries, the pinned toolchain, the CI jobs. They live between <!-- dev-docs:begin <key> --> and <!-- dev-docs:end <key> --> markers and are generated by python3 scripts/dev_docs.py from Cargo.toml , scripts/arch_fitness.py , rust-toolchain.toml and .github/workflows/ci.yml . Never edit them by hand — CI runs dev_docs.py --check and fails. If a fact is wrong, fix the source or the generator. Narrative — why things are the way they are, how flows work, how to contribute. It is written by hand and reviewed after each release. See 11 — Publishing the docs ."},{"p":"Delonix Runtime — Contributor Handbook","h":"Reading paths","u":"index.html#reading-paths","t":"If you want to… Read, in order Start from zero, with no one to ask 00 (keep 14 open) → the path below that matches your change Send a first PR (a CLI fix, a docs fix, a small feature) 00 → 01 → 02 (keep 15 at hand) → 10 → 12 → 06 for the crate you touch Understand the engine in depth 03 → 04 → 13 → 05 → 06 → 07 Work on VMs or VM images 01 → 02 → 08 → 09 → the delonix-vm section of 06 → the VM tuning section of 15 Change how documentation is produced 11 Configure, isolate or tune a run (state roots, logging, escape hatches, providers) 02 — Isolating the engine's state → 15"},{"p":"Delonix Runtime — Contributor Handbook","h":"Pages","u":"index.html#pages","t":"# Page What it answers 00 Start here Day 0 setup check, your first contribution end to end, where a change goes, the rules and their sources, what to do when stuck 01 Preparing your environment What the kernel and host need, the pinned toolchain, and the host traps that look like engine bugs 02 Clone, build and test Building, running tests, every CI gate as a local command, E2E and chaos with isolation 03 Rust primer The Rust this codebase actually uses 04 Cloud native primer Namespaces, cgroups v2, OCI, CRI, CNI, nftables, KVM — and where each appears in the engine 05 Architecture Layers, the crate graph, control and data paths, state on disk 06 The crates One block per crate: responsibility, main types, where to start reading 07 System Design Interview The engine designed as an interview answer, then compared with what was built 08 Delonixfile and VMfile The build file grammars and how they differ from a Dockerfile 09 Building microVMs KVM, Cloud Hypervisor and firmware, libvirt, VM images 10 Contribution workflow Worktrees, versions, language rule, architecture rules, ADRs, commits and PRs 11 Publishing the documentation How the site and this handbook are generated, gated and published 12 Coding conventions How code in this repository is written, and the checklist reviewers apply 13 Cloud native standards The cloud-native standards a change is measured against 14 Glossary The engine and cloud-native terms you meet here, with their Delonix meaning and where to read more 15 "},{"p":"0. Start here","h":"0. Start here","u":"00-start-here.html#0-start-here","t":"This page takes you from \"I just cloned the repository\" to \"my first pull request is under review\", one step at a time. Every step says what to do, what you should see, and which page explains the details. Every rule on this page links to where it is written down — if a rule is not linked, it is not a rule. If a word here is new to you, look it up in the glossary ."},{"p":"0. Start here","h":"Day 0 in 30 minutes","u":"00-start-here.html#day-0-in-30-minutes","t":""},{"p":"0. Start here","h":"What Delonix is (5 minutes)","u":"00-start-here.html#what-delonix-is-5-minutes","t":"Delonix Runtime is an engine that runs containers and microVMs on one node , together with the networking and storage they need. It is: declarative — you describe resources with its own Kinds ( delonix api-resources lists them) and the engine plans and applies the difference; daemonless — no background service is required; each command is a process that does its work and exits; rootless-first — the normal path runs as your own unprivileged user. It does not know who uses it: no platform, tenant, account or billing concept exists in the code. Read 05 — Architecture for the full picture; for now, those four sentences are enough."},{"p":"0. Start here","h":"What you need (10 minutes)","u":"00-start-here.html#what-you-need-10-minutes","t":"A Linux host with cgroup v2 and unprivileged user namespaces, the pinned Rust toolchain from rust-toolchain.toml , and protoc on your PATH . The full list, and the host traps that look like engine bugs, are in 01 — Preparing your environment . Read at least its Known host traps section before step 5 below."},{"p":"0. Start here","h":"Five commands that prove your setup works (15 minutes)","u":"00-start-here.html#five-commands-that-prove-your-setup-works-15-minutes","t":"Run these from the root of your checkout. If one of them does not give the shape shown, stop and fix it before going further — every later step depends on it. 1. Clone, with the tags. git clone https://github.com/angolardevops/delonix-runtime.git cd delonix-runtime git fetch --tags origin git describe --tags --abbrev=0 # prints the newest release, e.g. vX.Y.Z The tags matter: the version gate and the contract gate compare your branch with them ( 02 ). 2. Build the CLI. cargo build -p delonix-runtime-bin Expected: Finished on the last line and a binary at target/debug/delonix . If it stops with a message about protoc , install it ( 01 ). 3. Run the tests of a small, pure crate. cargo test -p delonix-net-rules delonix-net-rules has no dependencies at all (see its Cargo.toml ), so this only proves that your toolchain compiles and runs tests — nothing about the host. Expected: a line of the form test result: ok. N passed; 0 failed . 4. Run the binary you just built. ./target/debug/delonix --version ./target/debug/delonix --help Expected: --version prints delonix <version> on the first line, a one-line description of the engine on the second, and then a line of the form commit: <sha> · built: <date> · <licence> ; between releases the commit: part also says how far the build is from the last tag ( +N commits since vX.Y.Z ). A short get started: block follows. --help prints Usage: delonix [OPTIONS] <COMMAND> , a Commands: list and a COMMAND MAP . Always use ./target/debug/delonix , "},{"p":"0. Start here","h":"Your first contribution, end to end","u":"00-start-here.html#your-first-contribution-end-to-end","t":""},{"p":"0. Start here","h":"1. Pick something","u":"00-start-here.html#1-pick-something","t":"Look at open issues labelled good first issue or documentation on GitHub. Comment on the issue before starting, so two people do not do the same work. For anything non-trivial — a new command, a new manifest Kind, a change to namespace or cgroup setup, a new backend — open an issue first and agree on the approach ( CONTRIBUTING.md , 10 ). Check open pull requests so you do not duplicate work already in flight. Good first areas, because they are pure code with unit tests and no host privileges: a parser or validator in the CLI crate, an error message that does not say what to do, a missing Portuguese entry in bins/delonix-runtime-bin/data/pt.po , or a page of this handbook that is wrong."},{"p":"0. Start here","h":"2. Open a worktree from origin/main","u":"00-start-here.html#2-open-a-worktree-from-originmain","t":"One task, one worktree, one branch — never edit a shared checkout, never put the worktree in /tmp ( 10 — One worktree per task ): git fetch --tags origin git worktree add -b <topic>/<task> ../.worktrees/delonix-runtime/<task> origin/main cd ../.worktrees/delonix-runtime/<task> git log --oneline -- <path you will touch> # what was already decided or fixed there Reading the history of the area first is part of the job: a lot of this code records things that were tried, measured and changed ( 10 — Start from the latest tag )."},{"p":"0. Start here","h":"3. Find where the change goes","u":"00-start-here.html#3-find-where-the-change-goes","t":"Use the decision tree in Where does my change go? below, then read the section of 06 — The crates for that crate."},{"p":"0. Start here","h":"4. Write the test first","u":"00-start-here.html#4-write-the-test-first","t":"A new pure function (parser, validator, argument builder, plan) gets a unit test in the same file, under #[cfg(test)] mod tests . A test never touches the real state root: pass it a temporary directory — see 03 — Tests . A bug fix gets a test that fails without the fix . Revert your fix once, run the test, see it fail, then restore the fix. A test that passes either way proves nothing. A change to namespaces, cgroups, the network holder or VM boot also needs a live run with the state isolated, because unit tests cannot reach those paths ( 02 — Run the tests )."},{"p":"0. Start here","h":"5. Run the local gates","u":"00-start-here.html#5-run-the-local-gates","t":"Every CI job has a local command, listed in 02 — The gates CI runs . At minimum, before asking for review: cargo fmt --all --check cargo clippy --workspace --all-targets --locked -- -D warnings cargo test --workspace --locked --no-fail-fast python3 scripts/lang_ratchet.py python3 scripts/arch_fitness.py python3 scripts/dev_docs.py --check python3 scripts/version_gate.py Add the ones that match what you touched (the CLI surface gates if you added a command, the contract gate if you touched proto/ , the docs generator if help text changed) — the table in 02 says which."},{"p":"0. Start here","h":"6. Write the pull request","u":"00-start-here.html#6-write-the-pull-request","t":"Open it against main and fill in every section of .github/PULL_REQUEST_TEMPLATE.md . The template has four parts, and reviewers read all of them: What does this change do, and why? — the why ; the diff already shows the what . How was this tested? — the gates you ran, and for runtime/namespace/cgroup/network code, the command you ran live and its output. Checklist — build/clippy/fmt/test clean; new user-facing strings in English wrapped in po::t / po::tf with a Portuguese entry in pt.po ; every entry point of a command wired; unit tests for new pure functions; privilege boundaries called out. Does this cross a privilege or namespace boundary? — user namespace mapping, the holder netns, the control socket, setns / unshare , or path handling driven by user or manifest input. If you are not sure, say so. Say what was proved and what was not validated, and why ( 10 — Commits and pull requests )."},{"p":"0. Start here","h":"7. What reviewers check","u":"00-start-here.html#7-what-reviewers-check","t":"The code owners in .github/CODEOWNERS review every change. The review checklist is in 12 — Coding conventions ; the cloud-native standards a change is measured against are in 13 — Cloud native standards . Read both before you open the PR, not after the first round of comments."},{"p":"0. Start here","h":"8. After the merge","u":"00-start-here.html#8-after-the-merge","t":"Remove the worktree and the branch — the branch survives worktree remove : cd ../../../delonix-runtime # from the worktree of step 2 back to the clone git worktree remove ../.worktrees/delonix-runtime/<task> git branch -D <topic>/<task>"},{"p":"0. Start here","h":"Where does my change go?","u":"00-start-here.html#where-does-my-change-go","t":"Change Where it goes (checked in the tree) Read Rule and its source New CLI flag or subcommand bins/delonix-runtime-bin/src/cmd/<group>.rs (one module per group); strings through cmd/po.rs with Portuguese in data/pt.po ; manual text in cmd/manual_entries.rs ; leaf list in scripts/cli_baseline.tsv ( scripts/cli-tree.sh --update ). Pure validation of a run belongs in crates/contexts/delonix-compute/src/preflight.rs 10 — Adding or changing a CLI command , 03 — The CLI LANG-01 ( scripts/lang_ratchet.py ); CLI surface gate ( scripts/cli-tree.sh --gate , scripts/docs_cli_gate.py ); wire every entry point ( CONTRIBUTING.md ) New Kind, or a field on one The Kind's facts: FACTS in crates/contexts/delonix-stack/src/kinds.rs . Its spec type and apply: bins/delonix-runtime-bin/src/cmd/<kind>.rs . Live-updatable fields: hot_fields in crates/contexts/delonix-stack/src/reconcile.rs . The schema: TYPED_KINDS in cmd/schema.rs , and the published docs/schema/v1/delonix.json ( delonix manifest schema ) 04 — Declarative reconciliation , 06 — delonix-stack Open an issue first ( CONTRIBUTING.md ); the schema is generated from the code ( ADR-0007 ); tests in kinds.rs and schema.rs fail when a table is left out Network behaviour Pure rules with no I/O: crates/foundation/delonix-net-rules/src/lib.rs . Dataplane (holder, control socket, nftables, IPAM, CNI): crates/adapters/delonix-sdn/src/ ( infra.rs , ipam.rs , cni.rs ). The network step of container run : crates/contexts/delonix-compute/src/network"},{"p":"0. Start here","h":"Rules you must not break","u":"00-start-here.html#rules-you-must-not-break","t":"Each rule is enforced by a gate, a review, or both. The link is where it is written. Rule Source The engine knows no consumer. No product, platform, control plane, console or agent that uses the engine is named in crates/ , bins/ , proto/ or the manifests, comments included; no tenant, account, plan or billing. «Identidade e fronteira do motor» at the top of AGENTS.md . The named consumers are enforced by CONSUMER_NAMES in scripts/arch_fitness.py (a fixed list of names, matched by regular expression); the ban on tenant, account, plan and billing concepts is not matched by any gate and is checked in review Daemonless. No resident process by default; a new one needs an ADR with evidence of what systemd could not do. AGENTS.md (same section); 10 — Architecture rules Rootless-first. The normal path runs unprivileged; privilege is an explicit, announced opt-in. A new privilege boundary needs a GO/NO-GO spike and an ADR. AGENTS.md ; 10 — When to write an ADR Dependencies point inward, the directory is the layer, versions live only in the root. ADR-0040 ; scripts/arch_fitness.py LANG-01: code is English. Identifiers, comments and messages in English; Portuguese only through pt.po . 10 — Language ; scripts/lang_ratchet.py Version alignment. Do not change version in the root Cargo.toml in a feature PR; your branch must contain the newest tag. 10 — Version alignment ; scripts/version_gate.py One worktree per task , outside /tmp , stage files by name, remove worktree and branch at the e"},{"p":"0. Start here","h":"When you are stuck","u":"00-start-here.html#when-you-are-stuck","t":"Look in this order — each step is cheaper than the next: This handbook. The README has the page list; the glossary explains the vocabulary. AGENTS.md , organised by area. It is long and partly historical (and partly in Portuguese): use it to learn where to look, then confirm in the code. The ADR index , docs/adr/README.md — the decision behind a structure, and what was rejected. The history of the file : git log --oneline -- <path> and git log -p -S '<symbol>' . Commit messages here explain why. If you are still stuck, ask on GitHub: Comment on the issue you are working on, or open a new one with the feature request template (for questions about an approach) or the bug report template . A security problem goes through private vulnerability reporting , not an issue. The bug report template asks for: the output of delonix --version ; distro and kernel version, rootless or root, and whether you installed with install.sh , downloaded a binary, or built from source; the exact command or manifest that triggers it; what you expected, and the full, untrimmed output of what actually happened; whether it reproduces every time, sometimes, or only once; anything else that might be relevant. This handbook additionally recommends two things the template does not ask, because they save a round trip: the whole --version output of the binary you ran, including the commit: line (between releases every build reports the same version number, and only the commit tells them apart); whether DELONIX"},{"p":"0. Start here","h":"Progress checklist","u":"00-start-here.html#progress-checklist","t":"[ ] I read what Delonix is and the four principles ( 05 ). [ ] My host meets 01 , and I read the known host traps. [ ] cargo build -p delonix-runtime-bin finishes. [ ] cargo test -p delonix-net-rules reports test result: ok . [ ] ./target/debug/delonix --help works, and I stopped using the delonix on my PATH . [ ] delonix system info shows my scratch DELONIX_ROOT as the state root. [ ] I picked an issue and commented on it (or opened one for a non-trivial change). [ ] I work in my own worktree created from origin/main . [ ] I found where the change goes and read that crate's section in 06 . [ ] I wrote a test that fails without my change. [ ] The local gates from 02 pass. [ ] I read 12 and 13 . [ ] My PR fills every section of the template, including what was not validated. [ ] After the merge, I removed my worktree and my branch."},{"p":"1. Preparing your environment","h":"1. Preparing your environment","u":"01-environment.html#1-preparing-your-environment","t":"Delonix Runtime is Linux-only : every primitive it uses — namespaces, cgroups v2, nftables, pivot_root , the new mount API — lives in the Linux kernel. You can compile most of the workspace and run its pure-logic tests on any Linux box with the toolchain below; to run containers and exercise the live paths you need a host that satisfies the kernel and package requirements in this page. A lot of what looks like an engine bug on a fresh machine is a host prerequisite. Read the Known host traps section before you open an issue."},{"p":"1. Preparing your environment","h":"Toolchain","u":"01-environment.html#toolchain","t":"Rust toolchain: 1.96.0 (pinned in rust-toolchain.toml ; rustup installs it on the first cargo call) Components: rustfmt , clippy Install rustup and let it pick up the pinned channel; do not override it with stable . CI uses exactly the same file ( rustup show in every job)."},{"p":"1. Preparing your environment","h":"protoc (required to build)","u":"01-environment.html#protoc-required-to-build","t":"crates/interfaces/delonix-cri/build.rs compiles the Kubernetes CRI protobuf with tonic-build / prost , which needs the Protocol Buffers compiler on PATH . The delonix binary depends on delonix-cri , so a plain cargo build --workspace fails without it : # Debian / Ubuntu sudo apt install protobuf-compiler # Fedora / RHEL family sudo dnf install protobuf-compiler Or download a release from https://github.com/protocolbuffers/protobuf/releases . CI installs protobuf-compiler from apt in every job that compiles."},{"p":"1. Preparing your environment","h":"Optional tools, only for specific gates","u":"01-environment.html#optional-tools-only-for-specific-gates","t":"Tool Needed for Where it is pinned Python 3.11+ every scripts/*.py gate (they use tomllib ) — buf v1.73.0 and protoc-gen-openapi v0.7.1 (Go toolchain to install them) scripts/contract_gate.py contract job in .github/workflows/ci.yml cargo-deny the supply-chain check deny job, config in deny.toml Python markdown module docs/gen.py (renders ARCHITECTURE.md ) docs job groff checking the generated man pages docs job See 02 — Clone, build and test for how each one is run."},{"p":"1. Preparing your environment","h":"Kernel requirements","u":"01-environment.html#kernel-requirements","t":"These are the kernel features the engine relies on. The installer ( scripts/install.sh ) and delonix system doctor check most of them for you. Requirement Why How to check cgroup v2 (unified hierarchy) resource limits and accounting are written to /sys/fs/cgroup stat -fc %T /sys/fs/cgroup prints cgroup2fs Unprivileged user namespaces the rootless model: the engine becomes \"root\" only inside its own user namespace unshare -r -n true succeeds /dev/net/tun slirp4netns (rootless networking) and VM taps test -e /dev/net/tun overlayfs with the new mount API and lowerdir+ (Linux 6.5 or newer) container root filesystems are overlay mounts built with fsopen / fsconfig / fsmount , one lowerdir+ call per layer — see ADR-0037 uname -r br_netfilter loaded, net.bridge.bridge-nf-call-iptables=1 namespace isolation is enforced in nftables forward chains; without this module traffic between two containers on the same bridge never reaches them, and isolation is silently inert delonix system doctor KVM ( /dev/kvm ) only for microVMs — see 09 test -w /dev/kvm The 6.5 requirement has no preflight check : an older kernel fails when the first container rootfs is mounted, not at startup (ADR-0037 records this as a deliberate choice). On older Debian kernels unprivileged user namespaces are switched off by kernel.unprivileged_userns_clone=0 ; the installer sets it to 1 ."},{"p":"1. Preparing your environment","h":"Host packages","u":"01-environment.html#host-packages","t":"The source of truth is scripts/install.sh , which is also the official installer (it is published as a release asset). It detects the package manager through /etc/os-release and supports apt (Debian, Ubuntu and derivatives), dnf (Fedora, RHEL, CentOS Stream, Rocky, AlmaLinux), zypper (openSUSE, SLES) and pacman (Arch and derivatives). Prebuilt binaries exist only for x86_64 ; on other architectures build from source. To run containers the engine needs: Command Package (apt / dnf names) Why slirp4netns slirp4netns rootless networking and published ports — without it run -p fails newuidmap / newgidmap uidmap / shadow-utils setuid helpers that map more than one uid into the user namespace; without them images with a non-root user fail in chown() nft nftables SDN firewall, isolation and port DNAT ip iproute2 / iproute veth, bridge and netns plumbing conntrack (optional) conntrack / conntrack-tools cleaning up connections when a port is unpublished For VMs, qemu-img , cloud-localds ( cloud-image-utils ), virsh (libvirt) and/or Cloud Hypervisor with its firmware — see 09 . For building VM images, libguestfs-tools ( install.sh --with-image-build ). You also need a subordinate uid/gid range for your user in /etc/subuid and /etc/subgid ; without it the user namespace can map only one uid."},{"p":"1. Preparing your environment","h":"The quickest way to a working host","u":"01-environment.html#the-quickest-way-to-a-working-host","t":"You do not have to replicate the installer by hand. To install only the host dependencies and configuration, keeping the binary you build yourself: bash scripts/install.sh --no-binary Read the flag list at the top of the script first: some flags change host-wide security settings ( --low-ports lets any local program bind ports from 80, --with-image-build makes /boot/vmlinuz-* world-readable), and --no-tune skips the kernel modules and sysctls, including br_netfilter ."},{"p":"1. Preparing your environment","h":"Memory and disk","u":"01-environment.html#memory-and-disk","t":"There is no fixed minimum. What costs resources is what you run: container images and layers, VM disks, and the Rust target/ directory itself (debug builds of the whole workspace take several gigabytes). Keep an eye on free disk space — kubelets in a local cluster start evicting pods on disk pressure, which then looks like an engine problem."},{"p":"1. Preparing your environment","h":"Diagnosing the host","u":"01-environment.html#diagnosing-the-host","t":"Build the binary (see 02 ) and ask it. The commands below are read-only unless you pass --delegate : ./target/debug/delonix system doctor # is every prerequisite met? says how to fix each ./target/debug/delonix system info # rootless?, cgroup delegation, network infra, counts ./target/debug/delonix system setup # diagnose cgroup delegation ./target/debug/delonix system resources # which controllers are delegated, which flags are ignored delonix system doctor --strict exits non-zero when a check fails, which is useful in a provisioning script. If you run these on a machine that already has a Delonix installation in use, isolate the state first (see Isolating the engine's state )."},{"p":"1. Preparing your environment","h":"Known host traps","u":"01-environment.html#known-host-traps","t":""},{"p":"1. Preparing your environment","h":"Ubuntu 23.10+: AppArmor blocks user namespaces for your dev binary","u":"01-environment.html#ubuntu-2310-apparmor-blocks-user-namespaces-for-your-dev-binary","t":"Recent Ubuntu sets kernel.apparmor_restrict_unprivileged_userns=1 . A binary without an AppArmor profile then cannot create a user namespace, and the engine dies at unshare() with EPERM — which reads like an engine bug. install.sh installs a profile ( /etc/apparmor.d/delonix , flags=(unconfined) with userns ), but that profile is bound to one path : <install dir>/delonix ( /usr/local/bin/delonix by default, ~/.local/bin/delonix with --user ). A binary you just built at target/debug/delonix — or copied to /tmp — is not covered. Options, from least to most invasive: Add a second profile for your development path (for example your worktree's target/debug/delonix ), following the same shape as the one the installer writes, and load it with sudo apparmor_parser -r <file> . This touches nothing that is already running. Install your build to the profiled path ( sudo install -m 0755 target/debug/delonix /usr/local/bin/ ) — only on a machine where no Delonix workload is in use . The installed binary is what boot units ( ExecStart=<exe> container start … ) and the servers re-execute, so on a host with live workloads a debug build would silently become the production engine. Set kernel.apparmor_restrict_unprivileged_userns=0 — this lowers a host-wide boundary; only on a machine you own."},{"p":"1. Preparing your environment","h":"cgroup delegation: some limits are refused, others are not enforced","u":"01-environment.html#cgroup-delegation-some-limits-are-refused-others-are-not-enforced","t":"Resource limits only reach the kernel if the shell you run the engine from sits in a delegated cgroup. This is a cgroup v2 rule, not a Delonix limitation — rootless Podman has the same requirement. Without delegation the engine does two different things, depending on the flag: -m / --memory , -c / --cpus and --cpu-weight : container run refuses before creating anything, with an error that names the fix, and exits 69 ( Error::Unavailable , the EX_UNAVAILABLE class — preflight_resource_limits in bins/delonix-runtime-bin/src/cmd/container.rs ). DELONIX_ALLOW_UNENFORCED_LIMITS=1 runs the container anyway, unlimited, with a warning. --cpuset , --io-weight and the --device-read-bps / --device-write-bps / --device-read-iops / --device-write-iops family are not checked by that probe: they are accepted and applied best-effort, so without the cpuset / io controllers they have no effect and nothing fails. There is no --pids-limit flag; the pids ceiling is a property of the engine's cgroup group, not of container run . The common case is an SSH session : its session-N.scope is a sibling of user@<uid>.service , and moving a process between them requires writing to a cgroup owned by root. The per-command fix needs no root: systemd-run --user --scope -p Delegate=yes -- ./target/debug/delonix container run -d -m 128M alpine sleep 60 For long-lived workloads, use a systemd user unit with Delegate=yes . Some hosts delegate only cpu memory pids to user sessions; cpuset and io may never be avail"},{"p":"1. Preparing your environment","h":"A stale delonix on your PATH","u":"01-environment.html#a-stale-delonix-on-your-path","t":"If Delonix is installed on the machine, delonix on your PATH is the installed release, not your tree. Always run ./target/debug/delonix (or target/release/delonix ) when you test a change. --version shows the commit and the distance from the last tag ( commit: <hash> (+N commits since vX.Y.Z) ), because between releases two builds share the same version number."},{"p":"1. Preparing your environment","h":"Other traps you may hit","u":"01-environment.html#other-traps-you-may-hit","t":"Ports below 1024 fail rootless with slirp_add_hostfwd failed : the port is bound by slirp4netns , an unprivileged process. Use a high port, or opt in with install.sh --low-ports . Hosted CI runners (GitHub-hosted) block unprivileged user namespaces. The chaos workflow detects this and reports skipped , not success ; to exercise the live paths you need a real host, a self-hosted runner or a VM. VM firmware and image-building traps (the Cloud Hypervisor firmware choice, an old passt in libguestfs builds, /boot/vmlinuz-* permissions) are covered in 09 ."},{"p":"2. Clone, build and test","h":"2. Clone, build and test","u":"02-build-and-test.html#2-clone-build-and-test","t":"This page assumes the host from 01 — Preparing your environment : the pinned Rust toolchain and protoc . Everything here runs from the root of your checkout — ideally a git worktree , see 10 — Contribution workflow ."},{"p":"2. Clone, build and test","h":"Clone","u":"02-build-and-test.html#clone","t":"git clone https://github.com/angolardevops/delonix-runtime.git cd delonix-runtime git fetch --tags origin # several gates compare against the release tags"},{"p":"2. Clone, build and test","h":"Build","u":"02-build-and-test.html#build","t":"cargo build --workspace # every crate and every binary cargo build -p delonix-runtime-bin # just the `delonix` CLI cargo build --release -p delonix-runtime-bin # what the docs generator and the CLI gates use The workspace ships these binaries, from these packages: Binary Package Output delonix (the CLI) delonix-runtime-bin target/debug/delonix or target/release/delonix delonix-cri delonix-cri target/<profile>/delonix-cri delonix-mcp delonix-mcp-bin target/<profile>/delonix-mcp delonix-mgmt delonix-mgmt-bin target/<profile>/delonix-mgmt The release workflow builds exactly these four packages. If you set CARGO_TARGET_DIR , binaries land there instead of target/ . Two practical notes: Always test the binary you built ( ./target/debug/delonix ), never a delonix on your PATH — that is an installed release, often several versions behind. If you work in several worktrees, pointing them all at one shared CARGO_TARGET_DIR saves disk, but two builds running at the same time on it will wait on each other and can invalidate each other's artefacts. A target directory per worktree is slower the first time and predictable afterwards."},{"p":"2. Clone, build and test","h":"Installing your build locally","u":"02-build-and-test.html#installing-your-build-locally","t":"Most changes never need an installed build: run ./target/debug/delonix from your worktree. Install only when you need a stable path — a systemd unit, a script on another shell, a kubelet talking to delonix-cri . Before and after installing, confirm which build you are running: ./target/debug/delonix --version # commit: <hash> (+N commits since vX.Y.Z) · built: <date> command -v delonix # which `delonix` your shell would run instead The commit: line comes from bins/delonix-runtime-bin/build.rs ( DELONIX_GIT_HASH , DELONIX_GIT_SINCE ). Between releases every build carries the same version number, so the commit is the only way to tell your build from the released one."},{"p":"2. Clone, build and test","h":"How delonix finds its server binaries","u":"02-build-and-test.html#how-delonix-finds-its-server-binaries","t":"delonix serve cri , delonix serve api and delonix mcp do not contain the servers: they exec delonix-cri , delonix-mgmt and delonix-mcp ( exec_server in bins/delonix-runtime-bin/src/cmd/serve.rs ). The lookup is: the file of that name next to the running delonix ; otherwise the name on PATH . delonix passes the server its own version in DELONIX_DISPATCH_VERSION , and a server from a different release refuses to start. It also passes itself in DELONIX_BIN , so the server calls back the same CLI. A server started directly (for example by a unit) finds the CLI through DELONIX_BIN , then a delonix next to itself, then PATH ( cli_bin in crates/foundation/delonix-runtime-core/src/dispatch.rs ). Keep the four binaries of one build together ; a mix of your build and a release is refused, or runs code you did not mean to test. delonix cluster kubeadm and delonix image vm build look for delonix-cri in their own order ( resolve_cri_bin in bins/delonix-runtime-bin/src/cmd/vmimage.rs ): --cri-bin , then next to delonix , then a cargo build --release -p delonix-cri if the current directory is inside a source checkout, and only then a download of the released asset. Build the four before installing them: cargo build --release -p delonix-runtime-bin -p delonix-cri -p delonix-mgmt-bin -p delonix-mcp-bin"},{"p":"2. Clone, build and test","h":"Option A — run it from the worktree (safest)","u":"02-build-and-test.html#option-a-run-it-from-the-worktree-safest","t":"Nothing is copied, so nothing outside your checkout can pick it up by accident: alias delonix-dev=\"$PWD/target/release/delonix\" delonix-dev --version The servers are found because they sit next to it in target/release/ . On Ubuntu 23.10+ this path needs its own AppArmor profile (see 01 — AppArmor )."},{"p":"2. Clone, build and test","h":"Option B — install for your user in ~/.local/bin","u":"02-build-and-test.html#option-b-install-for-your-user-in-localbin","t":"install -d ~/.local/bin install -m 0755 target/release/delonix target/release/delonix-cri \\ target/release/delonix-mgmt target/release/delonix-mcp ~/.local/bin/ hash -r # forget the path your shell cached command -v delonix && delonix --version If a release is also installed in /usr/local/bin , whichever directory comes first on PATH wins. AppArmor. scripts/install.sh writes one profile, /etc/apparmor.d/delonix , bound to <install dir>/delonix , and only on hosts with kernel.apparmor_restrict_unprivileged_userns=1 . A binary you copied to a new path is not covered. Do not re-run the installer to \"move\" that profile on a machine that also uses a released installation: the profile file is rewritten, and the released binary loses it. Add a second profile with a different name instead — the same shape the installer writes, so it replaces nothing: printf 'abi <abi/4.0>,\\ninclude <tunables/global>\\nprofile delonix-dev %s flags=(unconfined) {\\n userns,\\n}\\n' \\ \"$HOME/.local/bin/delonix\" | sudo tee /etc/apparmor.d/delonix-dev >/dev/null sudo apparmor_parser -r /etc/apparmor.d/delonix-dev Unverified here: this command mirrors install.sh (the AppArmor block) with a different profile name and file; it was not loaded on a host with the restriction active while writing this page. The installer also adds shell completion, man pages and editor syntax files, but only in its binary phase. For your own build, generate them from the binary if you want them: mkdir -p ~/.local/share/bash-completi"},{"p":"2. Clone, build and test","h":"Option C — system install in /usr/local/bin","u":"02-build-and-test.html#option-c-system-install-in-usrlocalbin","t":"sudo install -m 0755 target/release/delonix target/release/delonix-cri \\ target/release/delonix-mgmt target/release/delonix-mcp /usr/local/bin/ Only on a machine where no Delonix workload is in use. The installed binary is not just a command: boot units written by delonix system boot enable start ExecStart=<exe> container start <name> , where <exe> is the path of the binary that ran enable ( bins/delonix-runtime-bin/src/cmd/boot.rs , unit prefix delonix-boot- ); replacing that file changes what comes up after the next reboot; dist/delonix-cri.service runs /usr/local/bin/delonix-cri , so on a Kubernetes node the kubelet gets your build at the next restart of that unit; long-lived processes started earlier (the network pin and control process, container supervisors) keep running the code they started with, so for a while two builds run side by side. Check first: delonix container ls -a; delonix vm ls ls ~/.config/systemd/user/delonix-boot-* /etc/systemd/system/delonix-* 2>/dev/null pgrep -a delonix"},{"p":"2. Clone, build and test","h":"Preparing the host","u":"02-build-and-test.html#preparing-the-host","t":"scripts/install.sh does two separate jobs. Only the first one is about the binary: Part What it does Flag that skips or enables it Binary downloads a release, verifies the minisign signature and sha256, installs delonix (plus delonix-mcp , delonix-mgmt , and delonix-cri with --with-cri ), then completion, man pages, editor syntax and the editor extension skipped with --no-binary ; --user picks ~/.local/bin Host packages slirp4netns , uidmap , nftables , iproute2 , conntrack always Rootless identity /etc/subuid and /etc/subgid ranges for your user always AppArmor profile for <dir>/delonix when the userns restriction is active always (when the restriction is on) Old Debian kernel.unprivileged_userns_clone=1 when it is 0 always (when needed) VM dependencies libvirt, qemu, cloud-init tooling; Cloud Hypervisor and its firmware downloaded from upstream skipped with --no-vm Kernel tuning /etc/modules-load.d/delonix.conf , /etc/sysctl.d/99-delonix.conf skipped with --no-tune cgroup delegation user@.service drop-in, only if not already delegated skipped with --no-delegate Accelerators NVIDIA CDI and render group, only when a GPU is present skipped with --no-gpu Opt-ins ports below 1024 ( --low-ports ), VM image building ( --with-image-build ), scale tuning ( --production ) off by default To prepare a host for your own build without downloading any Delonix release , run the installer from your checkout with --no-binary : bash scripts/install.sh --no-binary # add --no-vm if you do not n"},{"p":"2. Clone, build and test","h":"Use an isolated state root","u":"02-build-and-test.html#use-an-isolated-state-root","t":"An installed build uses your real state root by default: the same containers, networks and volumes as the release. Export DELONIX_ROOT and DELONIX_NET_RUNTIME_DIR first (see Isolating the engine's state ), and see 15 — Environment variables for every other variable your build reads."},{"p":"2. Clone, build and test","h":"Uninstall and roll back","u":"02-build-and-test.html#uninstall-and-roll-back","t":"There is no uninstall flag in install.sh . Remove what you copied: rm -f ~/.local/bin/delonix ~/.local/bin/delonix-cri ~/.local/bin/delonix-mgmt ~/.local/bin/delonix-mcp hash -r sudo apparmor_parser -R /etc/apparmor.d/delonix-dev && sudo rm /etc/apparmor.d/delonix-dev # if you added it To go back to a released binary, run the installer again; it replaces the binaries in its install directory with the release you name: curl -fsSL https://github.com/angolardevops/delonix-runtime/releases/latest/download/install.sh | bash -s -- --user curl -fsSL https://github.com/angolardevops/delonix-runtime/releases/latest/download/install.sh | bash -s -- --version vX.Y.Z Completion files and man pages you generated by hand are not removed by either step. Finish with delonix --version to confirm the commit you are back on."},{"p":"2. Clone, build and test","h":"Run the tests","u":"02-build-and-test.html#run-the-tests","t":"cargo test --workspace # the whole suite cargo test -p delonix-sdn # one crate cargo test -p delonix-stack -- reconcile # tests whose path contains \"reconcile\" cargo test -p delonix-stack -- --exact kinds::tests::nenhum_kind_aparece_duas_vezes Tests that need privileges or a real host skip themselves instead of failing, so the suite is meaningful on a laptop and on CI. A few live tests are marked #[ignore] and name the command to run them in their doc comment (for example in crates/adapters/delonix-vm/src/lib.rs ); run those only on a machine you own: cargo test -p <crate> -- --ignored <test-name> A green cargo test proves the pure logic. It does not prove that a change to namespaces, cgroups, the network holder or VM boot works — that needs a live run (see End-to-end battery and Chaos harness )."},{"p":"2. Clone, build and test","h":"The gates CI runs","u":"02-build-and-test.html#the-gates-ci-runs","t":"CI job What it checks fmt rustfmt lang lang ratchet arch arch fitness contract contract gate version version gate cli-surface cli surface clippy clippy -D warnings test test deny cargo-deny docs generated docs and valid examples Every job in .github/workflows/ci.yml can be reproduced locally. Run the ones that match what you touched before you push; run all of them before asking for review. Job Local command Fails when fmt cargo fmt --all --check the code is not rustfmt-formatted (default config) lang python3 scripts/lang_ratchet.py Portuguese identifiers, comments or messages increase — or decrease without lowering scripts/lang_baseline.json in the same commit ( --list shows them, --update lowers the baseline) arch python3 scripts/arch_fitness.py a dependency goes against the layer direction, a crate sits in the wrong directory, a member crate pins a dependency version, a consumer name appears in code, or a debt ratchet moves ( --list , --update ) arch python3 scripts/dev_docs.py --check a generated fact in docs/dev/ is stale — run python3 scripts/dev_docs.py and commit contract python3 scripts/contract_gate.py the node contract in proto/delonix/node/v1 is not buf format -clean, fails buf lint , breaks compatibility with the last tag, lacks an HTTP mapping, or docs/api/openapi.yaml is not the generated one ( --update rewrites it). Needs protoc , buf v1.73.0 and protoc-gen-openapi v0.7.1 on PATH , and the tags version python3 scripts/version_gate.py the workspace version is n"},{"p":"2. Clone, build and test","h":"Isolating the engine's state","u":"02-build-and-test.html#isolating-the-engines-state","t":"Anything beyond --help touches engine state. By default that is your real state : your containers, networks, volumes and the network holder. Before running the engine for testing — by hand, through e2e.sh , or through any script — point both state roots at a scratch directory: export DELONIX_ROOT=$HOME/scratch/dlx/root # containers, images, networks, IPAM, volumes export DELONIX_NET_RUNTIME_DIR=/tmp/dlx-run # the holder's control and slirp sockets mkdir -p \"$DELONIX_ROOT\" \"$DELONIX_NET_RUNTIME_DIR\" Both, always. Half isolation is worse than none. The network sockets and the pidfiles are resolved separately: pidfiles live under the state root , while the holder's control and slirp sockets live in a runtime directory (by default /tmp/delonix-net-<uid> ). When two state roots ended up on the same runtime directory, each read its own (absent) pidfile, concluded there was no network infra, and started or tore down infra on top of the other one's sockets. On a development host running live workloads this ended with the real root rebuilding its network infra and restarting real containers. The engine now derives a suffix from a non-default DELONIX_ROOT for the runtime directory ( runtime_dir / root_suffix in crates/adapters/delonix-sdn/src/infra.rs ), which closes that collision for the common case. Keep exporting both anyway: it makes the isolation explicit, keeps the socket path short and under your control, and it is what scripts/e2e.sh and scripts/chaos.sh do (e2e fills in which"},{"p":"2. Clone, build and test","h":"End-to-end battery (scripts/e2e.sh)","u":"02-build-and-test.html#end-to-end-battery-scriptse2esh","t":"e2e.sh runs the CLI against the real kernel: every leaf's --help , plus real executions of a large part of the surface, and prints a PASS/FAIL/SKIP/XFAIL report (JSONL detail in $OUT/results.jsonl , default OUT=/tmp/delonix-e2e ). ./scripts/e2e.sh # uses ./target/debug/delonix ./scripts/e2e.sh ./target/release/delonix It isolates itself by default : it sets DELONIX_ROOT and DELONIX_NET_RUNTIME_DIR to its own directories (unless you export both first) and tears down the infra it started. E2E_SHARED_STATE=1 runs against the real machine state — only for diagnosing a host. Exit code is non-zero when a check fails, or when a check marked as a known defect ( XFAIL ) unexpectedly passes. SKIPs do not fail the run but are listed in their own block: a skipped check proved nothing. It needs network access to pull images; sections whose preconditions are missing skip with the reason. A green run means the --help of every leaf was verified and some leaves were executed. Read the header of the script for what is executed and what is not."},{"p":"2. Clone, build and test","h":"Chaos harness (scripts/chaos.sh)","u":"02-build-and-test.html#chaos-harness-scriptschaossh","t":"The chaos harness breaks a running engine on purpose — killing the holder, filling the disk, concurrent attaches, partial applies — and reports whether it degraded the way it promises to. scripts/chaos.sh # every scenario, ./target/debug/delonix scripts/chaos.sh holder_kill oom # selected scenarios scripts/chaos.sh --keep scale # leave the sandbox up for a post-mortem scripts/chaos.sh --clean # tear the kept sandbox down It always redirects both roots into its sandbox ( DELONIX_CHAOS_DIR , default /tmp/dlx-chaos ) and never touches the real engine's containers, networks or records. The image directories ( images , layers , blobs ) are symlinks to your real store to avoid downloads: the harness only reads them in practice, but a scenario that wrote an image would write to the real store. It refuses to run on a busy machine (load above a threshold, shared with scripts/bench.sh through scripts/bancada.sh ): under load, scenarios fail for reasons that belong to the bench, not the product. --max-load N changes the threshold; --force runs anyway and marks the verdict as not publishable. Exit code is 0 only when no scenario fails. SKIPs are listed separately. Some scenarios need external resources and skip without them (for example truenas_destroy needs DELONIX_CHAOS_TRUENAS_URL / _USER / _PASS ). Scratch directories under /tmp are fine for these throwaway sandboxes. Your worktrees are not — see 10 ."},{"p":"3. Rust primer for this codebase","h":"3. Rust primer for this codebase","u":"03-rust-primer.html#3-rust-primer-for-this-codebase","t":"This is not a Rust tutorial. It is the subset of Rust you need to read this repository , with each idea pinned to a file you can open. If a concept is new to you, the \"Read more\" links go to the official source; come back here to see how the engine uses it. Paths are relative to the repository root. Symbols are named so you can grep for them — line numbers are deliberately left out, because they move with every PR. The pinned toolchain is in rust-toolchain.toml (a fixed channel , plus rustfmt and clippy ). rustup honours it automatically, so you build with the same compiler CI uses."},{"p":"3. Rust primer for this codebase","h":"3.1 The Cargo workspace","u":"03-rust-primer.html#31-the-cargo-workspace","t":"The repository is one Cargo workspace : a root Cargo.toml with [workspace] members = [...] and one Cargo.toml per crate. Three conventions matter here: Every path and every version is written once, in the root. The root has a [workspace.dependencies] table listing both the engine's own crates (by path ) and every third-party crate (by version ). A member never writes a version; it writes: # crates/foundation/delonix-runtime-core/Cargo.toml [dependencies] serde = { workspace = true } thiserror = { workspace = true } A member may add features = [...] , and that is all. default-features = false lives in the root because a member cannot turn off defaults the workspace turned on. scripts/arch_fitness.py (function inline_versions ) fails the build if a member writes its own version. The directory is the layer. Crates live in crates/foundation/ , crates/contexts/ , crates/adapters/ , crates/providers/ , crates/interfaces/ and binaries in bins/ (ADR-0040). scripts/arch_fitness.py refuses a crate whose directory does not match its declared layer, and a dependency that points against the allowed direction. See 5. Architecture for the layer rules. Lints are inherited. The root declares [workspace.lints.clippy] with undocumented_unsafe_blocks = \"deny\" , and each member opts in with [lints] workspace = true . Every unsafe block therefore carries a // SAFETY: comment (see §3.4). The root also sets [workspace.package] (the shared version , edition , license ), which members consume as versi"},{"p":"3. Rust primer for this codebase","h":"3.2 Errors: one Error, and exit codes derived from its type","u":"03-rust-primer.html#32-errors-one-error-and-exit-codes-derived-from-its-type","t":"Almost every fallible function in the engine returns delonix_runtime_core::Result<T> , an alias over the shared enum defined in crates/foundation/delonix-model/src/error.rs . The enum lives in the pure foundation crate delonix-model ; delonix-runtime-core re-exports Error and Result ( pub use delonix_model::{Error, Result}; ), so the delonix_runtime_core:: path most call sites use still works. It is built with thiserror : #[derive(Error)] generates Display from the #[error(\"...\")] attribute, and #[from] generates From impls so ? converts a lower-level error automatically: // crates/foundation/delonix-model/src/error.rs #[error(\"I/O error: {0}\")] Io(#[from] std::io::Error), The variants are about what the caller should do next , not about which subsystem failed: NotFound , NotRunning , Conflict , Unavailable (a missing tool or kernel feature), Timeout , Invalid , Registry , Runtime { context, message } for a failed syscall, and so on. The doc comments on each variant explain why it exists; read them before adding a variant. Those variants become process exit codes in one place: crates/foundation/delonix-model/src/exitcode.rs , function for_error . The CLI re-exports the module ( pub use delonix_model::exitcode; in bins/delonix-runtime-bin/src/cmd/mod.rs ) and bins/delonix-runtime-bin/src/main.rs calls std::process::exit(cmd::exitcode::for_error(&e)) . // crates/foundation/delonix-model/src/exitcode.rs pub fn for_error(e: &Error) -> i32 { match e { Error::NotFound(_) | Error::V"},{"p":"3. Rust primer for this codebase","h":"3.3 Traits as ports: VmBackend and the backend registry","u":"03-rust-primer.html#33-traits-as-ports-vmbackend-and-the-backend-registry","t":"The engine talks to providers through traits (\"ports\"), and a provider is an implementation of one. The clearest example is VmBackend in crates/adapters/delonix-vm/src/lib.rs : pub trait VmBackend { fn id(&self) -> &'static str; fn available(&self) -> bool; fn boot(&self, vmdir: &Path, cfg: &VmConfig, overlay: &str, on: &dyn Fn(CreateStage)) -> Result<Boot>; fn is_running(&self, vm: &Vm) -> bool; fn ip(&self, vm: &Vm) -> Option<String>; // ... more methods, many with default bodies } Implementations: CloudHypervisorBackend and LibvirtBackend in the same file, and ProxmoxBackend in crates/providers/delonix-proxmox/src/lib.rs . Methods with a default body in the trait (for example auto_selectable ) let a new backend inherit sensible behaviour and override only what differs. Backends are chosen at runtime, so they are handled as trait objects , Box<dyn VmBackend> . They are created through a registry of factories: // crates/adapters/delonix-vm/src/lib.rs pub type BackendFactory = Box<dyn Fn() -> Result<Box<dyn VmBackend>> + Send + Sync>; static BACKENDS: std::sync::OnceLock<std::sync::RwLock<Vec<BackendRegistration>>> = std::sync::OnceLock::new(); The factory is a closure ( dyn Fn ), because a remote backend needs captured configuration (endpoint, node, credential) that a plain fn() pointer cannot carry. Send + Sync is required because the table is a process-wide static ; the bound constrains the closure, not the VmBackend trait. OnceLock initialises the table lazily ( builtin_b"},{"p":"3. Rust primer for this codebase","h":"3.4 unsafe, FFI and Linux syscalls","u":"03-rust-primer.html#34-unsafe-ffi-and-linux-syscalls","t":"A container engine is mostly system calls. This repo reaches the kernel through three crates: Crate Used for Example in this repo nix Safe-ish wrappers: clone , setns , unshare , pivot_root , fork , mount , signals use nix::sched::{clone, setns, unshare, CloneFlags}; in crates/adapters/delonix-linux/src/lib.rs libc Raw calls nix does not wrap, or where the exact struct matters libc::getsockopt(.., SO_PEERCRED, ..) in crates/foundation/delonix-runtime-core/src/peer_cred.rs ( peer_uid ); libc::flock in store.rs rustix The new mount API ( fsopen / fsconfig / fsmount / move_mount ) fsopen_overlay in crates/adapters/delonix-linux/src/lib.rs Every unsafe block states why it is sound , next to it (the workspace lint enforces the comment's presence; reviewers enforce its truth): // crates/foundation/delonix-runtime-core/src/peer_cred.rs // SAFETY: getsockopt on SO_PEERCRED with a correctly-sized ucred buffer. let r = unsafe { libc::getsockopt(stream.as_raw_fd(), libc::SOL_SOCKET, libc::SO_PEERCRED, ...) };"},{"p":"3. Rust primer for this codebase","h":"Where the container is born","u":"03-rust-primer.html#where-the-container-is-born","t":"fn spawn in crates/adapters/delonix-linux/src/lib.rs ends in: // SAFETY: single-threaded; the child mounts the container and does `exec`. let cloned = unsafe { clone(cb, &mut stack, flags, Some(Signal::SIGCHLD as i32)) }; The child runs container_init , which calls setup_rootfs (bind mounts, then pivot_root ), drop_capabilities , installs seccomp, and finally execvp . setns is how exec enters an existing container's namespaces. The uid/gid maps of a user namespace are written from the parent ( write_userns_maps , which uses the newuidmap / newgidmap helpers when available)."},{"p":"3. Rust primer for this codebase","h":"Why fork/clone in a multi-threaded process is dangerous","u":"03-rust-primer.html#why-forkclone-in-a-multi-threaded-process-is-dangerous","t":"After fork or clone , only the calling thread exists in the child, but the child inherits every lock as it was — including locks held by threads that no longer exist. If another thread held the allocator lock at that instant, the child's first allocation blocks forever. fork mitigates some of this with pthread_atfork handlers; raw clone does not run them. This repo has two concrete rules because of it, both explained in comments you should read: serve docker-api never calls spawn in-process. The doc comment above the re-exec helper in bins/delonix-runtime-bin/src/cmd/dockerapi.rs explains that the server is a multi-threaded tokio runtime, so it re-executes the binary ( __apirun <spec.json> ) to get a fresh single-threaded process where the clone precondition is true again. After a raw fork , only async-signal-safe work. reexec_mapped and reexec_mapped_hold in crates/adapters/delonix-linux/src/lib.rs pre-compute every allocation before the fork. The comment on reexec_mapped_hold also records why they use raw fork instead of std::process::Command + pre_exec : Command::spawn waits for the child to reach exec , and a pre_exec hook that blocks waiting on the parent deadlocks."},{"p":"3. Rust primer for this codebase","h":"The new mount API, and why it exists here","u":"03-rust-primer.html#the-new-mount-api-and-why-it-exists-here","t":"mount_overlay_if_marked mounts a container's overlay root through rustix::mount::fsopen and one fsconfig_set_string(&fs, \"lowerdir+\", lower) call per layer. The classic mount(2) packs all options into a single page-sized data string, which the kernel truncates silently for images with many layers. The measurement and decision are in ADR-0037 . This is a good example of the repo's habit: the /// comment above a non-obvious syscall explains the failure that motivated it. Read more: The Rust Book — Unsafe Rust ; the Rustonomicon (especially FFI ); man pages clone(2) , setns(2) , pivot_root(2) , fork(2) , signal-safety(7) , fsopen(2) ."},{"p":"3. Rust primer for this codebase","h":"3.5 Serialization: serde, YAML manifests, generated schema","u":"03-rust-primer.html#35-serialization-serde-yaml-manifests-generated-schema","t":"State on disk is JSON; manifests are YAML. Both go through serde derives. Backward-compatible records. A record written by an older binary must still load. The rule is: a new field gets #[serde(default)] , and when \"absent\" must mean something different from the type's default, it becomes an Option : // bins/delonix-runtime-bin/src/cmd/vmimage.rs (VmImage) #[serde(default)] pub cloud_init: Option<bool>, Here None (every record written before the field existed) is read as \"yes\"; a plain bool would have defaulted to false and silently changed behaviour for old images. You will see the same reasoning on Mount::propagation and Mount::optional in crates/foundation/delonix-runtime-core/src/lib.rs ( #[serde(default, skip_serializing_if = \"Option::is_none\")] ). Manifests. bins/delonix-runtime-bin/src/cmd/manifest.rs parses multi-document YAML with serde_yaml::Deserializer::from_str(text) into ManifestDoc , whose spec stays a raw serde_yaml::Value until the owning Kind deserializes it into its typed spec. Generated schema. Spec types derive schemars::JsonSchema (for example PodSpec in crates/contexts/delonix-compute/src/pod.rs ), and bins/delonix-runtime-bin/src/cmd/schema.rs generates the published JSON Schema from them ( ADR-0007 ). Note the comment there: schemars honours #[serde(rename)] but not #[serde(alias)] . Read more: serde.rs — field attributes ( default , skip_serializing_if , alias ); serde_yaml ; schemars ."},{"p":"3. Rust primer for this codebase","h":"3.6 The CLI: clap derive and translated output","u":"03-rust-primer.html#36-the-cli-clap-derive-and-translated-output","t":"The delonix binary is bins/delonix-runtime-bin . Its top level is a clap derive in src/main.rs : #[derive(Parser)] struct Cli holding a global --l18n flag and #[command(subcommand)] cmd: Cmd , where enum Cmd is a #[derive(Subcommand)] . Each group has its own module and enum in src/cmd/ — for example pub enum ContainerCmd in src/cmd/container.rs . Doc comments ( /// ) on variants and fields become the --help text. You will see #[allow(clippy::large_enum_variant)] on command enums, with a comment explaining why: they are parsed once per invocation, so boxing variants would buy nothing. Source strings are English; Portuguese comes from a catalogue. src/cmd/po.rs embeds data/pt.po with include_str! and exposes: po::t(\"already exists, nothing to do\") — a fixed string; po::tf(\"port {port} is taken by '{owner}'\", &[(\"port\", &hp), (\"owner\", &ow)]) — a template with named placeholders, because a translation may reorder them; po::translate_help , which rewrites clap's help text after po::peek_lang has decided the language before parsing. A new user-facing string is English in the code plus an entry in data/pt.po . The language policy (LANG-01) and its gate are covered in 10. Contributing workflow . Read more: clap derive tutorial ; std — include_str! ."},{"p":"3. Rust primer for this codebase","h":"3.7 Async and gRPC — and why most of the engine is synchronous","u":"03-rust-primer.html#37-async-and-grpc-and-why-most-of-the-engine-is-synchronous","t":"The core engine is synchronous : the CLI starts, does its work with blocking syscalls and file I/O, and exits. There is no daemon and no ambient async runtime. Async code appears only in the interfaces that serve a protocol: CRI server — crates/interfaces/delonix-cri . The gRPC stubs are generated at build time: build.rs calls tonic_build::configure()...compile_protos(&[\"proto/api.proto\"], &[\"proto\"]) . That needs protoc installed (CI installs protobuf-compiler ); a missing protoc is the most common first build failure. serve_blocking in src/lib.rs creates its own tokio::runtime::Builder::new_multi_thread() runtime, and handlers push blocking engine work off the async workers through the blocking helper in src/runtime_svc.rs — otherwise a clone or a shell-out to delonix would stall the tokio workers. L7 reverse proxy — bins/delonix-runtime-bin/src/cmd/ingress_proxy.rs uses hyper directly ( hyper::service::service_fn ) on its own tokio runtime. MCP server — crates/interfaces/delonix-mcp uses rmcp over stdio ( rmcp::transport::io::stdio ). Telemetry — crates/adapters/delonix-telemetry/src/telemetry.rs exports OpenTelemetry spans from a dedicated thread with a blocking HTTP client, precisely so the synchronous CLI does not need a runtime. The node contract under proto/delonix/node/v1 is a separate protobuf API with its own gate ( scripts/contract_gate.py , buf ); see 2. Build and test . Read more: Asynchronous Programming in Rust ; Tokio tutorial ; tonic and tonic-build ; Protoc"},{"p":"3. Rust primer for this codebase","h":"3.8 Concurrency and shared state","u":"03-rust-primer.html#38-concurrency-and-shared-state","t":"There is no database. State is JSON files under the state root, and several processes (two CLI invocations, the CRI server, a supervisor) may touch the same record at once. The pattern for every read-modify-write is update with a closure, under an exclusive flock : // crates/foundation/delonix-runtime-core/src/store.rs (Store::update) let id = self.load(id_or_name)?.id; let _lock = FileLock::acquire(&self.lock_path(&id))?; // Re-read UNDER the lock ... let mut c = self.load(&id)?; if !f(&mut c) { return Ok(c); } self.save(&c)?; JsonStore<T>::update in the same file is the generic version for other record types. Rules that follow from it: Never do load → mutate → save by hand for a record another process can write; you will lose updates. Use update . Re-read inside the lock. The value you loaded earlier may already be stale. FileLock releases the lock in Drop — the RAII pattern. Inside a single process, shared state uses the standard types: an Arc<std::sync::RwLock<Arc<Vec<Route>>>> holds the proxy's hot-swappable route table ( SharedRoutes in cmd/ingress_proxy.rs ), and the dashboard shares its slow sample through an Arc<Mutex<...>> ( cmd/dash.rs ). A related idea you will meet in crates/foundation/delonix-runtime-core/src/typestate.rs : the typestate pattern, where lifecycle states are types and illegal transitions do not compile (its doc tests include compile_fail examples). Read more: The Rust Book — Shared-state concurrency , Drop ; flock(2) ; std — Arc , RwLock ."},{"p":"3. Rust primer for this codebase","h":"3.9 Tests","u":"03-rust-primer.html#39-tests","t":"Unit tests live next to the code , in a #[cfg(test)] mod tests { ... } at the bottom of the file. Many pure functions exist precisely so a decision can be tested as data (for example reconcile::plan in crates/contexts/delonix-stack/src/reconcile.rs ). Integration tests live in crates/<layer>/<crate>/tests/*.rs and use only the crate's public API. Example: crates/interfaces/delonix-cri/tests/grpc_status.rs starts the CRI server on a Unix socket and talks to it with the generated gRPC client (which is why build.rs sets build_client(true) ). Tests under crates/providers/*/tests/live.rs need a real provider and are opt-in. Property tests use proptest : see crates/adapters/delonix-sdn/tests/ip_invariants.rs ( proptest! { ... } ). Doc tests run too — including compile_fail blocks such as those in typestate.rs . Benchmarks use criterion ( crates/adapters/delonix-oci/benches/ ). Names. Test names describe the behaviour being proven. Many existing tests have Portuguese names; new identifiers are English (LANG-01, enforced as a ratchet by scripts/lang_ratchet.py ). Tests must not touch the host's real state. Take a temporary directory and pass it in (the stores take a root path) rather than calling code that resolves the real state root. Anything that needs real namespaces, cgroups or a network holder belongs to the live/E2E validation described in 2. Build and test . Read more: The Rust Book — Writing tests , Test organization ; rustdoc — Documentation tests ."},{"p":"3. Rust primer for this codebase","h":"3.10 Tooling the CI enforces","u":"03-rust-primer.html#310-tooling-the-ci-enforces","t":"Tool CI command (from .github/workflows/ci.yml ) What it catches rustfmt cargo fmt --all --check formatting drift clippy cargo clippy --workspace --all-targets --locked -- -D warnings any warning, including undocumented_unsafe_blocks tests cargo test --workspace --locked --no-fail-fast regressions cargo-deny EmbarkStudios/cargo-deny-action with deny.toml RUSTSEC advisories, licences, crate sources The repo also has Python gates ( scripts/arch_fitness.py , scripts/lang_ratchet.py and others). The full list and how to run each one locally is in 2. Build and test . Read more: Clippy , rustfmt , cargo-deny ."},{"p":"4. Cloud native primer","h":"4. Cloud native primer","u":"04-cloud-native-primer.html#4-cloud-native-primer","t":"The engine is a thin, careful layer over Linux kernel features and a handful of open specifications. This page gives you just enough of each concept to read the code, and says where it lives in this repository. For depth, follow the official links — they are better than any summary here. Each section has three parts: the concept, In Delonix (files and symbols you can grep ), and Read more . Paths are relative to the repository root. For orientation in the wider ecosystem, the CNCF Landscape and the CNCF Glossary are good maps. Comparisons with runc, crun, containerd or Podman appear only where they help explain a design choice."},{"p":"4. Cloud native primer","h":"4.1 Linux namespaces and rootless operation","u":"04-cloud-native-primer.html#41-linux-namespaces-and-rootless-operation","t":"A namespace gives a process its own view of one kind of global resource. A container is, at its core, a process started in a fresh set of them: mount (its own filesystem tree), PID (its own process numbering, with itself as PID 1), network (its own interfaces and routes), IPC , UTS (hostname), cgroup (its own view of the cgroup tree) and user . The user namespace is what makes rootless containers possible. Inside it, a process can be uid 0 with full capabilities over resources owned by that namespace , while on the host it is an ordinary user. The mapping between inside and outside uids is written to /proc/<pid>/uid_map and gid_map . An unprivileged user can map only its own uid; mapping a range requires the setuid helpers newuidmap / newgidmap , which check /etc/subuid and /etc/subgid . Some distributions additionally restrict unprivileged user namespaces through AppArmor — see 1. Environment for the practical consequences. In Delonix fn spawn in crates/adapters/delonix-linux/src/lib.rs builds the CloneFlags ( CLONE_NEWNS , CLONE_NEWPID , CLONE_NEWNET , CLONE_NEWUSER , …) and calls nix::sched::clone . IPC/UTS sharing between pod members is handled by setns in container_init . write_userns_maps in the same file writes the maps from the parent: a single-uid map ( 0 <euid> 1 ) rootless, or a subuid range through newuidmap / newgidmap when have_subid_helpers() says they are available. USERNS_UID_BASE / USERNS_RANGE define the range used when running as root. setup_rootfs mounts "},{"p":"4. Cloud native primer","h":"4.2 cgroups v2 and delegation","u":"04-cloud-native-primer.html#42-cgroups-v2-and-delegation","t":"Control groups limit and account for resources (memory, CPU, PIDs, I/O) for a set of processes. cgroup v2 is a single tree mounted at /sys/fs/cgroup ; a directory is a group, and files like memory.max , cpu.max , pids.max and memory.events are its interface. A controller is available to a child only if the parent lists it in cgroup.subtree_control . An unprivileged user can manage a subtree only if it has been delegated to them. On systemd hosts, user@<uid>.service typically delegates some controllers, and systemd-run --user --scope -p Delegate=yes creates a delegated scope on demand. A process in an SSH session scope usually sits outside the delegated subtree, and the \"no internal processes\" rule prevents moving it in — so limits can silently not apply there. The engine is v2-only. In Delonix Root mode places containers under delonix_runtime_core::DELONIX_SLICE ( /sys/fs/cgroup/delonix.slice ). Rootless mode finds the user's service cgroup and creates leaves under <user@uid.service>/dlx-containers — see user_service_base and try_delegated_base in crates/adapters/delonix-linux/src/lib.rs . cgroup_limits_apply answers \"will limits apply on this host?\" without starting a container. The design decision about the intermediate level is ADR-0015 ; how the CRI follows the kubelet's cgroup hierarchy is ADR-0038 , with the kubelet's parent validated by KubeCgroupParent::parse in crates/foundation/delonix-runtime-core/src/lib.rs . Read more: Linux kernel — Control Group v2 , systemd — "},{"p":"4. Cloud native primer","h":"4.3 Capabilities, seccomp, AppArmor, masked paths","u":"04-cloud-native-primer.html#43-capabilities-seccomp-apparmor-masked-paths","t":"Root's power is split into capabilities ( CAP_NET_ADMIN , CAP_SYS_ADMIN , …). A container keeps a small default set and drops the rest. seccomp installs a BPF filter that allows or denies system calls, reducing kernel attack surface. AppArmor (and SELinux on other distributions) are Linux Security Modules that confine a process by profile. Finally, runtimes mask sensitive /proc and /sys paths (bind something empty over them) and make others read-only, because those files leak host information or allow host control. A subtle point the code documents: clone3 passes its flags through a pointer that a seccomp filter cannot inspect, so a filter that blocks clone(CLONE_NEWUSER) must also make clone3 fail with ENOSYS to force libc back to the filterable clone . In Delonix Capabilities: KEPT_CAPS and resolve_cap_keep in crates/adapters/delonix-linux/src/capabilities.rs ; drop_capabilities in lib.rs . seccomp: apply_seccomp in crates/adapters/delonix-linux/src/lib.rs (built with the seccompiler crate, including the clone3 → ENOSYS pre-filter); custom JSON profiles are parsed and compiled in seccomp_profile.rs ( parse , compile ). AppArmor: apply_apparmor in lib.rs . Masked and read-only paths: DEFAULT_MASKED_PATHS , DEFAULT_READONLY_PATHS , apply_masked_paths , apply_readonly_paths , mask_proc_paths in lib.rs . The node-level security decisions (policy, admission, events, score) are a separate pure crate: evaluate in crates/contexts/delonix-security-runtime/src/admission.rs ( ADR-0026"},{"p":"4. Cloud native primer","h":"4.4 OCI images, content-addressed storage and overlayfs","u":"04-cloud-native-primer.html#44-oci-images-content-addressed-storage-and-overlayfs","t":"The Open Container Initiative publishes three specifications: the image spec — an image is a manifest (JSON) that points to a config and an ordered list of layers (tarballs), and possibly an index that points to one manifest per platform; the distribution spec — the HTTP API registries serve ( /v2/<name>/manifests/<ref> , /v2/<name>/blobs/<digest> , token auth); the runtime spec — how a runtime such as runc or crun is told to run a filesystem bundle. Everything is content-addressed : a blob is named by the SHA-256 digest of its bytes, so a client verifies what it downloaded by hashing it. Pulling by digest ( name@sha256:… ) is only a guarantee if the manifest is checked against that digest as well as each blob against the manifest. At run time, layers are stacked with overlayfs : read-only lowerdir s, a writable upperdir where changes are copied up, and a workdir . Many containers can share the same lower layers. In Delonix Registry client (distribution spec): crates/adapters/delonix-oci/src/registry.rs — pull_from_registry* functions, the ACCEPT_MANIFEST media types, and verify_manifest_digest . Types come from the oci-spec crate. Content-addressed blob store: Cas in crates/adapters/delonix-oci/src/cas.rs . Writing an OCI image layout archive: write_oci_archive in save.rs . Overlay preparation: ImageStore::prepare_overlay in overlay.rs writes an overlay-lowers marker ( LOWERS_FILE ); the mount itself happens inside the container's user and mount namespaces in mount_overlay_i"},{"p":"4. Cloud native primer","h":"4.5 Container networking","u":"04-cloud-native-primer.html#45-container-networking","t":"Linux networking building blocks: a network namespace has its own interfaces, routes and firewall; a veth pair is a virtual cable with one end in each namespace; a bridge is a virtual switch joining many veth ends; nftables is the kernel packet filter and NAT engine; DNAT rewrites a destination (how a published port reaches a container), and conntrack tracks flows so reply traffic of an allowed connection passes ( ct state established,related ); slirp4netns gives an unprivileged network namespace outbound connectivity by emulating a TCP/IP stack in user space, and forwards host ports into it; VXLAN carries L2 frames over UDP between hosts, and WireGuard encrypts a tunnel. CNI (Container Network Interface) is a spec where a runtime executes plugin binaries ( bridge , host-local , portmap , …) with ADD / DEL commands and a JSON config from /etc/cni/net.d . Kubernetes runtimes use it for pod networking. In Delonix Rootless networking cannot create interfaces on the host, so the engine keeps a long-lived holder network namespace: a minimal pin process owns the namespaces, and a restartable control process serves a Unix socket. See start_pin , start_control and ensure_up in crates/adapters/delonix-sdn/src/infra.rs . Attaching a workload: attach_container (IPAM + control command) and do_attach (veth into the bridge, inside the holder). Bridge names come from bridge_name in the dependency-free crates/foundation/delonix-net-rules/src/lib.rs . Outbound and port forwarding: slirp_attac"},{"p":"4. Cloud native primer","h":"4.6 Kubernetes: CRI, kubelet, kubeadm and kind","u":"04-cloud-native-primer.html#46-kubernetes-cri-kubelet-kubeadm-and-kind","t":"The kubelet is the Kubernetes node agent. It does not run containers itself; it talks to a container runtime over the Container Runtime Interface , a gRPC API ( RuntimeService , ImageService ) served on a local Unix socket. The kubelet creates a pod sandbox ( RunPodSandbox ) and then containers inside it. It also has a cgroup driver setting ( systemd or cgroupfs ) that must match how the runtime manages cgroups, or pod cgroups and container cgroups diverge. kubeadm bootstraps a cluster on existing machines ( kubeadm init , kubeadm join ). kind runs Kubernetes nodes as containers built from the kindest/node image. In Delonix crates/interfaces/delonix-cri is a CRI runtime.v1 server. The protobuf is proto/api.proto inside that crate, compiled by build.rs with tonic-build . The binary is src/bin/delonix-cri.rs . The cgroup driver reported to the kubelet: engine_cgroup_driver in runtime_svc.rs ; its doc comment records why the answer is what it is and what would have to change for the other one. A round-trip over real gRPC is tested in crates/interfaces/delonix-cri/tests/grpc_status.rs . Cluster bootstrap commands: kubeadm over SSH in bins/delonix-runtime-bin/src/cmd/cluster.rs (with kubeadm_config.rs , etcd.rs , lb.rs ), and kind-style local clusters in kindmode.rs . Read more: Kubernetes — Container Runtime Interface , cri-api repository , Configuring a cgroup driver , kubeadm , kind ."},{"p":"4. Cloud native primer","h":"4.7 Virtualization: KVM, virtio, Cloud Hypervisor, libvirt, cloud-init","u":"04-cloud-native-primer.html#47-virtualization-kvm-virtio-cloud-hypervisor-libvirt-cloud-init","t":"KVM is the kernel's hypervisor, exposed as /dev/kvm ; a user-space VMM (QEMU, Cloud Hypervisor) uses it to run guests. virtio is the family of paravirtual devices (disk, network, 9p filesystem sharing) guests use for efficient I/O. Cloud Hypervisor is a Rust VMM focused on cloud workloads that can run unprivileged with access to /dev/kvm ; it boots guests through a firmware (an EDK2 UEFI build or rust-hypervisor-firmware ) or directly from a kernel image. libvirt manages QEMU/KVM domains described in XML, via virsh and libvirtd . Cloud images are generic; per-instance configuration (hostname, SSH keys, users, network) comes from cloud-init , which reads a datasource. The NoCloud datasource is a small ISO labelled cidata holding user-data , meta-data and optionally network-config . In Delonix The port is VmBackend in crates/adapters/delonix-vm/src/lib.rs , implemented by CloudHypervisorBackend and LibvirtBackend there and by ProxmoxBackend in crates/providers/delonix-proxmox ( ADR-0008 ). Cloud Hypervisor firmware search order: DEFAULT_CH_FIRMWARES (EDK2 CLOUDHV.fd before hypervisor-fw ); the VMM command line is built in boot_ch . libvirt domain XML: libvirt_domain_xml . NoCloud seed generation: generate_seed_iso in bins/delonix-runtime-bin/src/cmd/vm.rs . VMs on the rootless SDN get a DHCP lease derived from their MAC: dhcp_lease_ip in crates/adapters/delonix-sdn/src/infra.rs . Practical setup and known host pitfalls: 9. MicroVM setup . Read more: kernel — KVM , virtio specif"},{"p":"4. Cloud native primer","h":"4.8 Declarative reconciliation","u":"04-cloud-native-primer.html#48-declarative-reconciliation","t":"Kubernetes popularised a model where users submit desired state as typed objects ( apiVersion , kind , metadata , spec ), and controllers repeatedly compare it with actual state and act to converge. kubectl apply adds a three-way diff : it stores the last applied configuration on the object, so it can tell \"you removed this field from your file\" (revert it) from \"someone set this field by hand\" (leave it alone). In Delonix The engine has its own Kinds in API groups ( delonix api-resources lists them). Facts about each Kind (domain, whether it converges, teardown, namespacing) live in one table: KindFacts in crates/contexts/delonix-stack/src/kinds.rs . The planner is pure: plan(desired, actual, stack) in crates/contexts/delonix-stack/src/reconcile.rs . The module doc has the three-way truth table, and the last-applied spec is stored on the resource itself under the LAST_APPLIED annotation ( delonix.io/last-applied ) — there is no separate state file. Ownership is a label on the resource; revision history is in revision.rs ( ADR-0019 ). Manifests are parsed in bins/delonix-runtime-bin/src/cmd/manifest.rs ; stack plan / apply live in cmd/stack.rs . There is no controller loop running in the background: reconciliation happens when a command runs (daemonless). The proposed pull reconciler keeps that property by being a systemd timer that invokes the same apply, not a resident process ( ADR-0021 , status Proposed ). Read more: Kubernetes — Objects , Controllers , Declarative manage"},{"p":"4. Cloud native primer","h":"4.9 Observability and the MCP interface","u":"04-cloud-native-primer.html#49-observability-and-the-mcp-interface","t":"OpenTelemetry is a CNCF standard for traces, metrics and logs, exported over OTLP to a collector. Prometheus scrapes metrics from an HTTP /metrics endpoint in a text exposition format. The Model Context Protocol is an open protocol that lets AI clients discover and call tools exposed by a server, commonly over stdio. In Delonix Structured logging and optional OTLP spans: init in crates/adapters/delonix-telemetry/src/telemetry.rs (spans are exported only when DELONIX_OTLP_ENDPOINT is set; the exporter runs on its own thread so the synchronous CLI needs no async runtime). Prometheus registry (prefix delonix ) and text encoding: crates/adapters/delonix-telemetry/src/metrics.rs ( encode ). The local management API serves /metrics in crates/interfaces/delonix-mgmt/src/lib.rs . MCP: crates/interfaces/delonix-mcp (built on rmcp , stdio transport), with the binary in bins/delonix-mcp-bin . Scope and limits: ADR-0025 . Read more: OpenTelemetry documentation , OTLP specification , Prometheus — exposition formats , Model Context Protocol ."},{"p":"4. Cloud native primer","h":"4.10 Daemonless, in one paragraph","u":"04-cloud-native-primer.html#410-daemonless-in-one-paragraph","t":"containerd and the Docker Engine keep a resident daemon that owns container state; Podman showed that a runtime can instead be a command that exits, with per-container helper processes and systemd for anything that must persist. Delonix follows the second model: the CLI does the work and exits, state is files under the state root guarded by flock (see 3. Rust primer §3.8 ), a supervisor process exists per detached container, the network holder exists only while something needs it, and boot persistence is systemd units ( bins/delonix-runtime-bin/src/cmd/boot.rs ). The consequences — good and bad — are discussed in 5. Architecture and 7. System design interview ."},{"p":"5. Architecture","h":"5. Architecture","u":"05-architecture.html#5-architecture","t":"This page is the map a contributor needs before touching the backend: what the engine is, how the crates are layered, which processes exist at runtime, where state lives on disk, and how the pieces talk to each other. Every structural claim names the file and symbol it was checked against. The canonical, longer document is ARCHITECTURE.md at the repository root; the decisions behind the structure are in docs/adr/ , above all ADR-0040 . Two halves on this page. The layer table and the crate graph are generated by python3 scripts/dev_docs.py from Cargo.toml and scripts/arch_fitness.py — do not edit them by hand. Everything else is narrative and is reviewed after each release."},{"p":"5. Architecture","h":"Engine identity and boundaries","u":"05-architecture.html#engine-identity-and-boundaries","t":"The canonical text is the section «Identidade e fronteira do motor» at the top of AGENTS.md . In short: What it is. An execution abstraction for one node : it runs containers and microVMs and manages the networking and storage they need. It is declarative, with its own Kinds grouped by apiVersion ( core , compute , networking , gateway , storage , artifact , infrastructure — the table is crates/contexts/delonix-stack/src/kinds.rs , and delonix api-resources prints it). Providers sit behind ports. The Linux kernel, Cloud Hypervisor and libvirt, Proxmox VE and the Kubernetes CRI are reached through a trait, never through if provider == … spread across the code. Today's ports: VmBackend ( crates/adapters/delonix-vm/src/lib.rs ) and the compute ports in crates/contexts/delonix-compute/src/ports.rs and launch.rs ( ImageStore , StorageProvider , DeviceResolver , RunHost , NetworkProvider , VmNetwork , WorkloadRuntime ). An OpenStack backend is designed ( ADR-0039 , Proposed ) but has no crate yet. Cloud native — plan / apply / drift, the same operations exposed by several interfaces, observability through OpenTelemetry and Prometheus ( crates/adapters/delonix-telemetry ). Daemonless — no resident process by default. What must persist belongs to systemd or to a per-workload process with a clear owner (a container's supervisor, the network pin). Rootless-first — the normal path runs as an unprivileged user; privilege is an explicit opt-in ( --privileged , vm bridge ). Knows no consum"},{"p":"5. Architecture","h":"Layers and the allowed direction","u":"05-architecture.html#layers-and-the-allowed-direction","t":"ADR-0040 D1 fixes one dependency direction: interfaces ─► contexts (domain + use cases + ports) ◄─ adapters / providers │ ▲ └──────────────────── composes (bins/) ────────────────┘ Foundation ( crates/foundation/ ) — shared, pure-ish types every layer may name. Contexts ( crates/contexts/ ) — one crate per bounded context, named after the published API groups: use cases and the ports they need. No kernel, no HTTP, no provider. Adapters ( crates/adapters/ ) and providers ( crates/providers/ ) — implement ports: kernel, SDN, OCI store, VM backends; providers bring an HTTP client for one remote target. Interfaces ( crates/interfaces/ ) — CRI, management API, MCP: parse a request, call the engine, present. Binaries ( bins/ ) — composition roots. The layer each crate belongs to, and the direction it may depend in: Layer May depend on Foundation foundation Contexts foundation, contexts Adapters foundation, contexts Providers foundation, contexts Interfaces foundation, contexts, adapters, providers Binaries foundation, contexts, adapters, providers, interfaces Declared exceptions (each one names the ADR-0040 phase that removes it): delonix-mcp → delonix-mgmt — removed in P5 delonix-proxmox → delonix-vm — removed in P4 delonix-scanner → delonix-oci — removed in P4"},{"p":"5. Architecture","h":"Where the restructuring stands","u":"05-architecture.html#where-the-restructuring-stands","t":"ADR-0040 is a strangler plan in phases (P0 rails → P1 contract → P2 contexts → P3 adapters and binaries → P4 providers → P5 node API → P6 CRI → P7 observability). What the code shows today: P0 is done. Every crate lives in its layer's directory, versions are workspace-level, and the fitness gate runs in CI. P1 is done as a contract, not as a server. proto/delonix/node/v1/*.proto exists, the OpenAPI document docs/api/openapi.yaml is generated from it, and scripts/contract_gate.py guards both. Nothing serves the contract yet — no crate references delonix.node.v1 ( ADR-0042 D1 says the same). P2 has started. delonix-model (the shared Error and its DX_* codes, generated names, exit classes), delonix-stack (Kind table, 3-way reconciler, revisions) and delonix-compute (the one run specification RunOpts , resolve_run , build_record , the network and launch use cases) exist. Most application logic still lives in bins/delonix-runtime-bin/src/cmd/ . P3 is under way. The compute ports are implemented in adapters ( HostImages , HostVolumes , HostDevices , HostRuntime , HostNetwork , HostWorkload , HostVmNetwork ), telemetry left the foundation for delonix-telemetry , delonix-vm reaches the SDN only through the VmNetwork port, and the CRI, management API and MCP servers became their own executables. Four adapters carry their ADR-0040 names: delonix-scanner (was delonix-scan ), delonix-oci (was delonix-image ), delonix-sdn (was delonix-net ) and delonix-linux (was delonix-runtime , the con"},{"p":"5. Architecture","h":"Executables and processes at runtime","u":"05-architecture.html#executables-and-processes-at-runtime","t":"This is the C4 container level: the things that run. The build ships four executables (see the generated count in the handbook README ); several more processes appear per workload or per node, each with an owner. Process Born in Lives for delonix bins/delonix-runtime-bin/src/main.rs ( main , run ) one command. main intercepts the hidden entry points ( netns pin , netns control , netns run , __rmtree , __volsnap , __ovlmigrate , __ovlhold , __duusage , __buildtar , __apirun , __netnsconnect ) before clap parses delonix-cri crates/interfaces/delonix-cri/src/bin/delonix-cri.rs → delonix_cri::serve_blocking a service (typically a systemd unit). delonix serve cri exec s it ( cmd/serve.rs::exec_server ) delonix-mgmt bins/delonix-mgmt-bin/src/main.rs → delonix_mgmt::serve_blocking a service; delonix serve api exec s it delonix-mcp bins/delonix-mcp-bin/src/main.rs → delonix_mcp::serve_stdio one AI-client session (a child process on stdio); delonix mcp exec s it Docker API slice cmd/serve.rs → cmd::dockerapi::run , inside the delonix process while delonix serve docker-api runs supervisor delonix_linux::supervise::run_supervised , chosen by delonix_compute::launch::start for every detached start the caller can fork for the container's life; it is the real parent, so it collects the exit status and applies --restart container init delonix_linux::spawn → clone → container_init the container log shim fork inside spawn , running log_shim the container per-container slirp4netns delonix_sdn:"},{"p":"5. Architecture","h":"One set of operations, several interfaces","u":"05-architecture.html#one-set-of-operations-several-interfaces","t":"Interface Transport Entry Status CLI argv bins/delonix-runtime-bin the complete surface CRI ( runtime.v1 ) gRPC on a unix socket, 0600 + SO_PEERCRED delonix_cri::serve_blocking serves the kubelet Management API HTTP+JSON on a unix socket, same uid only delonix_mgmt::serve_blocking (routes such as /v1/containers , /v1/volumes , /metrics ) local only ( ADR-0010 rejected a remote API); to be replaced by the node contract MCP stdio delonix_mcp::serve_stdio local, no tenant ( ADR-0025 ) Docker Engine API slice HTTP on a unix socket cmd::dockerapi::run a compatibility slice, inside delonix Node contract delonix.node.v1 gRPC and HTTP/JSON on one unix socket proto/delonix/node/v1/ contract only — no server yet The node contract is the intended single API ( ADR-0040 D4, ADR-0042 ). The .proto files are the source of truth; docs/api/openapi.yaml is generated from them and never edited by hand. scripts/contract_gate.py fails on: buf format , buf lint , buf breaking against the last tag that carries proto/ , an RPC without an HTTP mapping (or a bidirectional stream with one), an OpenAPI document that differs from the generated one, and two paths that are the same URL under different variable names. Three rules it protects: one request message per RPC, explicit identity ( namespace / name ) in the request, and images addressed by query parameter."},{"p":"5. Architecture","h":"State on disk","u":"05-architecture.html#state-on-disk","t":"There is no database. State is files under one state root : DELONIX_ROOT when set; otherwise $XDG_DATA_HOME/delonix or ~/.local/share/delonix for an unprivileged user and /var/lib/delonix for root ( bins/delonix-runtime-bin/src/cmd/util.rs::state_root → ImageStore::default_root ; infra::base_root resolves the same rule on the network side). Sockets do not live under the state root. They are in a short per-user runtime directory ( infra::runtime_dir , overridable with DELONIX_NET_RUNTIME_DIR ) because AF_UNIX paths are limited in length; a non-default root gets a hashed suffix ( root_suffix ) so two roots on one login never share sockets. When you run anything in isolation, set both variables. Path under the root What Code containers/<id>.json one JSON record per container delonix_runtime_core::Store ( store.rs ) containers/<id>/{upper,work,merged} + overlay-lowers the container's writable layer and the list of shared image layers it mounts ImageStore::prepare_overlay ( delonix-oci/src/overlay.rs ) images/<id>.json , layers/<hex>/ , blobs/sha256/<hex> image metadata, unpacked layers shared by every container, content-addressed blobs ImageStore::open ( image.rs ), Cas ( cas.rs ) volumes/<name>/_data , volumes/.ns/<ns>/ named volumes, namespace-scoped volumes VolumeStore ( delonix-volume/src/lib.rs ) vms/ VM records ( JsonStore ) and per-VM files delonix-vm vm-images/ VM images ( .qcow2 + .json ) cmd/vmimage.rs::VmImageStore secrets/ encrypted secrets SecretStore ( delonix-runti"},{"p":"5. Architecture","h":"How the crates communicate","u":"05-architecture.html#how-the-crates-communicate","t":"Direct Rust calls, along the layer direction. The normal case. For example cmd_run ( cmd/container.rs ) calls delonix_compute::run::resolve_run with the adapters delonix_oci::run_images::HostImages , delonix_volume::HostVolumes , delonix_linux::cdi::HostDevices and delonix_linux::run_host::HostRuntime , then delonix_compute::network::{attach_custom_network, wire_network} with delonix_sdn::run_network::HostNetwork , then delonix_compute::launch::start with delonix_linux::workload::HostWorkload . Registration at the composition root. run() in bins/delonix-runtime-bin/src/main.rs registers configured remote VM backends ( cmd::vmbackends::register_configured → delonix_vm::register_backend ) and the SDN implementation of the VM network port ( delonix_vm::set_network(HostVmNetwork) ) before any command runs. Re-executing the engine's own binary. Still common, and counted by the self_exec_sites ratchet. The reasons are real: - clone is only safe in a single-threaded process, and the CRI, management API and Docker API servers are multi-threaded tokio runtimes. They hand a typed RunOpts in a 0600 file to a fresh delonix __apirun <spec> ( lifecycle.rs::write_run_spec , cmd::dockerapi::run_from_spec_file ). - A rootless process must enter the network pin's user and mount namespaces before a container can join a named netns there, so reexec_into_netns runs nsenter … ip netns exec <netns> delonix netns run <spec> . - Work on files owned by mapped subuids needs a process inside a mapped us"},{"p":"5. Architecture","h":"Two flows, as sequences","u":"05-architecture.html#two-flows-as-sequences","t":""},{"p":"5. Architecture","h":"container run -d --net web -p 8080:80 nginx, rootless","u":"05-architecture.html#container-run-d-net-web-p-808080-nginx-rootless","t":"Every arrow below is a call in cmd_run ( bins/delonix-runtime-bin/src/cmd/container.rs ) or in the functions it reaches. Without a custom network the flow has no second pass: spawn creates its own user namespace, and the parent writes the id maps ( write_userns_maps ), sets up the cgroup, runs the on_started hook (the per-container slirp_attach when there are -p ports) and only then sends the \"go\" byte to the child."},{"p":"5. Architecture","h":"CRI: RunPodSandbox → CreateContainer → StartContainer","u":"05-architecture.html#cri-runpodsandbox-createcontainer-startcontainer","t":"From crates/interfaces/delonix-cri/src/runtime_svc/lifecycle.rs ."},{"p":"5. Architecture","h":"Known limitations","u":"05-architecture.html#known-limitations","t":"Note — the node contract is not served. proto/delonix/node/v1 is gated and generates OpenAPI, but no process answers it. Integrations today use the CLI, the CRI, the local management API or MCP. Note — the servers still run the CLI. delonix-cri , delonix-mgmt and delonix-mcp start workloads by re-executing delonix . This keeps clone out of multi-threaded processes, at the cost of a process per operation and error text crossing a process boundary. Note — macvlan / ipvlan are declared, not realized. network create records them and reports Realized=False with reason DriverNotImplemented ( bins/delonix-runtime-bin/src/cmd/network.rs ): their physical plane needs CAP_NET_ADMIN in the host's initial network namespace. Note — recovery after the pin dies is by restart. If the control process dies, ensure_up restarts only it and no workload moves. If the pin dies, the netns is rebuilt and delonix net netns up restarts the stranded containers and pod members ( cmd/netns.rs::reconcile_after_respawn , which reads the container store only — VMs are not recovered this way). Note — IPv6 in the SDN is off by default. The ingress firewall is table ip ; the holder installs a dropping table ip6 ( infra::ingress_v6_refusal_ruleset ) and disables IPv6 inside container netns unless DELONIX_ENABLE_IPV6=1 ( ipv6_sdn_enabled ). Note — a caller that cannot fork starts unsupervised. launch::should_supervise requires detach && forkable ; without a supervisor nobody is the process's parent and the real e"},{"p":"5. Architecture","h":"Where to start reading","u":"05-architecture.html#where-to-start-reading","t":"Area Start here CLI entry and hidden re-exec entry points bins/delonix-runtime-bin/src/main.rs ( main , run ) container run end to end cmd/container.rs::cmd_run , then delonix-compute/src/{run,network,launch}.rs Process creation, namespaces, rootfs, seccomp, cgroups delonix-linux/src/lib.rs ( spawn , container_init , setup_rootfs , setup_cgroup ), supervise.rs , launch_spec.rs Rootless networking delonix-sdn/src/infra.rs ( ensure_up , control_main , attach_container , publish_port , ingress_table_ruleset , fw_chain_body ), pin_userns.rs , ipam.rs Images delonix-oci/src/{registry,cas,image,overlay,build}.rs VMs delonix-vm/src/lib.rs ( VmBackend , builtin_backends , register_backend , select_backend ), cloudinit.rs ; cmd/vm.rs , cmd/vmimage.rs Declarative apply delonix-stack/src/{kinds,reconcile}.rs ; cmd/stack.rs , cmd/manifest.rs CRI delonix-cri/src/lib.rs::serve_blocking , runtime_svc.rs , runtime_svc/lifecycle.rs Management API / MCP delonix-mgmt/src/lib.rs , delonix-mcp/src/lib.rs Node contract proto/delonix/node/v1/ , scripts/contract_gate.py , docs/api/openapi.yaml Architecture rules scripts/arch_fitness.py , ADR-0040"},{"p":"6. The crates","h":"6. The crates","u":"06-crates.html#6-the-crates","t":"This page is the map you keep open while reading the code. The table below is generated from Cargo.toml and scripts/arch_fitness.py ; everything after it is written by hand, and every pointer ( path:symbol ) was read in the tree before it was written down. Where a claim could not be confirmed it is not here. How to use it: Find the crate that owns what you want to change (layer first, see 05 — Architecture for why the layers exist and which direction a dependency may point). Read its Start reading at list in order, then its Gotchas : each one is a trap this code base has already paid for, and the comment that records it is still in the file. Before you add a use delonix_… line, check the table: an edge that is not in the \"Depends on\" column will fail scripts/arch_fitness.py unless it goes in the allowed direction. Two conventions you will meet everywhere: Pure versus effect. Contexts and foundation crates decide; adapters touch the kernel, the disk, a subprocess or the network. When a use case in a context needs an effect, it declares a port (a trait) and an adapter implements it. The composition root that wires ports to adapters is the delonix binary. \"Talks to\" means the mechanism, not just the dependency. A crate may depend on another and still reach it by running the delonix binary as a subprocess (the CRI and the local management API do this for anything that forks), or by writing a line on a unix control socket (the network holder)."},{"p":"6. The crates","h":"Reference table","u":"06-crates.html#reference-table","t":"Crate Layer Path Binaries Depends on (engine crates) Used by delonix-model Foundation crates/foundation/delonix-model — — delonix-runtime-bin , delonix-runtime-core , delonix-scanner delonix-net-rules Foundation crates/foundation/delonix-net-rules — — delonix-sdn , delonix-vm delonix-runtime-core Foundation crates/foundation/delonix-runtime-core — delonix-model delonix-compute , delonix-cri , delonix-linux , delonix-mcp , delonix-mcp-bin , delonix-mgmt , delonix-mgmt-bin , delonix-oci , delonix-proxmox , delonix-runtime-bin , delonix-sdn , delonix-security-runtime , delonix-stack , delonix-truenas , delonix-vm , delonix-volume delonix-compute Contexts crates/contexts/delonix-compute — delonix-runtime-core delonix-cri , delonix-linux , delonix-oci , delonix-runtime-bin , delonix-sdn , delonix-vm , delonix-volume delonix-security-runtime Contexts crates/contexts/delonix-security-runtime — delonix-runtime-core delonix-runtime-bin delonix-stack Contexts crates/contexts/delonix-stack — delonix-runtime-core delonix-runtime-bin delonix-linux Adapters crates/adapters/delonix-linux — delonix-compute , delonix-runtime-core delonix-cri , delonix-mcp , delonix-mgmt , delonix-runtime-bin delonix-oci Adapters crates/adapters/delonix-oci — delonix-compute , delonix-runtime-core delonix-cri , delonix-mgmt , delonix-runtime-bin , delonix-scanner delonix-scanner Adapters crates/adapters/delonix-scanner — delonix-model , delonix-oci delonix-mgmt , delonix-runtime-bin delonix-sdn Adapters crates"},{"p":"6. The crates","h":"Foundation","u":"06-crates.html#foundation","t":"Foundation crates carry no mechanism: types, pure rules and the on-disk state format. They may depend only on other foundation crates."},{"p":"6. The crates","h":"delonix-runtime-core Source","u":"06-crates.html#delonix-runtime-core","t":"Purpose. The shared vocabulary of the engine: the persisted records ( Container , Vm ), their Status , the error type every crate returns (defined in delonix-model and re-exported here with the same path), and the JSON-file stores with atomic writes. It also holds the small cross-cutting pieces that more than one crate needs and that would otherwise be copied: the SO_PEERCRED check for local sockets, the append-only event log, the encrypted secret store, and the rule a server binary follows when delonix runs it. It does not create processes, mount, or configure the network, and it has no notion of tenant, plan or billing (its crate doc says so, and the rest of the workspace relies on it). Key modules Module Responsibility lib.rs Container , Vm , Status , Mount , ContainerFw / FwRule , health config, cgroup-parent parsing, generate_id , pid liveness helpers store Store (one JSON file per container) and JsonStore<T> ; atomic write helpers events append-only events.jsonl event log ( emit , read ) secret , cred_vault named secrets encrypted at rest (XChaCha20-Poly1305) dispatch version check and CLI resolution for server binaries run by delonix peer_cred peer_uid from SO_PEERCRED typestate compile-time lifecycle phases ( Phase<Created/Running/Stopped> ) virt virtualization/virtio detection from /sys and /proc workload_net the workload IPv4 range, defined once Main public API Item What it is Where Container the container record everything reads and writes crates/foundation/delonix"},{"p":"6. The crates","h":"delonix-model Source","u":"06-crates.html#delonix-model","t":"Purpose. The part of the model any layer can name without depending on a mechanism: the engine's shared Error type with the stable DX_* code of each variant, the generated workload names, and the mapping from an Error to a process exit code. Pure — no I/O, no process state (crate doc). It does not hold records; those are in delonix-runtime-core . Key modules Module Responsibility error Error , Result , and Error::code (the DX_* string of each variant) exitcode exit-code classes ( NOT_RUNNING , NOT_FOUND , CONFLICT , …) and for_error names default names ( derived_name , random_name ) Main public API Item What it is Where Error::code the stable machine code ( DX_* ) of an error crates/foundation/delonix-model/src/error.rs:code exitcode::for_error the one place an Error becomes an exit code crates/foundation/delonix-model/src/exitcode.rs:for_error exitcode::merge the code for a batch of results crates/foundation/delonix-model/src/exitcode.rs:merge names::derived_name deterministic name from an id crates/foundation/delonix-model/src/names.rs:derived_name Talks to. No other engine crate: it is now a root of the graph, and delonix-runtime-core depends on it (the dependency used to point the other way). The CLI re-exports both modules as cmd::exitcode and cmd::names ( bins/delonix-runtime-bin/src/cmd/mod.rs ), so older call sites did not change. Notable external dependencies. thiserror (the Error derive) and serde_json (the Error::Json variant wraps serde_json::Error ). Tests. Inlin"},{"p":"6. The crates","h":"delonix-net-rules Source","u":"06-crates.html#delonix-net-rules","t":"Purpose. Network rules that can be computed without touching the kernel: bridge names, IP derivation inside a prefix, the Cidr value type, label matching, parsing of iptables-save output. It has zero dependencies , so any caller can compile the same rules the engine uses. It deliberately excludes anything that reads shared state (IP allocation reads the IPAM registry, so it stays in delonix-sdn ). Key modules. A single lib.rs . Main public API Item What it is Where Cidr IPv4 prefix type, no external crate crates/foundation/delonix-net-rules/src/lib.rs:Cidr bridge_name the one formula for a network's bridge device name crates/foundation/delonix-net-rules/src/lib.rs:bridge_name derive_ip_in , valid_ip_in_subnet preferred address for an id, and membership check crates/foundation/delonix-net-rules/src/lib.rs matches_labels label selector matching (used by kind: Service ) crates/foundation/delonix-net-rules/src/lib.rs:matches_labels parse_overlay_peer parse an overlay peer spec crates/foundation/delonix-net-rules/src/lib.rs:parse_overlay_peer Talks to. Nothing. delonix-sdn re-exports its items, so callers of delonix_sdn::Cidr etc. keep compiling. Notable external dependencies. None. Tests. Inline unit tests. Start reading at. src/lib.rs — the module doc lists what was left out and why. Gotchas. Parts of the module doc are still in Portuguese (LANG-01 debt); the code is the reference."},{"p":"6. The crates","h":"Contexts","u":"06-crates.html#contexts","t":"A context owns a domain's decisions and the ports its use cases need. No context touches the kernel."},{"p":"6. The crates","h":"delonix-compute Source","u":"06-crates.html#delonix-compute","t":"Purpose. The Compute context ( compute.delonix.io ): the run specification every entry point translates into ( RunOpts ), and the container run use case as pure steps over ports — preflight, resolve, build the record, wire the network, start. It also holds the Pod specification types and their translation to RunOpts . It does not spawn processes, pull images or configure networks; it calls traits that adapters implement. Key modules Module Responsibility run_opts RunOpts , the one run specification preflight refuse flag combinations that mean nothing, before any effect run resolve_run (through ports) and build_record (pure) network the network phase: attach_custom_network , wire_network launch Launch intent, WorkloadRuntime port, start use case, restart policy ports ImageStore , StorageProvider , DeviceResolver , RunHost , NetworkProvider , VmNetwork pod Pod spec types and pod_to_run_opts / container_to_run_opts notice Notice , a warning returned as data instead of printed Main public API Item What it is Where RunOpts the run specification crates/contexts/delonix-compute/src/run_opts.rs:RunOpts preflight::check_run_opts pure refusal of impossible combinations crates/contexts/delonix-compute/src/preflight.rs:check_run_opts run::resolve_run resolve image, volumes, devices, user, defaults through ports crates/contexts/delonix-compute/src/run.rs:resolve_run run::build_record turn spec + resolution into a Container (pure) crates/contexts/delonix-compute/src/run.rs:build_record net"},{"p":"6. The crates","h":"delonix-stack Source","u":"06-crates.html#delonix-stack","t":"Purpose. The Stack context ( core.delonix.io ): the table of Kinds and their facts, the three-way reconciler that plans a manifest against what exists, and the revision history of an apply. Planning is pure — nothing here opens a store of a concrete resource or runs a command (crate doc). Loading manifests and applying each Kind stay in the CLI. Key modules Module Responsibility kinds Kind name constants and KindFacts (domain, form, converges, teardown, namespaced, presence) reconcile Desired / Actual / Change , plan , the ownership label and last-applied annotation revision record and list apply revisions (for rollback) condition the Condition type Main public API Item What it is Where kinds::facts , kinds::stack_kinds , kinds::converges the single table the CLI consults per Kind crates/contexts/delonix-stack/src/kinds.rs reconcile::plan desired vs actual → Vec<Change> crates/contexts/delonix-stack/src/reconcile.rs:plan reconcile::STACK_LABEL , LAST_APPLIED ownership label and three-way diff annotation crates/contexts/delonix-stack/src/reconcile.rs reconcile::hot_fields_for which field changes can be applied live crates/contexts/delonix-stack/src/reconcile.rs:hot_fields_for revision::record , revision::list apply history crates/contexts/delonix-stack/src/revision.rs Talks to. delonix-runtime-core only. The CLI re-exports kinds , reconcile and revision as cmd::kinds etc. ( bins/delonix-runtime-bin/src/cmd/mod.rs ). Notable external dependencies. serde , serde_json . Tests. In"},{"p":"6. The crates","h":"delonix-security-runtime Source","u":"06-crates.html#delonix-security-runtime","t":"Purpose. The node's security decisions : the policy file, the single admission evaluation for containers and VMs, the security event, an explainable posture score, and redaction of secrets in text. Pure functions of their arguments. It deliberately has no sensors, watchers or resident process (crate doc: daemonless by design), and no tenant, project or environment field anywhere. Key modules Module Responsibility policy SecurityPolicy , Mode , lints admission Request , evaluate , Decision , Violation event SecurityEvent on the engine's event log score Score with deductions and reasons redact redaction of sensitive keys/values in hostile input severity Severity , ActionRisk , Confidence Main public API Item What it is Where SecurityPolicy::parse load a policy crates/contexts/delonix-security-runtime/src/policy.rs:SecurityPolicy admission::evaluate decide one request crates/contexts/delonix-security-runtime/src/admission.rs:evaluate admission::Request container or VM admission input crates/contexts/delonix-security-runtime/src/admission.rs:Request redact::redact_text mask secrets in text crates/contexts/delonix-security-runtime/src/redact.rs:redact_text Talks to. delonix-runtime-core ( events , now_unix ). Consumed by the CLI through bins/delonix-runtime-bin/src/cmd/policy.rs , which cmd_run calls before any image is resolved. Notable external dependencies. serde , serde_json . Tests. Inline unit tests, including a boundary_tests module in lib.rs and a doc-test in the crate doc"},{"p":"6. The crates","h":"Adapters","u":"06-crates.html#adapters","t":"Adapters are where the engine meets the kernel, the disk, host tools and remote registries. They depend on foundation and contexts, never on each other (the declared exceptions are listed in 05 )."},{"p":"6. The crates","h":"delonix-linux Source","u":"06-crates.html#delonix-linux","t":"Purpose. The low-level container runtime: clone with namespaces, pivot_root , cgroups v2, capabilities and seccomp, exec via setns , stop and remove, and the detached supervisor behind run -d . Its crate doc states the rule: the syscall boundary for containers lives here. It does not resolve images, parse CLI flags or configure the network; network effects arrive as hooks from the caller. Key modules Module Responsibility lib.rs RunSpec , create_with / spawn , container_init , rootfs setup and overlay mount, exec , stop , remove , live mounts, cgroups, reconcile_status workload HostWorkload , the WorkloadRuntime port implementation launch_spec run_spec , the one builder of RunSpec from a Launch supervise run_supervised , the forked parent of a detached container capabilities capability name↔number table and default set seccomp_profile OCI seccomp profile loading cdi CDI device spec consumer ( HostDevices ) run_host HostRuntime , the RunHost port implementation regulate , resource_advice , workload_view resource pressure, host advice, requested-vs-enforced view Main public API Item What it is Where RunSpec everything a spawn needs crates/adapters/delonix-linux/src/lib.rs:RunSpec create_with start a container (calls spawn ) crates/adapters/delonix-linux/src/lib.rs:create_with exec run a command inside a running container crates/adapters/delonix-linux/src/lib.rs:exec stop , remove lifecycle crates/adapters/delonix-linux/src/lib.rs reconcile_status refresh a record against the li"},{"p":"6. The crates","h":"delonix-oci Source","u":"06-crates.html#delonix-oci","t":"Purpose. OCI images: a content-addressed blob store, the image store and its metadata, registry pull/push with auth, per-container rootfs preparation (shared overlay layers), Dockerfile/Delonixfile parsing and build helpers, Cloud Native Buildpacks planning, archive load/save, and signature sign/verify. It does not run containers; a build runs its steps through the CLI. Key modules Module Responsibility cas Cas , sha256-addressed blobs image Image , ImageConfig , ImageStore registry reference parsing, resolve_or_pull , pull/push, OCI artifacts overlay prepare_container_rootfs , prepare_overlay , existing_rootfs_path build Dockerfile parser ( parse_dockerfile ), stages, commit_flat_rootfs run_images HostImages , the compute ImageStore port auth registry credentials ( login / lookup ) load , save Docker archive in, OCI archive out sign sign_image , verify_signature (ECDSA P-256) buildpack , detect , internal_registry CNB plan, language detection, throwaway registry rootfs_user --user resolution against a rootfs Main public API Item What it is Where ImageStore open, resolve, list, remove images crates/adapters/delonix-oci/src/image.rs:ImageStore registry::resolve_or_pull local image or pull crates/adapters/delonix-oci/src/registry.rs:resolve_or_pull pull_from_registry_with_creds pull with credentials (used by the CRI) crates/adapters/delonix-oci/src/registry.rs ImageStore::prepare_container_rootfs rootfs for a container id crates/adapters/delonix-oci/src/overlay.rs build::parse_"},{"p":"6. The crates","h":"delonix-sdn Source","u":"06-crates.html#delonix-sdn","t":"Purpose. The rootless SDN and the firewall. A long-lived pin process holds a user+network namespace; a restartable control process inside it serves a unix control socket and owns the bridges, nftables rules, DHCP and internal DNS; one slirp4netns bridges that namespace to the host. It also covers the slirp-per-container path for -p without a custom network, IPAM, CNI plugin execution, WireGuard overlay, and optional eBPF flow accounting. It re-exports delonix-net-rules . It does not spawn containers. Key modules Module Responsibility lib.rs NetworkStore , publish spec parsing, slirp_attach , slirp orphan reaping infra the holder: ensure_up , acquire , attach_container , publish_port , apply_firewall_all , network_route , vm_attach , the control socket run_network HostNetwork ( NetworkProvider port), publish_with_retry vm_network HostVmNetwork ( VmNetwork port) ipam lease registry for addresses cni CNI conformance: run plugin binaries wg WireGuard over the overlay bpf optional eBPF flow accounting discover listening ports of a workload from /proc/<pid>/net pin_userns the pin's own namespaces and id maps Main public API Item What it is Where NetworkStore declarative network registry crates/adapters/delonix-sdn/src/lib.rs:NetworkStore parse_publish , parse_publish_addr -p grammar crates/adapters/delonix-sdn/src/lib.rs slirp_attach a container's own slirp, with host forwards crates/adapters/delonix-sdn/src/lib.rs:slirp_attach infra::ensure_up bring the holder up (pin + control + "},{"p":"6. The crates","h":"delonix-vm Source","u":"06-crates.html#delonix-vm","t":"Purpose. MicroVMs and VMs behind the VmBackend trait and a runtime registry of backends. Cloud Hypervisor and libvirt are the local backends; a remote backend registers itself from outside the crate. It owns VM records, boot and lifecycle, snapshots, cloud-init seed generation, and backend selection (explicit, default file, or auto-detection). It does not hold an HTTP client or provider credentials. Key modules Module Responsibility lib.rs VmConfig , VmBackend , registry, CloudHypervisorBackend , LibvirtBackend , create_with , start / stop / remove , snapshots, status / list cloudinit build_user_data , build_network_config , generate_seed_iso Main public API Item What it is Where VmBackend the backend port ( boot , stop , destroy , resume , snapshot , ip , manages_own_storage , auto_selectable , …) crates/adapters/delonix-vm/src/lib.rs:VmBackend register_backend , BackendRegistration add a backend by factory crates/adapters/delonix-vm/src/lib.rs set_network register the VmNetwork port once per process crates/adapters/delonix-vm/src/lib.rs:set_network VmConfig what to create crates/adapters/delonix-vm/src/lib.rs:VmConfig create_with , start , stop , remove , status , list lifecycle crates/adapters/delonix-vm/src/lib.rs snapshot , restore , snapshots , delete_snapshot checkpoints crates/adapters/delonix-vm/src/lib.rs valid_vm_name name validation at the engine boundary crates/adapters/delonix-vm/src/lib.rs:valid_vm_name Talks to. delonix-runtime-core , delonix-compute (the VmNe"},{"p":"6. The crates","h":"delonix-volume Source","u":"06-crates.html#delonix-volume","t":"Purpose. Named volumes ( <root>/volumes/<name>/_data ) and bind mounts, including the -v grammar, quotas and usage measurement, network-backed volumes (NFS/CIFS/WebDAV mounted by host tools), shares under a parent volume, and snapshots. It implements the compute StorageProvider port. It does not create a NAS dataset (that is delonix-truenas ). Key modules. A single lib.rs . Main public API Item What it is Where VolumeStore create, list, remove, quota, mount crates/adapters/delonix-volume/src/lib.rs:VolumeStore VolumeStore::resolve_spec -v spec → Mount crates/adapters/delonix-volume/src/lib.rs Volume the volume record crates/adapters/delonix-volume/src/lib.rs:Volume measure , Usage disk usage with an unreadable counter crates/adapters/delonix-volume/src/lib.rs HostVolumes StorageProvider adapter crates/adapters/delonix-volume/src/lib.rs:HostVolumes Talks to. delonix-runtime-core , delonix-compute . Host tools: mount , umount , losetup . Removal of trees owned by mapped uids is injected by the caller ( remove_with takes an rmtree closure; the CLI passes delonix_linux::remove_tree_mapped ). Notable external dependencies. serde , serde_json . Tests. Inline unit tests. Start reading at. VolumeStore in src/lib.rs , then resolve_spec , then ensure_mounted . Gotchas. An unreadable directory is not an empty one: Usage.unreadable > 0 means bytes is a lower bound. In rootless mode a database volume made 0700 by a mapped uid is the normal case."},{"p":"6. The crates","h":"delonix-scanner Source","u":"06-crates.html#delonix-scanner","t":"Purpose. Image vulnerability scanning without root and without running the image: extract an SBOM (Alpine apk , Debian/Ubuntu dpkg ) by reading layers from the CAS, and match it against an advisory database. A pytree module scans Python module trees (manifest and dependency checks). It does not download the advisory database itself (no HTTP client in its dependencies). Key modules Module Responsibility lib.rs extract_sbom , AdvisoryDb , Finding , advisories_from_osv , version comparison error the crate's own Error , converted into the engine's delonix_model::Error class ( DX_* codes) pytree Python module-tree scanning Main public API Item What it is Where extract_sbom packages of an image crates/adapters/delonix-scanner/src/lib.rs:extract_sbom AdvisoryDb advisories to match against crates/adapters/delonix-scanner/src/lib.rs:AdvisoryDb advisories_from_osv load OSV-format advisories crates/adapters/delonix-scanner/src/lib.rs:advisories_from_osv Talks to. delonix-oci ( ImageStore , Image ) by direct call — a declared layering exception (see 05 ) — and delonix-model , into whose Error its own errors convert ( src/error.rs , impl From<Error> for delonix_model::Error ). Notable external dependencies. tar , flate2 , serde , serde_json . Tests. Inline unit tests. Start reading at. src/lib.rs from extract_sbom . Gotchas. None recorded in code beyond the crate doc."},{"p":"6. The crates","h":"delonix-telemetry Source","u":"06-crates.html#delonix-telemetry","t":"Purpose. Observability for the engine's binaries: structured tracing logging, optional OpenTelemetry span export over OTLP, and the shared Prometheus registry the servers expose. It left delonix-runtime-core so that a crate needing a Container type does not compile an OTLP client (crate doc). Key modules Module Responsibility telemetry init — fmt subscriber, plus OTLP when configured metrics Prometheus counters/gauges and encode Main public API Item What it is Where telemetry::init call once at the start of each binary crates/adapters/delonix-telemetry/src/telemetry.rs:init metrics::encode text exposition for /metrics crates/adapters/delonix-telemetry/src/metrics.rs:encode Talks to. No engine crate. OTLP export to a collector when configured. Notable external dependencies. tracing-subscriber , opentelemetry , opentelemetry_sdk , opentelemetry-otlp , tracing-opentelemetry , prometheus-client . Tests. Inline unit tests. Start reading at. src/telemetry.rs , then src/metrics.rs . Gotchas. The OTLP exporter batches. The short-lived delonix CLI does not flush on exit, so spans from a quick CLI invocation can be lost; long-running servers deliver reliably (module doc of telemetry.rs )."},{"p":"6. The crates","h":"Providers","u":"06-crates.html#providers","t":"Providers are backends that speak to an external system's management API. They live outside the adapters so that talking to a remote management API stays out of the engine adapters (Cargo.toml comments of both crates). This is not \"no HTTP in adapters\": delonix-oci has its own OCI registry client, and delonix-telemetry exports OTLP over HTTP."},{"p":"6. The crates","h":"delonix-proxmox Source","u":"06-crates.html#delonix-proxmox","t":"Purpose. A VmBackend backed by the REST API of one Proxmox VE node, named explicitly. No inventory and no node selection. It never touches a local disk ( manages_own_storage is true ) and is never auto-detected ( auto_selectable is false , because answering \"available?\" would cost a network round trip). Key modules. A single lib.rs . Main public API Item What it is Where Target , Auth node endpoint, node name, credentials crates/providers/delonix-proxmox/src/lib.rs Client API client ( connect , create_vm , start , stop , destroy , snapshot , wait_task , …) crates/providers/delonix-proxmox/src/lib.rs:Client ProxmoxBackend the VmBackend implementation crates/providers/delonix-proxmox/src/lib.rs:ProxmoxBackend register register the backend in delonix-vm 's registry crates/providers/delonix-proxmox/src/lib.rs:register Talks to. delonix-vm (the trait and register_backend ; a declared layering exception) and delonix-runtime-core . The node over HTTPS with blocking reqwest . The CLI registers it from environment configuration at startup ( bins/delonix-runtime-bin/src/cmd/vmbackends.rs:register_configured ). Notable external dependencies. reqwest (blocking, rustls), serde , serde_json . Tests. Inline unit tests; crates/providers/delonix-proxmox/tests/live.rs runs against a real node and skips with a printed line unless DELONIX_PROXMOX_TEST_URL is set. Start reading at. Crate doc in src/lib.rs , then Client::wait_task , then impl VmBackend for ProxmoxBackend . Gotchas. Most operations"},{"p":"6. The crates","h":"delonix-truenas Source","u":"06-crates.html#delonix-truenas","t":"Purpose. Provisioning on a TrueNAS SCALE appliance: dataset, quota, NFS share and permissions, so a kind: Volume does not require them made by hand. It only creates what lives on the NAS; mounting stays in delonix-volume through the same path as a hand-made share. Key modules. A single lib.rs . Main public API Item What it is Where Client::connect connect and pin a supported major version crates/providers/delonix-truenas/src/lib.rs:Client Client::ensure_dataset , set_permissions , ensure_nfs_share idempotent provisioning crates/providers/delonix-truenas/src/lib.rs Client::remove_nfs_share , remove_dataset teardown crates/providers/delonix-truenas/src/lib.rs validate_quota , validate_target_url , validate_dataset_name input checks before any request crates/providers/delonix-truenas/src/lib.rs Talks to. delonix-runtime-core only; the appliance over HTTPS. Used by bins/delonix-runtime-bin/src/cmd/provision.rs . Notable external dependencies. reqwest (blocking, rustls), serde , serde_json . Tests. Inline unit tests; crates/providers/delonix-truenas/tests/live.rs against a real appliance, skipped when unconfigured. Start reading at. Crate doc in src/lib.rs (four measured findings), then Client::connect , then ensure_dataset . Gotchas. Some calls return a job id that must be polled ( wait_job ). Numeric properties can be null ; \"no quota\" is not the number 0 (crate doc)."},{"p":"6. The crates","h":"Interfaces","u":"06-crates.html#interfaces","t":"Interfaces expose the engine over a protocol. Each server runs as its own binary; delonix serve <x> and delonix mcp exec it ( bins/delonix-runtime-bin/src/cmd/serve.rs:exec_server )."},{"p":"6. The crates","h":"delonix-cri Source","u":"06-crates.html#delonix-cri","t":"Purpose. A Kubernetes CRI server ( runtime.v1 RuntimeService and ImageService over gRPC on a unix socket), so a kubelet or crictl can use the engine as the node runtime. It also serves the streaming endpoints for exec/attach/port-forward (WebSocket and SPDY). It keeps its own sandbox and container records under <root>/cri/ and does not start containers in-process. Key modules Module Responsibility lib.rs generated cri stubs, DelonixImage (ImageService), serve_blocking runtime_svc RuntimeService: version , status , runtime config, dispatch to lifecycle runtime_svc/lifecycle pod sandboxes and containers streaming , spdy exec/attach/port-forward streaming servers cap_ceiling node-level upper bound on capabilities child_handle a spawned child handle safe against pid reuse bin/delonix-cri.rs the delonix-cri executable Main public API Item What it is Where serve_blocking run the gRPC server on a socket crates/interfaces/delonix-cri/src/lib.rs:serve_blocking CapCeiling , CeilingMode capability ceiling config crates/interfaces/delonix-cri/src/cap_ceiling.rs lifecycle::run_pod_sandbox , create_container , start_container the lifecycle entry points crates/interfaces/delonix-cri/src/runtime_svc/lifecycle.rs Talks to. Clients: gRPC over a unix socket ( tonic ); stubs generated by build.rs from crates/interfaces/delonix-cri/proto/api.proto . Images: delonix-oci in-process ( pull_from_registry_with_creds , ImageStore ). State: reads delonix_runtime_core::Store directly and calls delonix_li"},{"p":"6. The crates","h":"delonix-mgmt Source","u":"06-crates.html#delonix-mgmt","t":"Purpose. The local management API: HTTP+JSON over a unix socket, accepted only for the calling uid ( SO_PEERCRED ). Reads (volumes, containers, images, networks, VMs) are library calls; container mutations run the delonix CLI so they take the engine's real path. It also collects the dashboard summary and publishes Prometheus gauges. Its crate doc says new local clients belong on the node contract of ADR-0040/0041 rather than these routes. Key modules Module Responsibility lib.rs serve_blocking , the axum router, handlers, run_cli dashstats DashSummary , collect (counts, memory, network, disk), timeouts, metrics publishing Main public API Item What it is Where serve_blocking run the server crates/interfaces/delonix-mgmt/src/lib.rs:serve_blocking dashstats::collect summary shared by delonix dashboard and /metrics crates/interfaces/delonix-mgmt/src/dashstats.rs:collect Talks to. Direct calls into delonix-runtime-core ( Store ), delonix-volume , delonix-oci , delonix-scanner , delonix-vm , delonix-sdn ( infra , NetworkStore ), delonix-linux , delonix-telemetry . Mutations: the delonix CLI as a subprocess ( run_cli ). Notable external dependencies. axum , tokio , hyper , hyper-util , tower . Tests. Inline unit tests using tower against the router. Start reading at. The router in src/lib.rs ( .route( calls), then run_cli , then src/dashstats.rs . Gotchas. Arguments passed to the CLI are validated to reject a leading - ( valid_arg ), otherwise an id could be parsed as a flag."},{"p":"6. The crates","h":"delonix-mcp Source","u":"06-crates.html#delonix-mcp","t":"Purpose. A Model Context Protocol server: a local AI control surface. Stdio transport only; a child process of one client session, never a daemon. The single principal is the local uid. Tool inputs are typed and schema-validated; outputs are JSON text. It keeps a local audit log and an in-process task registry. Key modules Module Responsibility lib.rs DelonixMcp tools ( runtime.info , resource.list , container.restart , …), serve_stdio , doctor_checks risk risk level per tool audit append-only mcp/audit.log tasks session-scoped task registry Main public API Item What it is Where serve_stdio run the server crates/interfaces/delonix-mcp/src/lib.rs:serve_stdio DelonixMcp the tool handler crates/interfaces/delonix-mcp/src/lib.rs:DelonixMcp capabilities_table , doctor_checks delonix mcp capabilities / doctor crates/interfaces/delonix-mcp/src/lib.rs Talks to. Direct calls for reads: delonix-runtime-core , delonix-vm , delonix-volume , delonix-sdn , delonix-linux ( resource_advice ), and delonix-mgmt ( dashstats , a declared layering exception). Mutations run the delonix CLI ( run_cli_blocking , via dispatch::cli_bin ). Notable external dependencies. rmcp (server, stdio transport), schemars , tokio , sha2 (argument hashes in the audit log). Tests. Inline unit tests ( tempfile dev-dependency). Start reading at. Crate doc in src/lib.rs , the #[tool( handlers, then src/risk.rs . Gotchas. Same as the CRI: mutations go through cli_bin , never current_exe() (that is the server itself)."},{"p":"6. The crates","h":"Binaries","u":"06-crates.html#binaries","t":""},{"p":"6. The crates","h":"delonix-runtime-bin (binary delonix) Source","u":"06-crates.html#delonix-runtime-bin-binary-delonix","t":"Purpose. The CLI and the composition root. It parses commands ( clap ), translates every entry point (flags, manifests, compose files, the Docker Engine API slice, kind clusters) into engine calls, wires adapters to context ports, loads manifests and applies each Kind, and prints (with the po translation catalog). Hidden internal verbs ( netns pin , netns control , __apirun , __rmtree , __ovlhold , …) are intercepted in main before argument parsing so re-executed processes land in the right code. It also hosts the Docker API slice and the L7 ingress proxy in-process. Key modules (a selection; one module per command group in src/cmd/ ) Module Responsibility main.rs internal-verb interception, run , backend and network registration cmd/container.rs container group; cmd_run composes the run use case cmd/manifest.rs manifest loading ( load ), lowering of Stack / Workload cmd/stack.rs stack plan/apply/destroy : plan, per-Kind apply, converge, prune, revisions cmd/vm.rs , cmd/vmimage.rs , cmd/vmfile.rs VMs, VM images, VMfile cmd/network.rs , cmd/firewall.rs , cmd/netns.rs networks, ingress/egress, holder commands cmd/image.rs , cmd/build.rs images and builds cmd/serve.rs , cmd/mcp.rs exec into the server binaries; serve docker-api in-process cmd/dockerapi.rs Docker Engine API slice; run_from_spec_file for __apirun cmd/policy.rs node runtime policy via delonix-security-runtime cmd/vmbackends.rs registers configured remote VM backends cmd/output.rs , cmd/po.rs tables/describe output,"},{"p":"6. The crates","h":"delonix-mgmt-bin (binary delonix-mgmt) Source","u":"06-crates.html#delonix-mgmt-bin-binary-delonix-mgmt","t":"Purpose. The executable of the local management API. Checks the dispatch version, reads --addr / DELONIX_API_ADDR (default unix:///run/delonix-mgmt.sock ) and DELONIX_ROOT (default /var/lib/delonix ), then calls delonix_mgmt::serve_blocking . Talks to. delonix-mgmt , delonix-runtime-core ( dispatch ), delonix-telemetry ( init ). Run by delonix serve api . Tests. None of its own. Start reading at. bins/delonix-mgmt-bin/src/main.rs ."},{"p":"6. The crates","h":"delonix-mcp-bin (binary delonix-mcp) Source","u":"06-crates.html#delonix-mcp-bin-binary-delonix-mcp","t":"Purpose. The executable of the MCP server. Verbs serve [--transport stdio] , doctor , capabilities . Talks to. delonix-mcp ( serve_stdio , doctor_checks , capabilities_table ), delonix-runtime-core ( dispatch ), delonix-telemetry . Run by delonix mcp <verb> . Notable external dependencies. tokio . Tests. None of its own. Start reading at. bins/delonix-mcp-bin/src/main.rs ."},{"p":"6. The crates","h":"How a request crosses the crates","u":"06-crates.html#how-a-request-crosses-the-crates","t":"Three flows, each arrow traced to a call in the tree. Function names are the ones you can grep for."},{"p":"6. The crates","h":"1. delonix container run -d -p 8080:80 nginx","u":"06-crates.html#1-delonix-container-run-d-p-808080-nginx","t":"Default network ( --net host ), so the port is published by the container's own slirp4netns , not by the holder. With --net <custom> the flow differs: the first pass attaches through the holder and re-executes into the network namespace ( attach_custom_network , reexec_into_netns ), and ports are published on the holder by HostNetwork::publish ."},{"p":"6. The crates","h":"2. delonix stack apply -f manifest.yaml","u":"06-crates.html#2-delonix-stack-apply-f-manifestyaml","t":""},{"p":"6. The crates","h":"3. kubelet → delonix-cri → engine","u":"06-crates.html#3-kubelet-delonix-cri-engine","t":""},{"p":"7. System Design Interview — the Delonix Engine","h":"7. System Design Interview — the Delonix Engine","u":"07-system-design-interview.html#7-system-design-interview-the-delonix-engine","t":"Interviewer: Design a container and microVM engine for a single Linux node. It must run without root by default, without a resident daemon, and it must not know who is calling it. This page answers that prompt the way a strong candidate would, and then checks every answer against what the Delonix Engine actually does. Each deep dive ends with Where it lives in the code , listing files and symbols that were read for this page. For the structural map (layers, crate graph, processes, state paths) read 5. Architecture first; this page is about why the design is shaped that way. Numbers quoted below are measurements recorded in the repository with their date or release , not timeless facts. Re-measure before relying on one."},{"p":"7. System Design Interview — the Delonix Engine","h":"1. Requirements","u":"07-system-design-interview.html#1-requirements","t":"Candidate: Before drawing boxes I want to pin down what \"done\" means."},{"p":"7. System Design Interview — the Delonix Engine","h":"Functional","u":"07-system-design-interview.html#functional","t":"Run containers from OCI images: pull, unpack, create, start, stop, exec, logs, remove. Run microVMs from disk images, on more than one hypervisor. Networks between workloads: private bridges, published ports, firewall, DNS names, isolation by namespace. Storage : named volumes, bind mounts, network shares. Declarative operation: a manifest of Kinds, plan , apply , drift detection, prune. Serve the kubelet through the CRI, so the engine can be a Kubernetes node runtime. Expose the same operations to local programs (an API, an AI tool protocol), not only a shell."},{"p":"7. System Design Interview — the Delonix Engine","h":"Non-functional","u":"07-system-design-interview.html#non-functional","t":"Rootless-first. The normal path runs as an unprivileged user; privilege is opt-in and said. Daemonless. No process runs \"just in case\". Persistence belongs to systemd or to a per-workload process with an owner. No consumer knowledge. No tenant, account, plan or billing; the engine validates its own contract instead of trusting a caller to refuse what it cannot do. Observable through open standards (OpenTelemetry, Prometheus) and an event log. Honest failure. A refused or half-done operation is reported as such, with a reason and a stable exit class — never 0 over a failure. Survives restarts of its own control processes without disturbing running workloads."},{"p":"7. System Design Interview — the Delonix Engine","h":"2. Background constraints: what an unprivileged Linux user may do","u":"07-system-design-interview.html#2-background-constraints-what-an-unprivileged-linux-user-may-do","t":"Candidate: Rootless changes the design more than any other requirement, so let me list the kernel rules I have to live with. The kernel lets an unprivileged user… …but not Consequence for the design create a user namespace and be uid 0 inside it ( CLONE_NEWUSER ) map arbitrary host uids a single-uid map unless newuidmap / newgidmap and /etc/subuid grant a range; an image that chown s to uid 101 needs the range create net, mount, PID, IPC, UTS namespaces owned by that user namespace , with CAP_NET_ADMIN / CAP_SYS_ADMIN inside them touch the host's initial network namespace networking is built inside a namespace the engine owns; reaching the host needs a user-space bridge ( slirp4netns ) mount overlayfs, tmpfs, binds inside its own mount namespace mount in the host's view the container's own init performs the overlay mount after clone write limits into a delegated cgroup v2 subtree write cgroups it was not delegated limits only apply where systemd delegated them ( systemd-run --user --scope -p Delegate=yes ) setns into a namespace owned by its own user namespace setns into a namespace owned by another process's user namespace joining a network created elsewhere means entering that owner's user namespace first run clone safely in a single-threaded process assume clone is safe in a multi-threaded one ( clone does not run pthread_atfork handlers) servers built on tokio must hand process creation to a fresh process Two host policies turn up constantly and look like engine bugs: Ubu"},{"p":"7. System Design Interview — the Delonix Engine","h":"3. API","u":"07-system-design-interview.html#3-api","t":"Candidate: One set of operations, several doors — and one of them is the contract the others converge on. Door Encoding Who uses it CLI delonix argv, stable exit classes operators, scripts Node contract delonix.node.v1 gRPC and HTTP/JSON on the same local unix socket any local client (design; not served yet) CRI runtime.v1 gRPC on a unix socket the kubelet MCP JSON-RPC over stdio a local AI client, one session per process Design points of the node contract, all written in ADR-0040 D4 and ADR-0042 : The .proto files are the source of truth ; the REST mapping comes from google.api.http annotations and the OpenAPI document is generated from them. A CI gate checks format, lint, breaking changes against the last release, that every RPC except the bidirectional streams ( Exec , Console ) has an HTTP mapping, and that the committed OpenAPI is the generated one. Resource-oriented services ( ContainerService , PodService , VirtualMachineService , NetworkService , VolumeService , ImageService , StackService , NodeService , OperationService ), one request message per RPC, identity as explicit namespace / name . Long work returns an Operation that is persisted before it is acknowledged, so a restarted server can say Interrupted instead of RUNNING forever. Local only. SO_PEERCRED , same uid, no TCP, no TLS, no identity in the engine — ADR-0010 rejected a remote API. Anything off-node puts its own proxy in front. Interviewer: Why not just a REST server? Candidate: Because gRPC clients and "},{"p":"7. System Design Interview — the Delonix Engine","h":"4. High-level design","u":"07-system-design-interview.html#4-high-level-design","t":"Candidate: I will layer it so the rules of the domain never import the kernel, and I will make every long-lived process own exactly one thing. Layers (ADR-0040 D1): foundation → contexts → adapters/providers → interfaces → binaries, enforced by scripts/arch_fitness.py . State is JSON records and content-addressed files under one root, with atomic writes and flock around read-modify-write — no database, because there is no daemon to own one. Processes exist per workload (a supervisor that is the container's parent, the init, a log shim) and per node when networking is used (a pin that only holds namespaces, a restartable control process, one slirp4netns uplink). Nothing else stays up. Where it lives in the code: scripts/arch_fitness.py ( LAYERS , ALLOWED ); crates/foundation/delonix-runtime-core/src/store.rs ( Store::update , JsonStore::update , write_atomic ); crates/contexts/delonix-compute/src/{ports,launch}.rs ; crates/adapters/delonix-linux/src/supervise.rs ( run_supervised )."},{"p":"7. System Design Interview — the Delonix Engine","h":"5. Deep dives","u":"07-system-design-interview.html#5-deep-dives","t":""},{"p":"7. System Design Interview — the Delonix Engine","h":"5.1 A rootless container run","u":"07-system-design-interview.html#51-a-rootless-container-run","t":"Interviewer: Walk me through run -d -p 8080:80 nginx as an unprivileged user. Candidate: Resolve everything that can fail before creating a process; then create the process stopped, configure it from outside, and only release it when it is ready. Resolve. One run specification ( RunOpts ) comes from every entry — CLI flags, a Pod manifest, the Docker API, the CRI. resolve_run pulls the image if absent, prepares the root filesystem, resolves --user against it, resolves volumes and devices, and validates security options. Every refusal happens here, and a guard removes the prepared directory on any early return. Record. build_record turns the specification into a Container record (pure). Choose the parent. For a detached start the CLI forks a supervisor that becomes the container's parent ( launch::start → should_supervise ). Only the real parent can waitpid , so this is what makes the real exit code and --restart possible without a daemon. Clone. spawn calls clone with new mount, UTS, PID and IPC namespaces, plus user and network namespaces when the container gets its own. The child blocks on a pipe. Configure from outside, in a fixed order. The parent writes the uid/gid maps ( write_userns_maps , through newuidmap when a subuid range exists), sets up the cgroup, runs the on_started hook (here: slirp_attach with the -p ports, so the network exists before the entrypoint runs), and only then writes the \"go\" byte. Inside the child. container_init mounts the overlay ( mount_overla"},{"p":"7. System Design Interview — the Delonix Engine","h":"5.2 Networking: pin, control, slirp, nftables","u":"07-system-design-interview.html#52-networking-pin-control-slirp-nftables","t":"Interviewer: Containers need to talk to each other, be isolated by namespace, and publish ports — without CAP_NET_ADMIN on the host. Candidate: Build a private network world inside one namespace the user owns, bridge it to the host in user space, and do all filtering there. Split holding from serving. A single \"holder\" process that owned the namespaces and served requests would take every workload's network down whenever it restarted. So: the pin ( delonix netns pin ) creates the user, network and mount namespaces itself and then only sleeps ( pin_main ). Its pid is what every nsenter -t <pin> targets, and it never changes; the control process ( delonix netns control , started through nsenter into the pin's namespaces) serves a 0600 unix socket restricted by SO_PEERCRED , and runs DNS, DHCP and Router Advertisements. It is restartable: ensure_up restarts only it when the pin is alive; one slirp4netns attaches tap0 to the pin's netns and exposes an API socket for add_hostfwd . The pin creates its namespaces in-process (the caller writes the id maps through two pipes) instead of via unshare(1) , because an AppArmor profile is attached by the path of the executable that creates the user namespace, and /usr/bin/unshare is not the engine's. Joining a custom network. A container on --net web cannot setns into a netns owned by the pin's user namespace. The CLI therefore asks the control process to create the netns and veth ( attach … ), then re-executes itself inside the pin's user "},{"p":"7. System Design Interview — the Delonix Engine","h":"5.3 Images: CAS, shared layers, and the many-layer mount","u":"07-system-design-interview.html#53-images-cas-shared-layers-and-the-many-layer-mount","t":"Interviewer: A node runs twenty containers of the same image. What is on disk? Candidate: Blobs once, unpacked layers once, and one small writable directory per container. CAS. Blobs are named by their sha256 under blobs/sha256/<hex> ; writing an existing digest is a no-op. A pull verifies each blob against the manifest and the manifest against the digest the user pinned ( verify_manifest_digest ) — otherwise a pin would be decorative. Resumable downloads. A blob download retries with Range: from the bytes already held ( BLOB_ATTEMPTS ), and distinguishes a 206 at the requested offset (resume), a 206 elsewhere and a 200 (restart). The digest check at the end makes stitching safe. Shared layers. prepare_overlay creates upper/ , work/ , merged/ for the container and writes the ordered list of shared layer directories to overlay-lowers . The container's own init mounts it, inside its mount namespace, where an unprivileged user is allowed to. The contract is a file on disk rather than a field in memory because the rootless path re-executes the binary and a struct does not cross that boundary. Back of the envelope, as recorded for v0.59.0: the previous flat copy cost each container a full image tree — on one development host containers/ held 47 GiB, most of it identical copies, and each run of a 2.1 GiB image spent about 13 s copying. Sharing layers took that directory to 7.2 GiB. - Many layers. The classic mount(2) passes lowerdir=a:b:c… as one string and the kernel copies at mos"},{"p":"7. System Design Interview — the Delonix Engine","h":"5.4 microVMs: one port, a registry, and the firmware trap","u":"07-system-design-interview.html#54-microvms-one-port-a-registry-and-the-firmware-trap","t":"Interviewer: Add VMs without building a hypervisor abstraction that leaks everywhere. Candidate: A trait per provider, a registry the composition root fills, and quirks that belong to the provider answered by the provider. Port. VmBackend has id , available , boot , is_running , ip , stop , and optional operations ( pause , snapshot , restore , …) whose default answer is \"not supported\". Provider facts are methods, not string checks at call sites: ip_is_predicted (Cloud Hypervisor's address is computed from the MAC, not observed), manages_own_storage (a remote node owns its disk), destroy distinct from stop (locally the disk is the engine's; remotely only destroy releases it). Registry. builtin_backends seeds Cloud Hypervisor and libvirt in preference order; register_backend adds more (the Proxmox backend, registered by the CLI's composition root only when configured). A registration carries a factory closure and an auto_selectable flag, so auto-detection never constructs — and therefore never authenticates — a remote backend. Registering does no I/O. Networking a VM. Cloud Hypervisor runs inside the pin's netns and gets a tap on a network bridge through the VmNetwork port, which the SDN implements ( HostVmNetwork ); delonix-vm does not depend on delonix-sdn . Because the DHCP server is the engine's own and deterministic, the lease is known before the guest boots, which is what lets namespace isolation apply to a VM's address from the first packet — and why \"has an IP\" is not"},{"p":"7. System Design Interview — the Delonix Engine","h":"5.5 The declarative reconciler, without a state file","u":"07-system-design-interview.html#55-the-declarative-reconciler-without-a-state-file","t":"Interviewer: apply must converge, detect drift, and prune — Terraform-like — but you said no daemon and no database. Candidate: Keep the last applied spec on the resource itself, derive ownership from a label, and make planning a pure function. Pure plan. reconcile::plan(desired, actual, stack) takes two snapshots and returns Vec<Change> ; it never opens a store. That makes the hard cases testable as data. Three-way diff. The last applied field map is stored on the resource ( delonix.io/last-applied ). A field present on the machine but absent from the manifest is reverted only if we set it ; otherwise it is left alone — the distinction a two-way diff cannot make. Ownership by label ( delonix.io/stack ). A resource with no owner is Adopt ed; one owned by another stack is a Conflict and never touched; --prune and destroy only see what carries the label. Actions are Create , Adopt , Update (hot, same PID), Replace (refused unless --replace <Kind>/<name> is given, checked before anything is created), NoOp , Delete , Conflict , NotConverged . plan --detailed-exitcode answers 0/2/1 for a CI drift gate. One table of Kind facts (domain, form, whether it converges, has teardown, is namespaced, how presence is observed) governs the planner, apply order and teardown order, instead of lists kept in sync by hand. Where it lives in the code: crates/contexts/delonix-stack/src/reconcile.rs ( plan , Action , Change , STACK_LABEL , LAST_APPLIED , hot_fields_for , encode_last_applied ); crates"},{"p":"7. System Design Interview — the Delonix Engine","h":"6. Trade-offs","u":"07-system-design-interview.html#6-trade-offs","t":"Decision What it buys What it costs No daemon ; a supervisor per detached container, systemd for boot persistence no single process whose death takes every workload; each process has an obvious owner nothing sees a process die unless its own supervisor does; a caller that cannot fork starts unsupervised and the real exit code is lost; orphans need explicit reapers JSON files + flock instead of a database inspectable state, crash-tolerant, no extra dependency, works across CLI/CRI/supervisor processes no transactions or queries; the truth about liveness is reconciled on read ( reconcile_status , safe_to_signal ) User-space uplink ( slirp4netns ) instead of veth pairs in the host works with zero host privilege extra hop and CPU in user space; a loopback client appears as the slirp gateway ( SLIRP_GW ) rather than itself Pin/control split a control restart moves no wire two processes to reason about, and in-place upgrades must still recognize older pins Re-exec instead of in-process clone in servers clone never runs in a multi-threaded process a process per operation, and error text crossing a process boundary — the cycle ADR-0040 removes with a launcher Overlay mounted by the container's init one copy of each layer on disk; unprivileged mount a stopped container's merged view needs a helper process to hold the mount ( reexec_mapped_hold ) Verdict map dispatch constant per-packet cost as containers grow rules are generated text; the generator and the counter reader must share fo"},{"p":"7. System Design Interview — the Delonix Engine","h":"7. Failure modes and the limits of one node","u":"07-system-design-interview.html#7-failure-modes-and-the-limits-of-one-node","t":"Interviewer: Tell me how it breaks. The control process dies. ensure_up finds the pin alive and restarts only the control plane inside the surviving namespaces; running workloads keep their PIDs and network. The pin dies. The namespaces go with it and cannot be re-entered, so the infra is rebuilt. delonix net netns up finds containers and pod members that were running with a network and restarts them ( reconcile_after_respawn ; DELONIX_NO_AUTO_RECOVER=1 only reports). This is recovery by restart, and it reads the container store only — VMs are not recovered this way. An upgrade over an older holder. A pre-split holder serving a legacy socket path is detected and reported with both paths; it is deliberately not killed automatically, because that would drop every workload's network. apply dies midway. Apply is fail-fast without rollback. Before creating anything it validates the graph and refuses unauthorized replacements; if a layer fails, what this run created is stamped with ownership ( salvage_ownership ) so a later destroy or --prune can still reach it, and the failed run is recorded as a revision. Leaks without a daemon. Every lease and reference is released by a normal detach, so anything that dies another way leaks. Measured on 2026-08-25: one network's IPAM file held 391 leases, of which 47 belonged to an existing container. The reapers respect a grace window ( REF_MARKER_GRACE ) because a container being created holds a lease and a reference before it has a record; th"},{"p":"7. System Design Interview — the Delonix Engine","h":"8. Follow-up questions","u":"07-system-design-interview.html#8-follow-up-questions","t":"Why not add a small daemon for events and restarts? Because every resident process is a failure domain and an attack surface. The event log is an append-only file ( delonix_runtime_core::events ), restarts belong to the per-container supervisor, and boot persistence is a systemd unit per workload ( delonix system boot ). A daemon needs its own ADR with evidence of what the alternatives could not do — see ADR-0034 for a case where the question came up, and ADR-0021 ( Proposed ) for continuous reconciliation that stays daemonless. How does the CRI start a container if the server must not clone ? StartContainer builds a typed RunOpts , writes it to a 0600 file and runs delonix __apirun <spec> , which calls the same cmd_run as the CLI. ADR-0040 D5 replaces the CLI hop with a launcher executable. Resource policy on the CRI path follows the kubelet ( ADR-0038 ). Why is the management API local-only? Remote access means identity, authorization, certificates and audit of callers the engine has no notion of. ADR-0010 rejected it; the MCP surface is local for the same reason ( ADR-0025 ). How would you add a new VM provider? A new crate implementing VmBackend , registered at the composition root — no edit to call sites ( ADR-0008 ). ADR-0040 D3 moves provider knobs into namespaced extensions and quirks into capabilities; OpenStack is gated on a spike ( ADR-0039 ). How do Services load-balance without a VIP? A Service selects containers by label and the internal DNS returns several A re"},{"p":"8. Delonixfile and VMfile","h":"8. Delonixfile and VMfile","u":"08-delonixfile-and-vmfile.html#8-delonixfile-and-vmfile","t":"Delonix has two build files, and they look alike on purpose: anyone who has written a Dockerfile can read both. What they build is different. A Delonixfile builds an OCI container image (layers of a filesystem). It is a Dockerfile grammar with a few Delonix instructions on top, built by delonix build . A VMfile builds a bootable qcow2 disk for a VM. It borrows the Dockerfile shape , but the mechanism is qemu-img + virt-customize on a whole disk, built by delonix image vm build . This page describes what the parsers in this repository actually accept — not what Docker accepts. Every rule below points at the code that enforces it. Examples marked parse-checked were run against a binary built from this tree, with DELONIX_ROOT , DELONIX_NET_RUNTIME_DIR and TMPDIR pointed at a scratch directory, up to the point where the build would start pulling images or downloading base disks. Full builds were not executed in this review (they need registry/network access and, for VMs, libguestfs and several GB of RAM and disk)."},{"p":"8. Delonixfile and VMfile","h":"Part 1 — Delonixfile","u":"08-delonixfile-and-vmfile.html#part-1-delonixfile","t":""},{"p":"8. Delonixfile and VMfile","h":"Where the code lives","u":"08-delonixfile-and-vmfile.html#where-the-code-lives","t":"Concern File Symbol Grammar (parser) crates/adapters/delonix-oci/src/build.rs parse_dockerfile_with_args , parse_run_flags , parse_secret_mount , resolve_target_stage Build orchestration bins/delonix-runtime-bin/src/cmd/build.rs run , build_from_spec , build_one_stage , default_build_file Image commit crates/adapters/delonix-oci/src/build.rs ImageStore::commit_flat_rootfs (rootless), commit_upper + build_image (root) Project templates bins/delonix-runtime-bin/templates/<name>/Delonixfile rendered by delonix init / stack init"},{"p":"8. Delonixfile and VMfile","h":"File lookup","u":"08-delonixfile-and-vmfile.html#file-lookup","t":"delonix build [CONTEXT] without -f calls default_build_file ( cmd/build.rs ): it uses <context>/Delonixfile if it exists, otherwise <context>/Dockerfile . The grammar is the same for both names — the Delonix instructions below are accepted in a file called Dockerfile too. Delonixfile is just the name that is discovered first. -t/--tag is required. delonix build -t myapp:dev . # Delonixfile, else Dockerfile delonix build -t myapp:dev -f build/Other . # explicit file"},{"p":"8. Delonixfile and VMfile","h":"Instructions the parser accepts","u":"08-delonixfile-and-vmfile.html#instructions-the-parser-accepts","t":"parse_dockerfile_with_args is fail-closed : an instruction it does not know is an error with the line number, and any instruction except ARG before the first FROM is an error. Lines ending in \\ are joined; lines starting with # are comments. Instruction names are case-insensitive. Instruction What happens Notes ARG NAME[=default] Declares a build variable; ${NAME} / $NAME is substituted in every later line Allowed before FROM (to parameterize it). --build-arg NAME=VALUE overrides only a declared ARG . Simplification: args live in one scope for the whole file, not per stage. No ${NAME:-default} forms ( substitute_vars ). FROM <image> [AS <name>] Opens a stage A later stage may also say FROM <earlier-stage> (see multi-stage). RUN <shell> Runs in a working container via exec Only --mount=type=secret,... is accepted as a flag (below). COPY [--from=<stage>] <src> <dst> Writes into the stage's rootfs on disk Confined to the context/rootfs ( safe_join , confine_to ): .. , absolute escapes and symlinks that leave the base are refused. ADD Same as COPY No URL download, no automatic archive extraction. ENV K=V [K2=\"v 2\" …] or ENV K V Affects later RUN s; final stage's ENV goes into the image config Values are expanded against earlier ENV s ( expand_env_value ). WORKDIR <dir> Working dir of later RUN s; final value goes into the image config USER <name|uid[:gid]> Recorded in the image config Empty → inherits the base image's. CMD , ENTRYPOINT Recorded in the image config Exec (JSON) and"},{"p":"8. Delonixfile and VMfile","h":"Multi-stage, COPY --from and --target","u":"08-delonixfile-and-vmfile.html#multi-stage-copy-from-and-target","t":"Each stage (the final one included) gets its own working container and rootfs ( build_one_stage ). The working container ( sleep infinity over the stage's rootfs) is created by ensure_container through the same WorkloadRuntime that container run and start use ( container::with_host_workload ), so a build cannot drift from a container's spawn specification. Intermediate stages stay on disk until the whole build ends, so: COPY --from=<name-or-index> <src> <dst> reads straight out of that stage's rootfs ( resolve_copy_source , copy_into_rootfs ). FROM <earlier-stage> clones that stage's rootfs with cp -a --reflink=auto ( clone_rootfs ) — cp -a so symlinks such as /bin -> usr/bin stay symlinks. --target <name-or-index> ( resolve_target_stage ) stops at that stage and packages it; later stages are not built. An unknown name is refused and the error lists the stages that exist. With --target on an intermediate stage, CMD / ENTRYPOINT / USER / ENV / WORKDIR / HEALTHCHECK come from that stage, not from the file's final stage. Two root-mode (overlay) restrictions, both refused up front with a clear message: the final stage cannot be FROM <earlier-stage> (the OCI commit needs a real base image for lineage), and --target on an intermediate stage is refused. Rootless has neither restriction."},{"p":"8. Delonixfile and VMfile","h":"Build secrets","u":"08-delonixfile-and-vmfile.html#build-secrets","t":"delonix build -t app:dev --secret id=npmrc,src=$HOME/.npmrc . RUN --mount=type=secret,id=npmrc,target=/root/.npmrc npm ci --secret id=<name>,src=<path> is repeatable; a malformed entry or a missing src file is a hard error ( parse_build_secrets ). In the file: --mount=type=secret,id=<name>[,target=<path>][,required=true|false] . target defaults to /run/secrets/<id> ; required defaults to false (a missing optional secret is skipped, like Docker). type=ssh , type=cache and type=bind are refused ( parse_secret_mount ). The secret is bind-mounted live ( mount_run_secrets ) inside the working container's mount namespace only for that one RUN , then unmounted — the host-side view of the rootfs that the commit and the layer cache read never contains it. The secret material is also not hashed into the cache key."},{"p":"8. Delonixfile and VMfile","h":"--platform and binfmt","u":"08-delonixfile-and-vmfile.html#-platform-and-binfmt","t":"--platform linux/<arch> ( parse_platform : only linux/ is accepted) resolves the base image for that architecture and stamps the arch into the result. Running a foreign-arch RUN needs the host's own binfmt_misc + qemu-user-static registration, which Delonix does not manage. build_from_spec checks /proc/sys/fs/binfmt_misc/qemu-<arch> before building and refuses with the interpreter name if it is missing or disabled."},{"p":"8. Delonixfile and VMfile","h":"Rootless vs root, and the layer cache","u":"08-delonixfile-and-vmfile.html#rootless-vs-root-and-the-layer-cache","t":"Rootless (normal path) Root Rootfs of a stage flat directory ( prepare_rootfs_flat ) overlay Commit commit_flat_rootfs — one squashed layer on top of the base's layers commit_upper (tar of the upperdir) + build_image Layer cache yes never The cache ( <DELONIX_ROOT>/build-cache/<hash>/rootfs , --no-cache to bypass) is a rolling hash chain, one link per instruction. RUN / COPY snapshot the full rootfs after running; ENV / WORKDIR fold into the chain without a snapshot. A COPY link hashes the bytes being copied, so a changed file invalidates everything after it. On a hit, the cached snapshot is cloned into a fresh container's rootfs — never synced onto a live one (that corrupted the /proc / /sys / /dev mounts and was abandoned). Trade-offs stated in the module doc: full-rootfs snapshots, not per-layer diffs (mitigated by --reflink=auto ), and no cache GC — build-cache/ only grows. Because every RUN executes in a real container, the host prerequisites of 01 — Preparing your environment (user namespaces, subuid/subgid, AppArmor on Ubuntu) apply to builds too."},{"p":"8. Delonixfile and VMfile","h":"Worked example: a template","u":"08-delonixfile-and-vmfile.html#worked-example-a-template","t":"delonix init -t <template> [DIR] (or delonix stack init --template <template> ) renders bins/delonix-runtime-bin/templates/<template>/ , replacing __NAME__ , __PORT__ and __TEMPLATE_VERSION__ (the version= default in template.meta , or -v/--template-version ). In an empty directory it writes the full project; in a non-empty one it only adds the Delonix glue (Delonixfile, manifest, CI files). Generated in a scratch directory from httpd ( run ): $ delonix init -t httpd web detected an empty directory → stack init --template httpd created: web/.dockerignore created: web/Delonixfile ... # Delonixfile — production-ready Apache httpd. # Build with: delonix build -t web:dev . FROM httpd:2.4-alpine # Listen on 8080 and harden a little (no version banner). RUN sed -i 's/^Listen 80$/Listen 8080/' /usr/local/apache2/conf/httpd.conf && \\ printf '\\nServerTokens Prod\\nServerSignature Off\\nTraceEnable Off\\n' >> /usr/local/apache2/conf/httpd.conf COPY public /usr/local/apache2/htdocs EXPOSE 8080 HEALTHCHECK CMD wget -qO- http://127.0.0.1:8080/healthz || exit 1 CMD [\"httpd-foreground\"] The generated delonix-manifest.yaml passes delonix manifest validate ( run ). --up would build, stack apply and wait for health — not executed here. A multi-stage file using most of the grammar, parse-checked (the --target nosuch error proves the file parsed and lists its stages, before any pull): ARG GO_VERSION=1.23 FROM golang:${GO_VERSION}-alpine AS builder WORKDIR /src COPY go.mod ./ RUN --mount=type=secret"},{"p":"8. Delonixfile and VMfile","h":"The repository's own Delonixfile is not built by delonix","u":"08-delonixfile-and-vmfile.html#the-repositorys-own-delonixfile-is-not-built-by-delonix","t":"The Delonixfile at the repository root packages the delonix CLI into a container image. It is built with Docker or Podman ( docker build -f Delonixfile … , or make image ), not with delonix build : it uses # syntax=docker/dockerfile:1 and RUN --mount=type=cache , which delonix build refuses. Do not use it as an example of the Delonix grammar; use the templates."},{"p":"8. Delonixfile and VMfile","h":"Part 2 — VMfile","u":"08-delonixfile-and-vmfile.html#part-2-vmfile","t":""},{"p":"8. Delonixfile and VMfile","h":"Where the code lives","u":"08-delonixfile-and-vmfile.html#where-the-code-lives_1","t":"Concern File Symbol Grammar, scaffold, builder bins/delonix-runtime-bin/src/cmd/vmfile.rs parse , classify_base , resolve_base , stage_ops , build , finalize , scaffold CLI entry, golden recipe bins/delonix-runtime-bin/src/cmd/vmimage.rs VmImageCmd::Build , VmImageStore , VmImage , customize_args , tool_failure_hint Declarative build bins/delonix-runtime-bin/src/cmd/vm.rs VmBuildSpec ( spec.build of kind: VirtualMachine )"},{"p":"8. Delonixfile and VMfile","h":"Scaffold","u":"08-delonixfile-and-vmfile.html#scaffold","t":"delonix vm init --vmfile [DIR] [--name <n>] # or: delonix image vm init <name> [-d DIR] Both write VMfile and cloud-init/user-data.yaml ( run in a scratch directory). The scaffold is meant to be a working recipe, and the parseia_o_scaffold_que_escrevemos test keeps it parseable. Two things to correct by hand in the \"Next:\" hint it prints: the flag is vm create --disk , not --disk-image ; and see the CLOUDINIT note below about the file name."},{"p":"8. Delonixfile and VMfile","h":"Building","u":"08-delonixfile-and-vmfile.html#building","t":"delonix image vm build -t web:1.0 [-f VMfile] [--network] [--no-compress] [CONTEXT] There is no delonix vm build ; the build lives under image vm . Without -f , a VMfile in the context is picked up automatically (like a Delonixfile beating a Dockerfile ); with no VMfile , the same command runs the built-in golden recipe instead (see 09 ). The golden-recipe flags ( --k8s-version , --extra-package , --extra-run , --offline , --no-k8s , --cri-bin , --delonix-bin ) are refused with a VMfile, and --network is refused without one."},{"p":"8. Delonixfile and VMfile","h":"Instructions","u":"08-delonixfile-and-vmfile.html#instructions","t":"parse is fail-closed: an unknown instruction is an error naming the supported set. Only FROM may come first. \\ continuations are joined; a # starts a comment only at the start of a line (so RUN sed 's/#x/y/' is not mangled). Instruction Scope Effect ( stage_ops → virt-customize ) FROM <ref> [AS <name>] opens a stage See What FROM accepts . as is case-insensitive. RUN <shell> step --run-command inside the guest; offline unless --network . COPY <src> <dst> step Copy-in from the build context; src confined to the context ( build::safe_join ). Exactly two arguments. COPY --from=<stage> <src> <dst> step virt-copy-out from that stage's disk to a host staging dir, then copy-in. The stage must be named and declared earlier — checked at parse time. ENV KEY=value step Appends KEY=value to /etc/environment (a VM has no image config). One pair per line; the key and value are passed to a shell unquoted , so avoid spaces and shell metacharacters. USER <name> step useradd -m -s /bin/bash <name> if the account does not exist. PASSWORD <user>:<password> step Sets that account's password. It is baked into every copy of the image. ROOTPASSWORD <password> step Sets root's password. Same caveat. SSHKEY <user> <path-or-key> step Appends to /home/<user>/.ssh/authorized_keys ( ~/ is expanded). The value must be a readable file or a key starting with ssh- / ecdsa- . The account must exist (create it with USER first). CLOUDINIT <path> step Copies that file (context-confined) into /etc/cloud/cloud.cfg."},{"p":"8. Delonixfile and VMfile","h":"What FROM accepts","u":"08-delonixfile-and-vmfile.html#what-from-accepts","t":"classify_base is pure and decides the kind of reference: A named earlier stage — wins over everything ( resolve_base ). ubuntu:<rel> , debian:<rel> , rocky:<rel> , fedora:<rel> — first the project's own base image for that distro/release (local copy, then the official registry — vmimage::official_distro_base ); if there is none, the distro's own cloud image, downloaded and verified against the publisher's checksum file ( vmimage::download_base ). http:// / https:// URL — downloaded; verified against <url>.sha256 when the publisher offers one, otherwise trusted on TLS alone and the build says so. Anything else — a VM image already in the local store ( delonix image vm ls ). Any other name:tag is treated as a local tag, not as an unknown distro."},{"p":"8. Delonixfile and VMfile","h":"What a stage is, and where the result lands","u":"08-delonixfile-and-vmfile.html#what-a-stage-is-and-where-the-result-lands","t":"A stage is a whole disk , not a layer ( build ): The base is flattened into <work>/<stage>.qcow2 with qemu-img convert (no backing file, so the artefact never depends on the base still being present). SIZE is applied, then all steps run in one virt-customize call ( --no-network unless --network ). vmimage::customize_args always appends an SELinux relabel as the last command (and turns off libguestfs's own deferred relabel), so SELinux guests do not boot with unlabeled files. Named stages are kept for COPY --from ; unnamed ones are not addressable. Only the last stage becomes an image. Unless --no-compress : virt-sparsify --in-place (best effort), then qemu-img convert -c -o compression_type=zstd . zstd because the image becomes the read-only backing file of every VM created from it, so decompression speed is a runtime property. The qcow2 is moved to VmImageStore::qcow2_path(tag) under <DELONIX_ROOT>/vm-images/ , and a VmImage metadata record is saved next to it. Recorded metadata: digest and size ; default_vcpus / default_memory / default_backend from the file; cloud_init: true ; built_by: \"delonix <version>\" ; distro and kernel_version inherited only when the final FROM is a local image (a URL gives nothing to inherit, and the code refuses to guess). vm create applies the recorded defaults only where the caller did not decide: --vcpus / --memory flags win over VCPUS / MEMORY ; for the backend, --backend > the image's HYPERVISOR > DELONIX_VM_BACKEND > vm default-backend > aut"},{"p":"8. Delonixfile and VMfile","h":"Declarative build","u":"08-delonixfile-and-vmfile.html#declarative-build","t":"kind: VirtualMachine can build its own disk with spec.build ( VmBuildSpec ): context (relative to the manifest's directory), file (default <context>/VMfile ), tag (default <metadata.name>:latest ), compress , network . apply calls the same vmfile::build . disk and build are mutually exclusive."},{"p":"8. Delonixfile and VMfile","h":"Worked example (parse-checked)","u":"08-delonixfile-and-vmfile.html#worked-example-parse-checked","t":"FROM my-base:1.0 AS builder RUN make -C /src FROM my-base:1.0 SIZE 20G HOSTNAME web COPY --from=builder /src/app /usr/local/bin/app ENV APP_ENV=production USER app VCPUS 2 MEMORY 2G HYPERVISOR ch LABEL org.opencontainers.image.title=web With no image called my-base:1.0 in the scratch store, the file parses and the build stops at base resolution — before any disk work: $ delonix image vm build -t web:1.0 . [1/2] builder: FROM my-base:1.0 error invalid argument: FROM my-base:1.0: no such local VM image, and it is not a URL nor a known cloud image (ubuntu:/debian:/rocky:) — see `delonix vm ls` And the parser's refusals: VMfile:2: unknown instruction 'CMD' — supported: FROM RUN COPY ENV USER PASSWORD ROOTPASSWORD CLOUDINIT SSHKEY SIZE HOSTNAME VCPUS MEMORY HYPERVISOR LABEL VMfile:2: no earlier stage named 'nope' VMfile:2: invalid argument: unknown VM backend: 'vmware' (use 'cloud-hypervisor', 'libvirt') --offline belong to the built-in golden recipe and mean nothing with a VMfile — the VMfile describes all of that itself A full build ( virt-customize , downloads, compression) was not executed in this review ."},{"p":"8. Delonixfile and VMfile","h":"Comparison","u":"08-delonixfile-and-vmfile.html#comparison","t":"Dockerfile (Docker/BuildKit) Delonixfile ( delonix build ) VMfile ( delonix image vm build ) Output OCI image OCI image (pushable, pullable by Docker) bootable qcow2 + VmImage metadata Unit of a stage filesystem layers a working container + rootfs a whole flattened disk FROM image image or earlier stage distro:release, URL, local VM image, or earlier stage RUN executes in build container working container via exec (rootless userns) guest, via virt-customize (offline by default) COPY --from yes yes (name or index) yes (named, earlier stages only; virt-copy-out ) COPY flags --chown/--chmod yes no no ADD URL / archive extraction yes no ( ADD = COPY ) no ADD RUN --mount secret, ssh, cache, bind, tmpfs type=secret only none --target yes yes no --platform yes linux/<arch> , host binfmt required no (images stay amd64 — ADR-0018 ) Layer cache yes rootless only, no GC none CMD / ENTRYPOINT / USER / ENV image config image config no CMD ; USER creates an account; ENV → /etc/environment Resource hints — CPUS / MEMORY / SECURITY recorded, not applied VCPUS / MEMORY / HYPERVISOR applied by vm create as defaults Vulnerability gate — SCAN parsed, not enforced (use image scan --fail-on ) — Accounts / keys / passwords — — USER , SSHKEY , PASSWORD , ROOTPASSWORD Disk size — — SIZE (before any step) Unknown instruction error error error"},{"p":"8. Delonixfile and VMfile","h":"If you change a grammar","u":"08-delonixfile-and-vmfile.html#if-you-change-a-grammar","t":"Keep both parsers fail-closed : an unknown instruction is an error, never a skipped line. A new instruction lands with a unit test in the parser's mod tests , a row in this page, and — if the scaffold uses it — the scaffold test still passing. If an instruction is parsed but not yet wired to an effect (as SCAN , CPUS / MEMORY / SECURITY and VMfile LABEL are today), say so here; a field the user writes and the engine ignores must be documented as such or removed. The Delonixfile parser lives in a library crate ( delonix-oci ), so it must not print; the VMfile parser is in the CLI binary. See 06 — Crates ."},{"p":"9. Building microVMs","h":"9. Building microVMs","u":"09-microvm-setup.html#9-building-microvms","t":"This page takes a contributor from a bare Linux host to building, booting and testing VMs with Delonix, and shows where the code lives when something needs changing. It assumes you have already read 01 — Preparing your environment and can build the tree ( 02 ). What was verified for this page. Every command and flag below was checked against delonix <group> --help of a binary built from this tree, and the read-only / configuration commands ( vm ls , vm reach , vm default-backend , image vm ls , manifest validate , stack apply --dry-run , the backend-selection errors) were run with DELONIX_ROOT and DELONIX_NET_RUNTIME_DIR pointed at a scratch directory. Booting a VM, building an image and pulling from the registry were not executed in this review — behaviour for those comes from the code, the ADRs and the scripted tests referenced below. Always use the binary you built ( ./target/debug/delonix ), not one on your PATH , and always isolate both state roots while experimenting — see Isolation ."},{"p":"9. Building microVMs","h":"1. Host prerequisites","u":"09-microvm-setup.html#1-host-prerequisites","t":""},{"p":"9. Building microVMs","h":"KVM","u":"09-microvm-setup.html#kvm","t":"Both local backends need hardware virtualization: ls -l /dev/kvm # must exist; missing = VT-x/AMD-V off in firmware, or no nested virt id -nG | tr ' ' '\\n' | grep -x kvm # your user must be in the kvm group scripts/install.sh (default, i.e. without --no-vm ) adds you to the kvm and libvirt groups and warns when /dev/kvm is missing. Group changes need a new login session."},{"p":"9. Building microVMs","h":"Cloud Hypervisor and its firmware","u":"09-microvm-setup.html#cloud-hypervisor-and-its-firmware","t":"The backend is available when cloud-hypervisor is on PATH ( CloudHypervisorBackend::available in crates/adapters/delonix-vm/src/lib.rs ). Where the distribution does not package it, the installer downloads the static upstream binary to /usr/local/bin/cloud-hypervisor , pinned to a version and a SHA-256 in scripts/install.sh . To boot a cloud image without --kernel , CH needs UEFI firmware. default_ch_firmware uses $DELONIX_HYPERVISOR_FW if set, otherwise the first file that exists in DEFAULT_CH_FIRMWARES : /usr/local/share/delonix/CLOUDHV.fd ← EDK2 build from cloud-hypervisor/edk2 (preferred) /usr/share/delonix/CLOUDHV.fd /usr/local/share/delonix/hypervisor-fw ← rust-hypervisor-firmware (fallback) /usr/share/delonix/hypervisor-fw The order matters. Measured and recorded in the constant's doc comment: under rust-hypervisor-fw none of the images this project builds boot in CH; with EDK2 CLOUDHV.fd they do. hypervisor-fw stays as a fallback for hosts that only have it. The installer fetches both (each pinned by tag and SHA-256). The unit test o_edk2_vem_antes_do_hypervisor_fw_na_procura_de_firmware guards the order."},{"p":"9. Building microVMs","h":"libvirt / QEMU","u":"09-microvm-setup.html#libvirt-qemu","t":"The libvirt backend is available when both virsh and qemu-system-x86_64 are on PATH ( LibvirtBackend::available ). The installer installs QEMU and a libvirt daemon package and enables libvirtd (socket-activated where supported). Which libvirt connection is used matters more than it looks ( libvirt_uri_for ): Situation Connection Consequence --net-mode nat or bridge qemu:///system reachable IP; needs the libvirt group (or root) no --net-mode , system connection usable qemu:///system , nat chosen automatically IP by DHCP from the libvirt network ( virbr0 ) no --net-mode , system connection not usable, rootless qemu:///session , user-mode no visible or reachable IP ; vm create warns On qemu:///system QEMU runs as the libvirt service user, which cannot read a disk under a 0700 home. For a rootless caller the domain XML gets a static DAC seclabel pinning QEMU to your uid/gid with relabel='no' , so your own overlay boots without being chowned away."},{"p":"9. Building microVMs","h":"Tools the VM code shells out to","u":"09-microvm-setup.html#tools-the-vm-code-shells-out-to","t":"Tool Package (Debian / Fedora) Used by qemu-img qemu-utils / qemu-img per-VM overlays, vm convert , snapshots on CH, image builds cloud-localds cloud-image-utils / cloud-utils NoCloud seed ISO — generated on every vm create of a cloud-init image unless --seed is given ( crates/adapters/delonix-vm/src/cloudinit.rs ) virsh libvirt-clients / libvirt-client libvirt backend virt-customize , virt-sparsify , virt-copy-out libguestfs-tools / guestfs-tools image vm build only vmimage::tool_package maps a missing binary to its package, so a missing tool is reported by name rather than as a bare No such file or directory ."},{"p":"9. Building microVMs","h":"Only for building images: --with-image-build","u":"09-microvm-setup.html#only-for-building-images-with-image-build","t":"image vm build runs virt-customize , which builds a small appliance with supermin. Three host problems break it in ways that do not look like host problems; scripts/install.sh --with-image-build handles them, and tool_failure_hint ( cmd/vmimage.rs ) names them when a build fails: No DHCP client on the host. supermin copies host packages into the appliance; without isc-dhcp-client the appliance has no network and the build dies on Temporary failure resolving … . The installer installs it. /boot/vmlinuz-* is 0600 (Debian/Ubuntu). supermin copies the host kernel and fails with Permission denied . The installer runs chmod 0644 /boot/vmlinuz-* — this lowers a host security boundary (any local user can read the kernel image), so it is opt-in and prints how to revert ( sudo chmod 0600 /boot/vmlinuz-* ). The failure hint also shows how to make it survive kernel updates. passt , only with image vm build --network . libguestfs gives the appliance a network through passt. Its AppArmor profile (Debian/Ubuntu) forbids the runtime directory libguestfs uses, and the passt packaged in Ubuntu 24.04 starts but never hands out a lease — dhclient waits ~300 s and the build continues without network, failing later on a package mirror. Remedies: mkdir -p /tmp/delonix-run && chmod 700 /tmp/delonix-run XDG_RUNTIME_DIR=/tmp/delonix-run ./target/debug/delonix image vm build --network … and, if it still fails, a current passt first on PATH (the installer builds one into /usr/local/bin ). Do not \"disabl"},{"p":"9. Building microVMs","h":"Isolation first","u":"09-microvm-setup.html#isolation-first","t":"This machine may run other workloads. Before any vm command beyond --help : export DELONIX_ROOT=$HOME/dlx-dev/root export DELONIX_NET_RUNTIME_DIR=/tmp/dlx-dev-run # keep it SHORT (AF_UNIX sun_path is 108 bytes) export TMPDIR=$HOME/dlx-dev/tmp # VMfile builds put whole disks here mkdir -p \"$DELONIX_ROOT\" \"$DELONIX_NET_RUNTIME_DIR\" \"$TMPDIR\" Both roots, always: isolating only DELONIX_ROOT leaves the network sockets shared with the real state and has restarted real workloads before (see 02 ). A Cloud Hypervisor VM also refuses up front when <root>/vms/<name>.sock would not fit in sun_path ( ch_socket_paths_fit ), so avoid very deep DELONIX_ROOT paths."},{"p":"9. Building microVMs","h":"2. Backends and how one is chosen","u":"09-microvm-setup.html#2-backends-and-how-one-is-chosen","t":""},{"p":"9. Building microVMs","h":"The port and the registry","u":"09-microvm-setup.html#the-port-and-the-registry","t":"VmBackend ( crates/adapters/delonix-vm/src/lib.rs ) is the port every hypervisor implements: id , available , boot , is_running , ip , stop , plus defaulted methods ( destroy , pause , unpause , resume , snapshot / restore / snapshots / delete_snapshot , preserve_snapshots , ip_is_predicted , manages_own_storage , auto_selectable , disk_health ). A default that cannot be honoured fails closed with a message, never a silent no-op. Backends live in a registry ( BACKENDS ), not a match : Backend Crate Aliases Auto-selectable cloud-hypervisor delonix-vm (built in) ch , cloudhypervisor yes libvirt delonix-vm (built in) kvm , qemu yes proxmox delonix-proxmox (registered by the CLI) — no — selected by name only register_backend refuses an id/alias that belongs to another backend, and refuses auto_selectable: true for anything that is not built in: auto-detection asks every candidate available() , and a remote backend can only answer that over the network. Registering does no I/O; the factory runs the first time the backend is selected. backend_for on an existing VM resolves the recorded backend and an unknown name is an error (it used to fall back to CH)."},{"p":"9. Building microVMs","h":"Selection precedence for a new VM","u":"09-microvm-setup.html#selection-precedence-for-a-new-vm","t":"From delonix_vm::create_with and resolve_vm_defaults ( cmd/vm.rs ), first match wins: --backend (or backend: in the manifest). The image's HYPERVISOR (recorded by a VMfile build), when --disk names a local image. DELONIX_VM_BACKEND (session-wide). delonix vm default-backend --set <backend> (machine-wide, stored in <DELONIX_ROOT>/vm-default-backend ). Capability heuristic: volumes present ⇒ libvirt (only libvirt does virtio-9p); a cloud image with no --kernel ⇒ libvirt if libvirt is available ; otherwise auto-detection — the first auto-selectable registered backend that is installed (CH, then libvirt). So on a host with both hypervisors, a plain vm create of a cloud image lands on libvirt ; pass --backend cloud-hypervisor (or set a default) to get a microVM on the SDN. $ delonix vm default-backend none (auto-detection: cloud-hypervisor if installed, else libvirt) $ delonix vm default-backend --set ch default backend set to cloud-hypervisor $ delonix vm default-backend --set bogus error invalid argument: unknown VM backend: 'bogus' (use 'cloud-hypervisor', 'libvirt') $ delonix vm default-backend --clear default backend cleared (falls back to auto-detection)"},{"p":"9. Building microVMs","h":"Proxmox VE (remote)","u":"09-microvm-setup.html#proxmox-ve-remote","t":"bins/delonix-runtime-bin/src/cmd/vmbackends.rs::register_configured registers the Proxmox backend at startup when configured through the environment (a misconfiguration is a warning, never fatal for unrelated commands): Variable Meaning DELONIX_PROXMOX_URL API base URL, e.g. https://pve.example:8006 . Unset = backend not registered. DELONIX_PROXMOX_NODE Required. The one node this backend addresses (as GET /nodes names it). DELONIX_PROXMOX_SECRET Preferred credential: name of a kind: Secret with tokenId + tokenSecret (or username + password ). DELONIX_PROXMOX_TOKEN_ID + DELONIX_PROXMOX_TOKEN API token from the environment. DELONIX_PROXMOX_USER + DELONIX_PROXMOX_PASSWORD Password login (ticket, re-authenticated on 401). DELONIX_PROXMOX_INSECURE_TLS 1 / true / yes to skip certificate verification. Opt-in, never a fallback. DELONIX_PROXMOX_BRIDGE Default bridge on the node (a per-VM bridge wins). DELONIX_PROXMOX_VLAN Default VLAN tag, 1–4094; out of range is an error, not a silently untagged NIC. Without configuration, --backend proxmox answers that the backend \"is not available in this build\" and says what to set ( run ). The backend owns its storage ( manages_own_storage ), so no local overlay or NoCloud seed is made; --hostname / --ssh-key go to the node's cloud-init, and --user-data is refused. Design and limits: ADR-0008 . An OpenStack backend is proposed only ( ADR-0039 ); there is no code for it."},{"p":"9. Building microVMs","h":"3. Images","u":"09-microvm-setup.html#3-images","t":"VM images live in VmImageStore ( cmd/vmimage.rs ) under <DELONIX_ROOT>/vm-images/ : one qcow2 plus a JSON metadata record ( VmImage ) per image. delonix image vm ls lists them with TYPE (cloud-init / appliance) and DEFAULTS (recorded vCPU/memory)."},{"p":"9. Building microVMs","h":"Official images","u":"09-microvm-setup.html#official-images","t":"OFFICIAL_REPOS in cmd/vmimage.rs : Key Repository Content k8s ghcr.io/angolardevops/delonix-vm-k8s Kubernetes node (kubeadm/kubelet/kubectl + delonix-cri ) base ghcr.io/angolardevops/delonix-vm-base base OS with the delonix engine, no Kubernetes (e.g. ubuntu-24.04 ) appliances ghcr.io/angolardevops/delonix-vm-appliances vendor appliances, no cloud-init delonix vm ls-remote # tags of the Kubernetes golden repo delonix vm ls-remote --no-k8s # tags of the base repo delonix vm pull # the official Kubernetes golden delonix vm pull --no-k8s # the official base image delonix vm pull <oci-ref> --name <local-name> Images are single-blob OCI artifacts; the pull verifies the manifest and blob digests and restores the metadata from manifest annotations. The same verbs exist as delonix image vm pull/ls-remote/push . Note: vm create with no --disk and no local image downloads the official golden image, so it needs network."},{"p":"9. Building microVMs","h":"Building the golden recipe","u":"09-microvm-setup.html#building-the-golden-recipe","t":"delonix image vm build -t <tag> with no VMfile in the context runs the built-in recipe: # Kubernetes node, packages fetched and verified on the HOST, guest offline delonix image vm build --offline --k8s-version 1.34 -t delonix-vm-k8s:1.34 # no Kubernetes: just the engine, rootless-ready delonix image vm build --no-k8s --distro debian --debian-release bookworm -t delonix-vm-base:debian-bookworm Relevant flags (check image vm build --help for defaults): --distro ubuntu|debian|rocky|fedora , --ubuntu-release , --debian-release , --rocky-release , --fedora-release (release and build, e.g. 42-1.1 ), --k8s-version , --offline , --no-k8s , --extra-package , --extra-run , --cri-bin , --delonix-bin , --root-password (without it no account has a password), --node-exporter[=<addr>] , --no-compress . Your own recipe is a VMfile — see 08 — Delonixfile and VMfile . Image builds are amd64 only ( ADR-0018 )."},{"p":"9. Building microVMs","h":"Converting and importing","u":"09-microvm-setup.html#converting-and-importing","t":"delonix vm convert <image-or-path> --to raw|qcow2|vmdk|vdi|vhdx|vhd [-o out] [--compress] delonix image vm import disk.qcow2 -t opnsense:26.1 --appliance --default-vcpus 2 --default-memory 2G vm convert flattens (no backing chain); --compress is accepted only for qcow2 and vmdk . import --appliance records cloud_init: false : vm create then attaches no seed and refuses --hostname / --ssh-key / --user-data by name, because the guest would never read them. Appliance build scripts live in scripts/appliances/ ."},{"p":"9. Building microVMs","h":"4. Create and run","u":"09-microvm-setup.html#4-create-and-run","t":""},{"p":"9. Building microVMs","h":"vm create","u":"09-microvm-setup.html#vm-create","t":"delonix vm create dev --disk delonix-vm-base:ubuntu-24.04 \\ --backend cloud-hypervisor --vcpus 2 --memory 2G \\ --ssh-key @$HOME/.ssh/id_ed25519.pub --hostname dev --wait What happens ( cmd/vm.rs → delonix_vm::create_with ): Node policy is enforced before any image is resolved ( policy::enforce ). Disk resolution ( resolve_image_ref ): --url-img wins (downloaded, cached, verified against <url>.sha256 when offered); else --disk is looked up as a local image name, then as a path to a stored image's qcow2; else it is used as a plain path; with no --disk , the single local golden image, or the official one is pulled. Defaults from the image metadata fill --vcpus / --memory / --backend only where unset. Seed : a NoCloud ISO (network config by MAC, hostname, keys) is generated unless --seed is given, the image is an appliance, or the backend is remote. --user-data replaces the generated user-data. Backend chosen (section 2); an admission check refuses when the host lacks RAM; --namespace other than default is refused on libvirt (the VM lives on virbr0 , outside the Delonix SDN). Overlay : <root>/vms/<name>.qcow2 , a thin qcow2 over the base ( prepare_local_overlay ); --disk-size <GiB> grows it and cannot be smaller than the base. Boot : the backend's boot . create is idempotent: an existing, running VM is returned as is. The published images set no password on any account (see --root-password above), so pass --ssh-key if you want to log in over SSH. --wait and the predicted IP. On l"},{"p":"9. Building microVMs","h":"Day-2 verbs","u":"09-microvm-setup.html#day-2-verbs","t":"Command Notes delonix vm ls [--namespace <ns>] [--ports] [-o json] lists VMs delonix describe vm <name> / delonix delete vm <name> there is no vm describe / vm rm ; the generic verbs replace them delonix vm console <name> [-e ^X] serial console; detach with Ctrl-] by default ( $DELONIX_CONSOLE_ESCAPE ). libvirt: virsh console as a child process; CH: the console socket <root>/vms/<name>.console delonix vm ssh <name|ip> [-l user] [-i key] [-- cmd] IP from the record; default user delonix on cloud-init images, root on appliances delonix vm vnc <name> only for libvirt VMs created with --vnc (CH has no display) delonix vm stop <name> keeps disk, record and snapshots. libvirt: the domain is undefined (snapshot metadata is preserved first) delonix vm start <name> / restart <name> rebuild the boot from the record and reuse the overlay; start on a running VM is a no-op, restart always reboots delonix vm pause / unpause <name> suspend vCPUs, memory kept in RAM; CH and libvirt delonix vm migrate <name> --host <h> --network <n> stop-copy-start over SSH to another host; real downtime ( ADR-0031 explains why live migration is out of scope) delonix vm prune reclaims state no VM record accounts for"},{"p":"9. Building microVMs","h":"Snapshots","u":"09-microvm-setup.html#snapshots","t":"delonix vm snapshot create|ls|rm|restore <vm> [<snapshot>] : Backend Running VM Stopped VM libvirt create is a system checkpoint (memory + disk) disk-only; the four verbs define the domain just for the command cloud-hypervisor ls only ( qemu-img info -U ); create / restore / rm refused — the running VMM locks the disk and CH has no live disk-snapshot API all four, via qemu-img snapshot proxmox create (with VM state), ls , restore same; rm not implemented (fails closed) libvirt snapshots survive vm stop / vm start : undefine --snapshots-metadata removes only libvirt's bookkeeping, so preserve_snapshots dumps each snapshot's XML to <root>/vms/<vm>/snapshots/ before stopping and boot redefines them (rewriting the domain uuid, which changes on every define)."},{"p":"9. Building microVMs","h":"Declarative","u":"09-microvm-setup.html#declarative","t":"kind: VirtualMachine ( compute.delonix.io/v1alpha1 ) mirrors vm create ; a full annotated example is examples/vm.yaml . kind: Workload with type: microvm lowers to a VirtualMachine with the backend forced to cloud-hypervisor ; asking for another backend is an error ( ADR-0006 ). Both run in a scratch root: apiVersion: compute.delonix.io/v1alpha1 kind: VirtualMachine metadata: { name: dev } spec: disk: delonix-vm-base:ubuntu-24.04 resources: { vcpus: 2, memory: 2G } cloudInit: hostname: dev sshKeys: [\"@~/.ssh/id_ed25519.pub\"] --- apiVersion: compute.delonix.io/v1alpha1 kind: Workload metadata: { name: fast } spec: type: microvm microvm: { disk: delonix-vm-base:ubuntu-24.04, vcpus: 1, memory: 1G } $ delonix manifest validate -f vm.yaml stack validate: OK — 2 document(s), all references resolved $ delonix stack apply -f vm.yaml --dry-run | grep backend backend: null backend: cloud-hypervisor Apply with delonix vm apply -f vm.yaml or delonix stack apply -f vm.yaml (not executed here)."},{"p":"9. Building microVMs","h":"5. Networking for VMs","u":"09-microvm-setup.html#5-networking-for-vms","t":"Cloud Hypervisor libvirt Where the NIC lives a tap on a Delonix network ( --network , default ingress ) inside the network holder — the same SDN as containers virbr0 (nat) or a host bridge, in the host's network namespace IP leased by the holder's DHCP, deterministic from the MAC libvirt DHCP; --ip reserves one (nat only) Namespace isolation yes ( --namespace ) refused VM ↔ container by IP direct container → VM works through the host; VM → container needs a published port or vm bridge vm reach (read-only, no privilege) lists the libvirt gateways and, for each port published by a running container, whether a VM can reach it. A port published on the default 127.0.0.1 is invisible to VMs; the command prints the exact re-publish, e.g. DELONIX_PUBLISH_ADDR=<gateway> delonix net ingress publish <c> <port> — reachable from VMs on that network, not from the external LAN. vm bridge <network> [--vm-subnet <cidr>] [--apply] is experimental and needs root : a veth from the host into the holder plus routes, giving libvirt VMs direct IP reachability to one container network. Without --apply it only prints the plan. vm unbridge <network> tears it down (also a dry-run without --apply ). This is the one deliberate exception to rootless in the VM code ( cmd/vmbridge.rs )."},{"p":"9. Building microVMs","h":"6. Troubleshooting","u":"09-microvm-setup.html#6-troubleshooting","t":"Symptom Cause Fix /dev/kvm does not exist / VM fails to start virtualization disabled, or no nested virt enable VT-x/AMD-V; in a VM, enable nested virtualization no VM backend available neither cloud-hypervisor nor virsh + qemu-system-x86_64 on PATH install one; scripts/install.sh does CH VM \"running\" but never answers; overlay stays tiny firmware cannot boot the image (e.g. only hypervisor-fw installed) install EDK2 CLOUDHV.fd to /usr/local/share/delonix/ , or set DELONIX_HYPERVISOR_FW , or use --backend libvirt vm ls shows no IP on a libvirt VM fell back to qemu:///session user-mode join the libvirt group and re-login, or --net-mode nat warning: cannot reach qemu:///system for NAT networking not in the libvirt group sudo usermod -aG libvirt $USER , new session vm console on libvirt: \"Active console session exists\" a previous session died uncleanly current code passes --force to virsh console ; update your binary CH VM refused before boot mentioning the socket path <root>/vms/<name>.sock exceeds 108 bytes shorter DELONIX_ROOT or VM name namespace '…' is not enforceable on the 'libvirt' backend libvirt VMs are outside the SDN --backend cloud-hypervisor , or drop --namespace vm snapshot create refused on a running CH VM CH has no live disk snapshot vm stop first, or use libvirt --hostname / --ssh-key refused the image is an appliance ( cloud_init: false ) configure the appliance through its own console/UI cloud-localds not found missing cloud-image-utils install it (needed for"},{"p":"9. Building microVMs","h":"7. For contributors","u":"09-microvm-setup.html#7-for-contributors","t":""},{"p":"9. Building microVMs","h":"Where the code lives","u":"09-microvm-setup.html#where-the-code-lives","t":"Area Path Port, registry, CH and libvirt backends, create_with , snapshots, firmware lookup crates/adapters/delonix-vm/src/lib.rs NoCloud seed generation crates/adapters/delonix-vm/src/cloudinit.rs Proxmox backend crates/providers/delonix-proxmox/ vm CLI, kind: VirtualMachine , vm reach bins/delonix-runtime-bin/src/cmd/vm.rs Image store, golden recipe, pull/push/import/convert, failure hints bins/delonix-runtime-bin/src/cmd/vmimage.rs VMfile bins/delonix-runtime-bin/src/cmd/vmfile.rs Backend registration from the environment bins/delonix-runtime-bin/src/cmd/vmbackends.rs vm bridge bins/delonix-runtime-bin/src/cmd/vmbridge.rs kind: Workload lowering bins/delonix-runtime-bin/src/cmd/workload.rs Appliance builds scripts/appliances/"},{"p":"9. Building microVMs","h":"Adding a backend","u":"09-microvm-setup.html#adding-a-backend","t":"Read ADR-0008 first; it is the template. In short: Implement VmBackend in its own crate if it talks to a remote API (the engine crate stays free of HTTP clients), placed in the directory of its layer and listed in scripts/arch_fitness.py (see 05 — Architecture ). Register it with register_backend from the process that knows its configuration, with auto_selectable: false unless it is a local, zero-configuration backend built into delonix-vm . Override manages_own_storage , destroy , resume and ip_is_predicted deliberately: for a remote backend stop and destroy are not the same operation, and boot on a stopped VM must not create a second one. Leave unsupported verbs on their fail-closed defaults; refuse unsupported VmConfig fields by name before creating anything. Do not publish a backend that has never been seen booting a VM. New backends and hypervisor boundaries go through an ADR ( docs/adr/ )."},{"p":"9. Building microVMs","h":"Testing","u":"09-microvm-setup.html#testing","t":"Pure unit tests — argv and XML builders are pure functions so they test without a hypervisor: e.g. libvirt_snapshot_argv_uses_flags_not_positional , snapshot_xml_with_uuid , libvirt_domain_xml , the firmware-order test above, the registry and auto_detect tests, and the parser/scaffold tests in cmd/vmfile.rs . Run cargo test -p delonix-vm and cargo test -p delonix-runtime-bin vmfile (see 02 for protoc and the target directory). scripts/e2e.sh — the vm sections run without a hypervisor (listing, refusals) and, when available, exercise snapshots across stop/start on libvirt (needs virsh , qemu-img and a usable qemu:///system ) and on Cloud Hypervisor. It isolates both state roots by default; the CH section refuses to run if only DELONIX_ROOT is isolated. Sections without their hypervisor are reported as skipped, not passed. Proxmox live test — crates/providers/delonix-proxmox/tests/live.rs creates and destroys one VM against a real node and is skipped unless DELONIX_PROXMOX_TEST_URL (plus _NODE , _USER , _PASS ) is set: cargo test -p delonix-proxmox --test live -- --nocapture . Use a disposable node. Provider lifecycle — when changing delonix-vm or a provider, prove the whole lifecycle (create, stop, start, snapshot, destroy) through the delonix CLI only , observing the hypervisor read-only ( virsh -r , API GET s), never repairing it by hand between steps. Look, do not guess. When a guest does not come up, the serial console ( vm console ) or a libvirt screenshot answers in seco"},{"p":"10. Contribution workflow","h":"10. Contribution workflow","u":"10-contributing-workflow.html#10-contribution-workflow","t":"This page is the \"how we work\" part of the handbook: where to make changes, which rules the gates enforce and why, when a change needs a written decision first, and how to send it. The gates themselves, and how to run them, are in 02 — Clone, build and test . For anything non-trivial — a new command, a new manifest Kind, a change to namespace or cgroup setup, a new backend — open an issue first and agree on the approach. It saves a rewrite."},{"p":"10. Contribution workflow","h":"Start from the latest tag, not from memory","u":"10-contributing-workflow.html#start-from-the-latest-tag-not-from-memory","t":"Before you write code: git fetch --tags origin git describe --tags --abbrev=0 origin/main # the newest release git log --oneline \"$(git describe --tags --abbrev=0 origin/main)\"..origin/main | wc -l # how far main is past it git log --oneline -- <path you will touch> # what was already decided, fixed or removed there Reading the history of the area you touch is not ceremony: a lot of this codebase is a record of things that were tried, measured and changed. What was already decided or removed is not redone out of unawareness. The long-form record lives in AGENTS.md (organised by area) and in docs/adr/ ."},{"p":"10. Contribution workflow","h":"One worktree per task","u":"10-contributing-workflow.html#one-worktree-per-task","t":"Several people and tools often work on the same clone at the same time. Editing in a shared checkout has already cost real work here: edits absorbed into someone else's commit, HEAD switching branches in the middle of a task, and a green cargo test on a dirty tree that did not prove HEAD compiled. So every task gets its own git worktree and branch, created from origin/main : git fetch origin git worktree add -b <topic>/<task> <workspace>/.worktrees/delonix-runtime/<task> origin/main cd <workspace>/.worktrees/delonix-runtime/<task> Never put a worktree in /tmp . Many systems empty /tmp on boot; a reboot mid-task takes the uncommitted work, and can leave half-written objects in the shared .git . Use a persistent directory outside the repository (the convention here is a .worktrees/ directory next to the repositories), so no grep -r , docker build context or gate script picks it up. Commit and push early , at every step that passes its checks. A persistent worktree survives a reboot; a pushed branch survives everything else. Stage files by name : git add <file> <file> , never git add -A , -u or . . Check git branch --show-current and recognise every entry of git status --short as yours before committing. Never git checkout -- <path> in a tree someone else may be using: it reverts to HEAD without a stash and destroys their uncommitted work. Rebase, do not merge , when your push is rejected for divergence: git pull --rebase . The history is linear. When the task is done, remove bo"},{"p":"10. Contribution workflow","h":"Version alignment","u":"10-contributing-workflow.html#version-alignment","t":"The version in the root Cargo.toml is load-bearing: it selects which release delonix-cri is downloaded from, it is reported as the Docker API ServerVersion , it is recorded in every backup, and the release workflow compares it with the built binary. scripts/version_gate.py (CI job version ) allows exactly two states: Equal to the newest tag the commit contains — all ordinary work. Do not bump the version in a feature PR, and do not use a -dev suffix (it would send the delonix-cri download to a release that does not exist). Greater, only in the release commit , together with docs/releases/v<version>.md . It also fails a branch that does not contain the newest tag : the branch started before that release, and merging it as-is would undo what the release published. Rebase onto origin/main . Between releases, delonix --version tells builds apart by commit and distance ( commit: <hash> (+N commits since vX.Y.Z) ), because two builds with the same version number are not the same build."},{"p":"10. Contribution workflow","h":"Language: English in the code (LANG-01)","u":"10-contributing-workflow.html#language-english-in-the-code-lang-01","t":"Identifiers, comments and user-facing messages are written in English . Portuguese reaches the operator only through the translation catalogue: bins/delonix-runtime-bin/src/cmd/po.rs — po::t(\"…\") for fixed strings, po::tf(\"… {name} …\", &[(\"name\", value)]) for interpolated ones (named placeholders, because a translation may reorder them). CLI --help text is translated at runtime by po::translate_help . bins/delonix-runtime-bin/data/pt.po — the Portuguese entries, embedded in the binary and selected with --l18n pt or DELONIX_L18N=pt . A missing catalogue entry degrades to English; a Portuguese string written directly in the code is a bug. scripts/lang_ratchet.py (CI job lang ) counts the Portuguese still left in identifiers, comments and messages against scripts/lang_baseline.json . It is a ratchet , not a ceiling: it fails when a count rises (new Portuguese entered) and when it falls without the baseline being lowered. When you translate something, run python3 scripts/lang_ratchet.py --update and commit the new baseline in the same commit as the translation. Tests in the CLI crate also check that command help has a Portuguese entry — add one when you add a command or flag."},{"p":"10. Contribution workflow","h":"Architecture rules the gates enforce","u":"10-contributing-workflow.html#architecture-rules-the-gates-enforce","t":"scripts/arch_fitness.py (CI job arch ) enforces the structure decided in ADR-0040 . The layer of each crate and the allowed direction are listed in 05 — Architecture . What it means for a change: The engine knows no consumer. No name of any product, platform, control plane, console or agent that uses the engine — nor tenant, account, plan or billing — in crates/ , bins/ , proto/ or the manifests, comments included. A requirement that comes from a consumer is written as the generic capability it is, in the engine's own vocabulary, and only enters if it makes sense for any client. History that needs external names lives in docs/ , never in code. Dependencies point inward. Foundation depends only on foundation; contexts do not depend on adapters; adapters and providers do not depend on interfaces; a binary composes one interface. The directory is the layer. A crate lives under crates/<layer>/ matching its entry in the LAYERS table. A new crate enters LAYERS and the right directory in the same commit. Dependency versions live only in the root [workspace.dependencies] ; a member crate writes { workspace = true, features = [...] } and nothing else. Exceptions name the phase that removes them. An exception without a phase fails, and so does an exception that no longer applies. Debt ratchets — for example self_exec_sites (a library re-running the engine's own binary instead of calling a function), library_prints ( println! / eprintln! in a library crate — libraries emit tracing , int"},{"p":"10. Contribution workflow","h":"When to write an ADR","u":"10-contributing-workflow.html#when-to-write-an-adr","t":"Write an Architecture Decision Record in docs/adr/ before the code when a change moves a structural boundary, for example: a new backend or provider (a hypervisor, a storage system), or a new port; a new external dependency in an engine crate, or a new daemon or resident process; a new privilege boundary — which also needs a GO/NO-GO spike first; a change to the node contract, the manifest schema's stability, or the layer structure. A routine feature inside an existing boundary does not need one. Format and the current list are in docs/adr/README.md : one file per decision, NNNN-title.md , in English. Accepted ADRs are never rewritten — a new ADR supersedes them."},{"p":"10. Contribution workflow","h":"Adding or changing a CLI command","u":"10-contributing-workflow.html#adding-or-changing-a-cli-command","t":"Wire every entry point. Several commands are reachable by more than one path (for example delonix vm pull and delonix image vm pull ). Change all of them, then check each one with the binary you built — including shell completion, because the clap declaration you edited may not be the one the user's path parses. The completion engine can be probed directly; _CLAP_COMPLETE_INDEX is the position of the word being completed: COMPLETE=bash _CLAP_COMPLETE_INDEX=3 ./target/debug/delonix -- delonix image vm '' Validate against the binary , not the source: ./target/debug/delonix <group> <command> --help and a real run with the state roots isolated (see 02 ). Update the CLI baseline ( scripts/cli-tree.sh --update ) in the same commit when you add or remove a leaf, and regenerate the site ( python3 docs/gen.py with a release build) when help text changes. Unit-test every new pure function — parsers, validators, argument builders. This codebase has a long record of real bugs caught exactly there. Classify errors. Exit codes carry a class (not found, conflict, …) decided in one place from the error type; return the right Error variant rather than a generic one."},{"p":"10. Contribution workflow","h":"Commits and pull requests","u":"10-contributing-workflow.html#commits-and-pull-requests","t":"One logical change per commit; say why in the message — the diff already shows what. Reference the issue when there is one. Open the PR against main and fill in the template : what you ran, and for runtime, namespace, cgroup or network code, what you ran live on a real host, with the command and its output. \"It compiles\" and \"the command returned 0\" do not close a change. Say what was proved and, just as explicitly, what was not validated and why. Every change is reviewed by the code owners listed in .github/CODEOWNERS ."},{"p":"10. Contribution workflow","h":"Security-sensitive changes","u":"10-contributing-workflow.html#security-sensitive-changes","t":"Call it out explicitly in the PR when a change crosses a privilege or namespace boundary: user namespace mapping, the network holder or its control socket, setns / unshare , capability or seccomp handling, or path handling driven by user or manifest input. These get extra review. If you found a vulnerability rather than a bug — privilege escalation, namespace escape, command injection, path traversal — do not open a public issue or PR. Follow SECURITY.md (GitHub Private Vulnerability Reporting)."},{"p":"11. Publishing the documentation","h":"11. Publishing the documentation","u":"11-publishing-docs.html#11-publishing-the-documentation","t":"Documentation in this repository is either generated from the code (and then checked by a gate) or written by hand (and then reviewed). This page explains which is which, how each is published, and what you have to do when your change affects documentation."},{"p":"11. Publishing the documentation","h":"Where it is published","u":"11-publishing-docs.html#where-it-is-published","t":"The site is served by GitHub Pages from the /docs directory of main , at https://angolardevops.github.io/delonix-runtime/ . The Pages source setting itself lives in the repository settings, not in the tree; what the tree does contain is docs/.nojekyll , which turns Jekyll off so every file under docs/ is served exactly as committed. A consequence worth knowing: because Jekyll is off, Markdown files under docs/ — including this handbook in docs/dev/ — are not rendered to HTML by Pages; they are served as plain files. The rendered view of the handbook is GitHub's own Markdown rendering when browsing the repository ( docs/dev/README.md on github.com), which also renders the Mermaid diagrams. Links between handbook pages are relative, so they work in both places."},{"p":"11. Publishing the documentation","h":"Who owns what","u":"11-publishing-docs.html#who-owns-what","t":"Surface What it is How it stays true README.rst the project front page hand-written, checked by docs_cli_gate.py docs/*.html , docs/comandos/*.html the user site generated by docs/gen.py from the binary's --help plus editorial text in the generator; checked by the docs CI job and at release time docs/releases/v<version>.md release notes, one per tag hand-written in the release commit; published as the GitHub Release body docs/RELEASES.md the per-release feature appendix generated by scripts/gen-releases.sh from docs/releases/ ; never edit by hand ARCHITECTURE.md C4 architecture diagrams hand-written, kept in step with the code; rendered into the site by docs/gen.py docs/adr/ architecture decisions one file per decision; accepted ADRs are superseded, never rewritten docs/api/openapi.yaml the node API's REST encoding generated from proto/delonix/node/v1 by scripts/contract_gate.py --update docs/dev/ this contributor handbook generated facts ( scripts/dev_docs.py ) plus hand-written narrative CONTRIBUTING.md the short front door for contributors hand-written; points into docs/dev/"},{"p":"11. Publishing the documentation","h":"The user site: docs/gen.py","u":"11-publishing-docs.html#the-user-site-docsgenpy","t":"The reference pages embed the real --help of the delonix binary, captured when the generator runs, so the site never documents a flag that does not exist. The editorial content (introductions, examples, notes) lives in dictionaries inside docs/gen.py itself — edit it there, not in the HTML. cargo build --release -p delonix-runtime-bin python3 -m pip install markdown # the generator renders ARCHITECTURE.md python3 docs/gen.py # uses the tree's release binary by default git diff --stat -- docs/ The generator also accepts the binary path as its first argument. Commit the regenerated files together with the CLI change that caused them. Two checks fail if you do not: the docs job in CI regenerates the site and fails when git diff -- docs/ is not empty; the same job validates every examples/*.yaml with stack apply --dry-run and stack validate , and checks the man pages generated by delonix man --dir <dir> --index with groff ; the release workflow repeats the regeneration against the release build before publishing, so a tag whose site is out of date does not ship."},{"p":"11. Publishing the documentation","h":"The command-citation gate: scripts/docs_cli_gate.py","u":"11-publishing-docs.html#the-command-citation-gate-scriptsdocs_cli_gatepy","t":"Every delonix … command quoted in a code context ( <code> , <pre> , Markdown code fences, reST literal blocks, YAML comments) in the current documentation must resolve in the command tree of the binary built from the tree (read through scripts/cli-tree.sh ). It runs in the cli-surface CI job: cargo build --release -p delonix-runtime-bin DELONIX_BIN=\"$PWD/target/release/delonix\" python3 scripts/docs_cli_gate.py python3 scripts/docs_cli_gate.py --list # every citation and where it is It exists because commands were removed in consecutive major releases and several current pages kept teaching unrecognized subcommand . Dated historical records — docs/releases/ , docs/RELEASES.md , docs/discovery/ and the dated audit and measurement reports — are excluded on purpose: they must keep quoting the spelling of the version they describe. A citation that is wrong on purpose goes into scripts/docs_cli_allow.tsv with a reason. The files it scans are listed in TARGETS at the top of the script. Check that list when you add a new documentation directory; a page outside it is not checked."},{"p":"11. Publishing the documentation","h":"The handbook's facts: scripts/dev_docs.py","u":"11-publishing-docs.html#the-handbooks-facts-scriptsdev_docspy","t":"The structural facts in docs/dev/ — the crates and their layers, the dependency graph, the binaries, the pinned toolchain and the CI jobs — are generated from Cargo.toml , scripts/arch_fitness.py , rust-toolchain.toml and .github/workflows/ci.yml . In a page they sit between markers: <!-- dev-docs:begin <key> --> …generated, do not edit… <!-- dev-docs:end <key> --> python3 scripts/dev_docs.py # rewrite every generated region python3 scripts/dev_docs.py --check # exit 1 when docs/dev is stale (CI job `arch`) Rules: Never edit inside a region. The next regeneration overwrites it and --check fails in CI. If a fact is wrong, fix its source ( Cargo.toml , the LAYERS table in arch_fitness.py , ci.yml ) or the generator. Text outside the markers is never touched, so narrative can surround a generated table. A new region needs a render_* function, a key in regions() and a marker on a page, in the same commit. No numbers that change on every commit (lines, tests, commits) — not in the generator and not in the narrative. A gate that is red on every PR stops being read, and a hand-written count silently becomes false. If your PR adds or moves a crate, changes a dependency between crates, adds a binary, bumps the toolchain or changes a CI job, run python3 scripts/dev_docs.py and commit the result with it."},{"p":"11. Publishing the documentation","h":"What happens at release time","u":"11-publishing-docs.html#what-happens-at-release-time","t":"The release workflow ( .github/workflows/release.yml ) runs on a pushed v* tag. For documentation it: regenerates the user site against the release build and fails if docs/ differs; publishes the GitHub Release with docs/releases/<tag>.md as its notes (or generated notes when that file does not exist); checks out main , runs scripts/gen-releases.sh (the docs/RELEASES.md appendix) and scripts/dev_docs.py (the handbook facts), and commits both to main with [skip ci] when they changed. The narrative of the handbook is not regenerated by CI. After a release has been published and validated, a maintainer-run review step reads the changes between the previous and the new tag together with the release notes, updates only the handbook pages those changes affect, and opens a pull request for it. When nothing structural or procedural changed, the outcome of that review is \"nothing to update\", stated explicitly."},{"p":"11. Publishing the documentation","h":"What this means for your pull request","u":"11-publishing-docs.html#what-this-means-for-your-pull-request","t":"Your change Also do CLI help, a command, a flag python3 docs/gen.py (release build) and commit docs/ ; update scripts/cli_baseline.tsv if leaves changed; fix any citation docs_cli_gate.py reports A crate, a crate dependency, a binary, the toolchain or a CI job python3 scripts/dev_docs.py and commit docs/dev/ The node contract in proto/ python3 scripts/contract_gate.py --update and commit docs/api/openapi.yaml A structural decision an ADR in docs/adr/ (see 10 ) A user-visible feature describe it in the PR so it can go into the next release notes How contributors build, test or work the relevant page of this handbook"},{"p":"12. Coding conventions","h":"12. Coding conventions","u":"12-coding-conventions.html#12-coding-conventions","t":"This page tells you how to write code that passes review in this repository, so you don't have to guess the rules or make up your own. Every rule below carries a tag and a source: Enforced (gate) : a CI job fails if you break it. The gate is named, so you can run it locally (see 02 — Build and test ). Decided (ADR/AGENTS.md) : an Accepted Architecture Decision Record in docs/adr/ or a section of AGENTS.md settles it. No gate checks it yet, so review does. Proposed (ADR, not yet decided) : the only written source is an ADR whose status is still Proposed (see the status column of docs/adr/README.md ). It is the direction the code is moving in, and review applies it, but it can still change; when the ADR is accepted or rejected, the tag changes with it. Convention (observed) : the code does it consistently and nothing wrote it down. The examples are real path:symbol references. Copy them. If a question is not answered here, and the code around your change doesn't answer it either, the honest answer is \"not decided — follow the surrounding code\" . The list of known open questions is in Undecided . Don't turn a personal preference into a rule in a PR. Related pages: the layers and the reasons behind them in 05 — Architecture , what each crate holds in 06 — Crates , the Rust idioms in 03 — Rust primer , and the workflow (worktree, version, ADRs, PRs) in 10 — Contribution workflow ."},{"p":"12. Coding conventions","h":"1. The tooling that enforces style","u":"12-coding-conventions.html#1-the-tooling-that-enforces-style","t":"Tool What it checks Tag and source rustfmt Formatting. No config file : there is no rustfmt.toml or .rustfmt.toml at the root, so the defaults apply. Enforced (gate) : CI job fmt runs cargo fmt --all --check ( .github/workflows/ci.yml ). CONTRIBUTING.md § Style: \" cargo fmt defaults, no custom config\". clippy Every clippy lint and every rustc warning fails the build, tests included ( --all-targets ). No clippy.toml , so the lint defaults apply. Enforced (gate) : CI job clippy runs cargo clippy --workspace --all-targets --locked -- -D warnings . [workspace.lints] The root Cargo.toml declares exactly one workspace lint: [workspace.lints.clippy] undocumented_unsafe_blocks = \"deny\" . Every member manifest has [lints] workspace = true . Enforced (gate) : clippy. It was introduced by phase P0 of ADR-0040 (\" [workspace.lints] ( undocumented_unsafe_blocks = deny )\"), an ADR that is still Proposed ; the gate holds regardless. cargo-deny RUSTSEC advisories and yanked crates, permissive licences only (no GPL/AGPL), crates.io as the only registry, no git sources. The [bans] section of deny.toml (duplicate versions and wildcards, set to warn) is not evaluated in CI. Enforced (gate) : CI job deny runs check advisories licenses sources with deny.toml . Convention (observed) : every ignored advisory in deny.toml carries a comment with its reason — no gate checks that the comment exists. scripts/lang_ratchet.py Portuguese in identifiers, comments and user-facing strings (LANG-01, see §2 ). En"},{"p":"12. Coding conventions","h":"2. Language of the code","u":"12-coding-conventions.html#2-language-of-the-code","t":"Identifiers, comments and messages are written in English. Enforced (gate) : scripts/lang_ratchet.py . Decided : AGENTS.md § \"Língua do código: inglês (LANG-01)\". The ratchet scans every .rs , .py , .ts , .go and .yml / .yaml file outside target/ , third_party/ and similar. It counts three things: identifiers : any name declared by fn , struct , enum , trait , const , static , type , mod , union or let whose snake_case / camelCase segments contain a word from scripts/lang_pt_lexicon.txt ; comments : // , /// and //! lines containing a lexicon word; user text : literals of 8 or more characters inside format! , println! , eprintln! , panic! , anyhow! , bail! , expect or unimplemented! . Portuguese reaches the operator only through the catalogue. Decided : AGENTS.md § \"i18n (fonte EN + catálogo pt.po embutido)\" and CONTRIBUTING.md § \"If you add or change a CLI command\". The English text goes in the source. The Portuguese goes in bins/delonix-runtime-bin/data/pt.po , which bins/delonix-runtime-bin/src/cmd/po.rs embeds with include_str! . po::t(\"…\") for a fixed string. po::tf(\"… {name} …\", &[(\"name\", value)]) for interpolated text. Use named placeholders: a translation may reorder them, and format! needs a compile-time literal, so the template is translated first and the values are substituted afterwards ( po.rs , doc comment of tf ). // bins/delonix-runtime-bin/src/cmd/config.rs — refuse_unknown_key Err(Error::Invalid(super::po::tf( \"'{key}' is not a config key — known: {known}\","},{"p":"12. Coding conventions","h":"3. Naming","u":"12-coding-conventions.html#3-naming","t":""},{"p":"12. Coding conventions","h":"3.1 Crates and directories","u":"12-coding-conventions.html#31-crates-and-directories","t":"The directory is the layer. A crate lives in crates/foundation/ , crates/contexts/ , crates/adapters/ , crates/providers/ or crates/interfaces/ , or it is a binary in bins/ . The directory must match the crate's entry in the LAYERS table. Enforced (gate) : scripts/arch_fitness.py misplaced() / LAYER_DIR . A new crate goes into LAYERS and the right directory in the same commit. Target naming convention for new or restructured crates. Proposed (ADR, not yet decided) : ADR-0040 D2.1. Role Name Shared pure foundation delonix-model Bounded context delonix-<context> , named after the published API group (D2.2): delonix-compute , delonix-stack Technology adapter delonix-<technology> : delonix-linux , delonix-sdn , delonix-oci Pluggable provider delonix-provider-<technology> Interface library delonix-<protocol> : delonix-cri , delonix-mcp A crate exists only if it is a bounded context, isolates a heavy or privileged dependency, or is a separately installed binary. No -core , -common , -utils or -types suffixes (ADR-0040 D2.1 records how a -core crate became \"the sink of everything\"). Some crates still carry older names: delonix-runtime-core , delonix-proxmox , delonix-truenas , delonix-security-runtime . ADR-0040 renames each one in the phase that restructures it, \"never twice\". Don't rename a crate outside its phase. - Every crate's path is written once , in [workspace.dependencies] of the root Cargo.toml . Members depend on each other with { workspace = true } . Decided : AGENTS.md"},{"p":"12. Coding conventions","h":"3.2 Modules and files","u":"12-coding-conventions.html#32-modules-and-files","t":"CLI command groups : one module per group in bins/delonix-runtime-bin/src/cmd/<group>.rs , with a pub enum <Group>Cmd (clap subcommands), a pub fn run(action: <Group>Cmd) -> Result<()> dispatcher, and one cmd_<verb> function per subcommand. Convention (observed) : cmd/volume.rs:VolumeCmd + run + cmd_create / cmd_ls / cmd_describe ; cmd/secret.rs:SecretCmd + run ; cmd/container.rs:cmd_run / cmd_start / cmd_stop . AGENTS.md § \"CLI ( delonix )\" states the one-module-per-group part. Adapters for a compute port go in a file named after the port's concern, not after the technology: crates/adapters/delonix-linux/src/workload.rs , .../run_host.rs , crates/adapters/delonix-sdn/src/run_network.rs , .../vm_network.rs , crates/adapters/delonix-oci/src/run_images.rs . Convention (observed) ."},{"p":"12. Coding conventions","h":"3.3 Types, traits, functions, constants","u":"12-coding-conventions.html#33-types-traits-functions-constants","t":"Ports are named after the capability, not the technology. Proposed (ADR, not yet decided) : ADR-0040 D3 lists the ports: WorkloadRuntime , SandboxProvider , VmProvider , NetworkProvider , StorageProvider , ImageRegistry , ImageStore , … The ones that exist today are in crates/contexts/delonix-compute/src/ports.rs ( ImageStore , StorageProvider , DeviceResolver , RunHost , VmNetwork , NetworkProvider ) and .../launch.rs ( WorkloadRuntime ). The older VmBackend in crates/adapters/delonix-vm/src/lib.rs is due to become VmProvider in P4. Host implementations of a port are called Host<Thing> . Convention (observed) : delonix-linux/src/workload.rs:HostWorkload (implements WorkloadRuntime ), delonix-sdn/src/run_network.rs:HostNetwork (implements NetworkProvider ), delonix-oci/src/run_images.rs:HostImages , delonix-volume/src/lib.rs:HostVolumes , delonix-linux/src/cdi.rs:HostDevices . Pure decision functions are verbs or questions: resolve_* , parse_* , valid_* , is_* , *_plan . Convention (observed) : cmd/vm.rs:resolve_vm_defaults , delonix-oci/src/registry.rs:parse_content_range , cmd/stack.rs:is_pending , delonix-net-rules/src/lib.rs:bridge_name . Constants are SCREAMING_SNAKE_CASE ( Enforced (gate) , rustc lint under clippy -D warnings ). Kind names are constants too, never repeated string literals ( Decided : AGENTS.md § \"Os Kinds ganham grupos e nomes definitivos\"; the constants are in crates/contexts/delonix-stack/src/kinds.rs : pub const VM: &str = \"VirtualMachine\"; )."},{"p":"12. Coding conventions","h":"3.4 Test names","u":"12-coding-conventions.html#34-test-names","t":"The ratchet counts test names. lang_ratchet.py matches every fn declaration and does not skip #[cfg(test)] , so a Portuguese test name raises identifiers and fails the gate. Enforced (gate) . Many existing tests have Portuguese sentence names, for example delonix-model/src/exitcode.rs:nao_existe_e_rebentou_deixam_de_ser_o_mesmo_numero . That is counted debt, not a style to copy. New tests are English sentences that state the behaviour being proven , like the newer tests in the same file: a_missing_capability_is_not_a_wrong_argument and the_text_class_and_the_number_cannot_diverge . Decided : LANG-01 (AGENTS.md); the \"sentence\" shape is a Convention (observed) . If you translate an existing test name, the count goes down, so run python3 scripts/lang_ratchet.py --update in the same commit."},{"p":"12. Coding conventions","h":"3.5 CLI commands and flags","u":"12-coding-conventions.html#35-cli-commands-and-flags","t":"Grouped commands, delonix <group> <verb> . No flat top-level shortcuts. Decided : AGENTS.md § \"Reorganização da raiz da CLI (v0.30.0)\"; docs/cli-stability.md (the top-level shortcuts were removed in v1.0.0). Verbs follow Docker/Podman/kubectl when such a verb exists. Decided : AGENTS.md § the \"Reestruturação da CLI (semântica Docker/Podman/kubectl)\" sprints; docs/cli-stability.md § \"Estável\". Listing verbs use ls ( network ls , volume ls , image ls …). image list was reverted to ls in v2.0.0 ( docs/cli-stability.md ). create only creates, and refuses an existing name with exit 5 unless --force . Upsert is a separate verb ( secret set ). apply is idempotent \"ensure present\". Decided : AGENTS.md § \"Sprint 1: secret create vs secret set \". describe is for humans (kubectl style), inspect is JSON for scripts. Decided : AGENTS.md § \"Output: ls estilo docker, describe estilo kubectl\". Flag order and names copy Docker where Docker has the concept: network connect <NETWORK> <CONTAINER> , -p [hostIp:]hostPort:containerPort , volume create --driver … --opt k=v . Decided : AGENTS.md § Sprints 5 and 6. Breaking changes are clean cuts, without aliases. The old spelling must fail with unrecognized subcommand , never silently do something else. Before you cut, grep for internal callers across the whole workspace. Decided : docs/cli-stability.md § \"Como uma quebra é feita\". The groups listed as stable in that file may only break in a major release. A command reachable from several paths must "},{"p":"12. Coding conventions","h":"3.6 Kinds, API groups and manifest fields","u":"12-coding-conventions.html#36-kinds-api-groups-and-manifest-fields","t":"Kinds are UpperCamelCase nouns in one of the published groups core , compute , networking , gateway , storage , artifact , infrastructure ( <group>.delonix.io/v1alpha1 ). Decided : AGENTS.md § \"Identidade e fronteira do motor\" and § \"Os Kinds ganham grupos\" (ADR-0020, which introduced the groups, is still Proposed ). Each Kind is one row in crates/contexts/delonix-stack/src/kinds.rs ( KindFacts : kind , plural , short , api_version , domain , form , in_stack , converges , …). delonix api-resources prints that table. Adding a Kind also touches tables that nothing derives from kinds.rs ( hot_fields , NAMESPACE_SOURCES , TYPED_KINDS , the generated schema). The tests fail until each one is done. Enforced (gate) : AGENTS.md § \" kind: Service \" lists which test caught each table. A renamed Kind keeps its old name as a silent, case-insensitive alias. A merge warns, because its meaning changed. Decided : AGENTS.md § \"Os Kinds ganham grupos e nomes definitivos\" (\"Alias silencioso, não depreciação\"); implemented in cmd/manifest.rs:KIND_ALIASES . ADR-0020 is still Proposed . Manifest fields are camelCase . If a field had a snake_case spelling before, that spelling stays accepted as a serde alias . Convention (observed) , per field rather than rename_all : // bins/delonix-runtime-bin/src/cmd/vm.rs — VmSpec /// Canonical `cpuAffinity`; `cpu_affinity` stays accepted (back-compat). #[serde(rename = \"cpuAffinity\", alias = \"cpu_affinity\")] cpu_affinity: Option<String>, Other examples: deloni"},{"p":"12. Coding conventions","h":"3.7 Environment variables","u":"12-coding-conventions.html#37-environment-variables","t":"Prefix DELONIX_ , upper case: DELONIX_ROOT , DELONIX_NET_RUNTIME_DIR , DELONIX_L18N , DELONIX_LOG_FORMAT , DELONIX_CRI_CAP_CEILING . Convention (observed) across crates/ and bins/ . For telemetry, read the standard OTEL_* variables , not a new DELONIX_* alias. Proposed (ADR, not yet decided) : ADR-0040 D6. This is the target, not today's code: crates/adapters/delonix-telemetry/src/telemetry.rs reads DELONIX_OTLP_ENDPOINT for the OTLP exporter, and from the standard set only OTEL_SERVICE_NAME . Don't add a new DELONIX_* telemetry variable, and don't remove DELONIX_OTLP_ENDPOINT outside the phase that migrates it. An escape hatch that weakens a safety default is loud and explicit. It is off unless set to 1 , and it warns: DELONIX_ENABLE_IPV6=1 , DELONIX_ALLOW_LINK_LOCAL=1 . Decided : AGENTS.md § \"Bloco 0 do plano 33 (v0.37.1)\". A flag beats the environment variable, which beats the default ( serve cri --cap-ceiling vs DELONIX_CRI_CAP_CEILING ). Decided : AGENTS.md § \"Tecto de capabilities no CRI\"."},{"p":"12. Coding conventions","h":"3.8 Exit codes and DX_* codes","u":"12-coding-conventions.html#38-exit-codes-and-dx_-codes","t":"Exit codes are derived from the error type in one place : the exhaustive crates/foundation/delonix-model/src/exitcode.rs:for_error . The classes are 1 generic, 2 usage, 3 NOT_RUNNING , 4 NOT_FOUND , 5 CONFLICT , 69 UNAVAILABLE , 74 IO , 77 NO_PERMISSION , 124 TIMEOUT . Each error also has a stable text identity, Error::code() , which returns a DX_* string. Enforced (gate) : the match has no _ => arm, so a new variant stops the build until someone classifies it. The test the_text_class_and_the_number_cannot_diverge keeps the two in step. Decided : AGENTS.md § \"Códigos de saída com classe (v0.49.0)\"; docs/cli-stability.md § \"Códigos de saída\". You don't pick a number, you return the right variant. \"It doesn't exist\" is Error::NotFound , \"it already exists\" is Error::Conflict , \"this host lacks a tool\" is Error::Unavailable . A new number needs a real producer. Decided : module docs of exitcode.rs (\"every extra number is a promise\")."},{"p":"12. Coding conventions","h":"4. Structure: where code goes","u":"12-coding-conventions.html#4-structure-where-code-goes","t":""},{"p":"12. Coding conventions","h":"4.1 The layers and the direction","u":"12-coding-conventions.html#41-the-layers-and-the-direction","t":"The allowed direction is written in one place, ALLOWED in scripts/arch_fitness.py . Enforced (gate) : Layer May depend on foundation foundation context foundation, context adapter / provider foundation, context interface foundation, context, adapter, provider bin everything Dev- and build-dependencies don't count. A declared exception must name the ADR-0040 phase that removes it, and an exception that no longer applies also fails ( EXCEPTIONS ). The generated layer table and current exceptions are in 05 . More structural rules, each Enforced (gate) by scripts/arch_fitness.py : Foundation and contexts stay pure of heavy dependencies. tokio , axum , hyper , tonic , reqwest , clap , ratatui , serde_yaml , rmcp , OpenTelemetry and prometheus-client are refused there ( HEAVY ). A binary composes exactly one interface. Enforced in rule_failures() (introduced by ADR-0040 D2.4, still Proposed ; also written in AGENTS.md § \"A direcção das dependências é um portão\"). Dependency versions live only in the root [workspace.dependencies] . A member writes { workspace = true, features = [...] } and nothing else. default-features = false stays in the root, because a member cannot turn off what the root turns on ( inline_versions() ). Libraries don't print. The library_prints ratchet counts println! / eprintln! / print! outside bins/ . Emit tracing instead (for example tracing::warn! in crates/adapters/delonix-sdn/src/lib.rs ) and let the interface present the output. Decided : AGENTS.md § \"A "},{"p":"12. Coding conventions","h":"4.2 \"My change is X → it goes in Y\"","u":"12-coding-conventions.html#42-my-change-is-x-it-goes-in-y","t":"This table uses the crates as they exist today. Check 06 for each crate's contents before you add to it. Where the source column cites ADR-0040 or ADR-0026, the tag is Proposed (ADR, not yet decided) : both ADRs are still Proposed, even though the crates they describe already exist. Your change Crate (layer) Source A pure rule on CIDRs, bridge names or IPAM arithmetic that both sides must compute identically delonix-net-rules (foundation) 06; AGENTS.md § Arquitetura A new error class, exit code or DX_* code; generated names delonix-model (foundation) ADR-0040 D1 A persisted record type, a store, the secret store, write_atomic* helpers delonix-runtime-core (foundation) — being split by ADR-0040 P3. Add here only what belongs to records and stores, never general helpers ADR-0040 D2.1 \"no -core \" Kind facts, the planner/diff, conditions, revisions delonix-stack (context) AGENTS.md § Arquitetura The run specification, its pure validation, a port the run use case needs delonix-compute (context): run_opts.rs , preflight.rs , ports.rs ADR-0040 D2.2 Security policy, admission, score, redaction delonix-security-runtime (context) ADR-0026 Namespaces, cgroups, mounts, capabilities, seccomp, devices delonix-linux (adapter) ADR-0040 D2.3 netns holder, nftables, slirp, DNS, DHCP, overlay, WireGuard, CNI delonix-sdn (adapter) ADR-0040 D2.3 Registry client, CAS, layers, overlay, image build delonix-oci (adapter) ADR-0040 D2.3 SBOM / CVE delonix-scanner (adapter) ADR-0040 D2.3 Tracing, OpenTe"},{"p":"12. Coding conventions","h":"4.3 Pure core, I/O at the edges","u":"12-coding-conventions.html#43-pure-core-io-at-the-edges","t":"Decisions are pure functions over data you have already read. They take no store, run no command and need no privilege, so a test can call them with plain values. Proposed (ADR, not yet decided) : ADR-0040 D1 (a context's domain/ has \"no I/O, no tokio , libc , nix , std::fs \"). Convention (observed) : delonix-stack/src/reconcile.rs (\"decides it WITHOUT touching the machine\"), cmd/vm.rs:resolve_vm_defaults , delonix-sdn/src/infra.rs:vmtap_line , delonix-oci/src/registry.rs:parse_content_range . // bins/delonix-runtime-bin/src/cmd/stack.rs — is_pending fn is_pending(present: &str, kind: &str, status: &str) -> bool { match present { // Declarative: nothing to observe, so nothing to wait for. \"-\" => false, \"yes\" => !ready_status(kind, status), // \"no\" (absent) and \"?\" (unknown/unreadable) both keep waiting. _ => true, } } If a pure function needs something from the outside, take it as a parameter. For example, resolve_image_ref takes the image store instead of opening the real one, so the test can pass a temporary directory. Decided : AGENTS.md § \"O manifesto de VM resolvia a imagem de outra maneira que a CLI\". One rule, one owner. When two call sites need the same derivation, extract one function and call it from both. A second copy drifts. Examples: delonix_net_rules::bridge_name , re-exported by delonix-sdn (the bridge name had two formulas and printed a device that did not exist), infra::dhcp_lease_ip , effective_entrypoints . Decided : AGENTS.md § \" delonix network \", § \"Iso"},{"p":"12. Coding conventions","h":"5. Internal API and boundaries","u":"12-coding-conventions.html#5-internal-api-and-boundaries","t":""},{"p":"12. Coding conventions","h":"5.1 Ports and adapters","u":"12-coding-conventions.html#51-ports-and-adapters","t":"A new backend implements a port. It is never an if provider == … somewhere else. Decided : AGENTS.md § \"Identidade e fronteira do motor\" (\"Um provider novo entra como implementação de uma porta, nunca como um if provider == … \"). ADR-0040 D3 rule 3 (\"No string matching on provider names outside the composition root\") restates it and is still Proposed . D3 plans a fitness test for this, but it does not exist yet in arch_fitness.py , so review enforces it for now. Backend-specific knowledge lives on the backend. For example, VmBackend::ip_is_predicted() answers whether a VM's IP was predicted, instead of the call site checking backend.contains(\"cloud-hypervisor\") . Decided : ADR-0008, quoted in the doc comment in crates/adapters/delonix-vm/src/lib.rs . An adapter does not depend on another adapter. What it needs from another concern comes in as a hook or a port, wired by the composition root. Enforced (gate) : ALLOWED (adapter → foundation, context). Convention (observed) : the doc comment of delonix-linux/src/workload.rs:HostWorkload explains its addresses / attach_slirp hooks this way. // crates/contexts/delonix-compute/src/ports.rs — NetworkProvider (excerpt) pub trait NetworkProvider { /// Refuses a network that does not exist. fn check_network(&self, name: &str) -> Result<()>; /// Undoes an attach; best effort, used on the way out of a failure. fn detach(&self, id: &str, ip: &str); /// Publishes one `-p` specification on a container's address. fn publish(&self, ip: &str, s"},{"p":"12. Coding conventions","h":"5.2 Errors per crate (ADR-0040 P3)","u":"12-coding-conventions.html#52-errors-per-crate-adr-0040-p3","t":"An adapter or provider defines its own Error and converts it into the shared class ( delonix_model::Error ), which carries the DX_* code. Proposed (ADR, not yet decided) : ADR-0040 P3. Enforced (gate) : the shared_error_imports ratchet in arch_fitness.py ( SHARED_ERROR , limited to crates/adapters/ and crates/providers/ ) counts use delonix_runtime_core::{…Error/Result…} or use delonix_model::{…} imports that make the shared type the crate's own result type. The shared type may still be named inside a From impl. The reference implementation is crates/adapters/delonix-scanner/src/error.rs : impl From<Error> for Dx { fn from(e: Error) -> Self { match e { e @ (Error::EmptySbom | Error::OsvShape | /* … */ Error::NoModule) => Dx::Invalid(e.to_string()), Error::ModuleScan(io) => Dx::Runtime { context: \"module scan\", message: io.to_string() }, Error::Engine(e) => e, } } } Three things in that file are the pattern: the conversion decides the class; code() asks the conversion instead of keeping a second table; and a test ( the_code_is_the_code_of_the_class_it_converts_into ) keeps the two in step. The converted message is also kept byte-for-byte identical to what the CLI printed before ( the_converted_message_is_the_one_printed_before ). - Ask the error its class; don't match on a variant of the shared error. Outside the foundation, write e.is_not_found() or e.class() , or match on e.root() when you need the payload — never Err(Error::NotFound(_)) or matches!(…, Error::NotFound(_)) . "},{"p":"12. Coding conventions","h":"5.3 Visibility","u":"12-coding-conventions.html#53-visibility","t":"Default to private. Inside the CLI crate, use pub(crate) for something another cmd module needs to call. Convention (observed) : cmd/container.rs:cmd_run , cmd_start and cmd_stop are pub(crate) so that pod , compose and stack can delegate to them; cmd/firewall.rs:update_locked ; cmd/manifest.rs:KIND_ALIASES . pub in a library crate is a promise to other crates. Removing a public item is a breaking change for library users, even with zero callers in this workspace. rustc's dead_code lint doesn't see unused pub items, so when you delete one, count the orphaned public items by hand. Decided : AGENTS.md § \" delonix_sdn::Net foi APAGADO — e é breaking para quem usa a biblioteca\"."},{"p":"12. Coding conventions","h":"5.4 The node contract","u":"12-coding-conventions.html#54-the-node-contract","t":"proto/delonix/node/v1 is the source of truth for both the gRPC and the HTTP/JSON encodings. docs/api/openapi.yaml is generated from it. Never edit the OpenAPI file by hand. Enforced (gate) : scripts/contract_gate.py runs buf format , buf lint , buf breaking against the last tag that contains proto/ , checks the HTTP mapping of every RPC, and checks that the OpenAPI file equals the generated one. Decided : AGENTS.md § \"O contrato de nó é um portão\" (which cites ADR-0040 P1; ADR-0040 D4 is still Proposed ). One request message per RPC , named <Rpc>Request . Enforced (gate) : buf lint RPC_REQUEST_STANDARD_NAME (see buf.yaml ). A shared request lets a field meant for one method show up in five. Explicit identity in the request : name and namespace fields, never a generic metadata message with fields the engine would ignore. Decided : AGENTS.md. Convention (observed) : compute.proto:GetContainerRequest { string name = 1; string namespace = 2; } . Images are addressed by query, not in the path , because in a path like alpine:3.20 the : would be read as a custom verb. Decided : AGENTS.md. Convention (observed) : infra.proto GetImage → get: \"/v1/images:get\" , with GetImageRequest { string reference = 1; } . Responses return the resource, or an Operation for long-running mutations. buf lint 's response-naming rules are switched off on purpose ( buf.yaml comment). Every RPC has an HTTP mapping except the bidirectional streams ( Exec , Console ), which must not have one. Enforced (gate)"},{"p":"12. Coding conventions","h":"5.5 The engine knows no consumer","u":"12-coding-conventions.html#55-the-engine-knows-no-consumer","t":"The engine doesn't know who uses it. No name of a platform, control plane, console or agent, and no notion of tenant, account, plan or billing, may appear in crates/ , bins/ , proto/ , the root Cargo.toml or the Makefile , comments included . Consumer names. Enforced (gate) : scripts/arch_fitness.py consumer_mentions() matches the CONSUMER_NAMES regular expression, a fixed list of names, in those paths. A name that is not on the list is not caught. Tenant, account, plan and billing concepts. Decided : AGENTS.md § \"Identidade e fronteira do motor\". No gate matches them; review checks it. If a consumer needs something, write it as a generic capability in the engine's own vocabulary (Kinds and resources), and add it only if it makes sense for any client. The engine validates its own contract and never trusts a caller to refuse what it doesn't support."},{"p":"12. Coding conventions","h":"6. Error handling and messages","u":"12-coding-conventions.html#6-error-handling-and-messages","t":"No silent failure. If an option is accepted and then ignored, that is worse than a missing feature, because the user believes it took effect. Refuse it with a clear error, and name the flag. Decided : AGENTS.md § \"Falhas silenciosas corrigidas (fail-closed)\"; the v0.37.0 audit (§ \"Auditoria sistemática dos 208 subcomandos\") calls this class \"relato desonesto\" (dishonest reporting). The patterns that section lists are: Don't destroy anything before you know the object is yours to destroy, and delete the bookkeeping last. If the record is removed first and the data removal then fails, the data is orphaned and a later create hands it to someone else. An unreadable measurement is unknown , never zero. A read_dir that fails is not an empty directory. That is why Usage { bytes, unreadable } exists. Watch the patterns that turn failures into success : let _ = on a result that matters (entropy, a socket read), as u64 on an f64 (it saturates), capture() read by its Result instead of its output. AGENTS.md § \"A classe «X não é Y»\" catalogues these. Unknown or unmeasurable is not a guess. When the engine can't read a value, it reports that it doesn't know, or it refuses. It doesn't pick the likelier answer. Example: system prune --auto refuses if disk usage can't be read, and the IPAM reaper fails closed when a store is unreadable (an empty list would read as \"nothing is alive\"). Decided : AGENTS.md § CLI ( system prune ), § \"O IPAM vaza\". Message shape: the fact first, then what to do ,"},{"p":"12. Coding conventions","h":"7. unsafe, syscalls and processes","u":"12-coding-conventions.html#7-unsafe-syscalls-and-processes","t":"Every unsafe block has a // SAFETY: comment directly above it. Enforced (gate) : undocumented_unsafe_blocks = \"deny\" in [workspace.lints.clippy] . // crates/adapters/delonix-linux/src/lib.rs — apply_filter_logged // SAFETY: `fprog` points to a valid BPF program; NO_NEW_PRIVS is already set. let rc = unsafe { libc::syscall(libc::SYS_seccomp, SET_MODE_FILTER, FLAG_LOG, &fprog as *const _) }; The comment has to state the invariant that makes the call sound. \"same\" is acceptable only directly after an identical, justified call (as in delonix-linux/src/lib.rs right after the first _exit(126) ). - No raw clone() / fork() in a multi-threaded process (the tokio servers, the Docker API shim). clone doesn't run pthread_atfork handlers, so the child can deadlock on the malloc lock. Re-execute a typed spec instead, handed over through a 0600 / O_EXCL file rather than argv. Decided : AGENTS.md § \"Auditoria de segurança #3\", item 5. Background: 03 . - A pre_exec hook must not block on something the parent does after spawn returns. Command::spawn only returns after exec , so the two processes wait for each other forever. Use a raw fork for handshakes. Decided : AGENTS.md § \"A classe «X não é Y»\" (the reexec_mapped_hold entry). - Temporary files: use delonix_runtime_core::write_private_temp . It opens with a unique name, O_EXCL and mode 0600 , so it never follows a planted symlink. Don't use a fixed or pid-derived name in /tmp . Decided : AGENTS.md § \"Auditoria de segurança #3\", passagem 2; "},{"p":"12. Coding conventions","h":"8. State and concurrency","u":"12-coding-conventions.html#8-state-and-concurrency","t":"Read–modify–write goes through update , never load → mutate → save . Store::update and JsonStore::update ( crates/foundation/delonix-runtime-core/src/store.rs ) take a flock , re-read under the lock , apply your closure and write atomically. A closure that returns false aborts the write. The CLI, the CRI server and background refreshes all touch the same records concurrently, and without the lock one write is silently lost. Decided : doc comments of both functions; AGENTS.md § \"Revisão ampla de código/arquitectura (2026-07-27)\", bugs 5 and the JsonStore item. Convention (observed) : a mutation that can itself fail is wrapped like this: // bins/delonix-runtime-bin/src/cmd/firewall.rs — update_locked let c = store.update(id_or_name, |c| match f(c) { Ok(commit) => commit, Err(e) => { err = Some(e); false } })?; Persist each step as soon as the dataplane confirms it. If a multi-step change fails halfway, the record must still match what the kernel actually has. Decided : AGENTS.md § \"Reconfiguração a quente\" (\"Persistência\"). New fields on persisted records take #[serde(default)] (or default = \"fn\" ), so records written by older versions still load. The default must describe what old records actually were, not a guess. Decided : AGENTS.md (for example Vm.namespace , VmImage.cloud_init ). Convention (observed) : delonix-runtime-core/src/lib.rs , the doc comment on Vm.namespace (\"the default is a statement of fact and not a guess\"). Anything needed to rebuild a resource must be per"},{"p":"12. Coding conventions","h":"9. Tests","u":"12-coding-conventions.html#9-tests","t":"Where they go. Unit tests go in #[cfg(test)] mod tests at the bottom of the file. Integration tests go in crates/<layer>/<crate>/tests/ and use only the public API. Live provider tests are opt-in. Convention (observed) ; details in 03 § 3.9 . Pure first. Put the decision in a pure function and test it as data. Anything that needs real namespaces, cgroups or a network holder is validated live or with scripts/e2e.sh . Decided : CONTRIBUTING.md (\"Write a unit test for any new pure function\"); AGENTS.md § \"IaC nativo\" ( reconcile.rs is pure so it can be tested as data). Names : English sentences that state the behaviour (see §3.4 ). Tests never touch the host's real state. Give stores a temporary root. Don't call code that resolves the real state root. Don't set_var (the env_writes ratchet, §4.1 ). Decided : AGENTS.md § \"IaC nativo\", the ShareVolume fusion note (\"Nota de método: um teste que chamasse apply_share … escreveria no estado REAL da máquina\"). Convention (observed) : delonix-runtime-core/src/store.rs tests use a tmp_dir(tag) helper. For manual and E2E runs, isolate both DELONIX_ROOT and DELONIX_NET_RUNTIME_DIR . Isolating only one is worse than none (AGENTS.md § \"Meia-isolação é pior que nenhuma\"; 02 ). A regression test must fail with the fix reverted. Revert the fix, watch the test fail, then restore the fix. A test that passes either way proves nothing, and AGENTS.md records several (an exit-code check that 1 couldn't distinguish; a chaos scenario that stayed green w"},{"p":"12. Coding conventions","h":"10. Comments and documentation","u":"12-coding-conventions.html#10-comments-and-documentation","t":"Comments explain why , not what . Write one for a hidden constraint, a workaround for a specific bug, or an invariant the code doesn't make obvious. Comments that restate the code are removed in review. Decided : CONTRIBUTING.md § Style. When a decision was measured, say what was measured. \"Measured: …\" beats \"should\". The module docs in exitcode.rs and reconcile.rs are the model. Convention (observed) . Doc comments ( /// , //! ) on public items and module headers say what the item promises and why it exists. Convention (observed) : every port in delonix-compute/src/ports.rs , store.rs:write_private_temp , exitcode.rs . Not decided : no missing_docs lint is enabled. Comments are English and name no consumer. The language ratchet and the consumer gate both scan comments. Enforced (gate) . Don't add abstractions, config flags or error handling for cases that can't happen. Decided : CONTRIBUTING.md § Style. When you move a boundary, update its records in the same change : an ADR before the code, for a new provider, port, daemon, external dependency in an engine crate, privilege boundary, or change to the contract or layers. Accepted ADRs are never rewritten; a new one supersedes them ( Decided : docs/adr/README.md ; 10 ); the AGENTS.md section that describes the area. A stale section there misleads the next person, and conflicts in AGENTS.md are resolved by keeping both sides ( Decided : AGENTS.md § \"Método: um worktree por sessão\"); the generated artefacts: docs/schema/v1/delo"},{"p":"12. Coding conventions","h":"Undecided","u":"12-coding-conventions.html#undecided","t":"Nothing in the repository settles these. Follow the surrounding code and mention the choice in your PR: When #[allow(clippy::…)] is acceptable. It is used ( too_many_arguments ) without a written policy. The Result type of today's compute ports. The ports in delonix-compute/src/ports.rs return delonix_runtime_core::Result , so the adapters that implement them ( HostWorkload , HostNetwork , …) import the shared result type, and those imports count towards shared_error_imports . P3 decides errors per crate. No document says how a port's signature changes, and adding such an import in a new file fails the ratchet. If your change needs one, raise it in the PR. Don't work around the regex. Crate names during the transition. ADR-0040 D2 (still Proposed) sets the target names, but a brand-new crate that lands before its context exists (for example a second provider before the delonix-provider-* renames) has no written rule. Ask in the issue first. Required doc comments. There is no missing_docs lint, only the observed habit. The fitness test for provider-name matching (ADR-0040 D3 rule 3) is proposed but not yet implemented."},{"p":"12. Coding conventions","h":"Review checklist","u":"12-coding-conventions.html#review-checklist","t":"Before you open the PR, go through the list: cargo fmt --all --check and cargo clippy --workspace --all-targets --locked -- -D warnings are clean. → §1 python3 scripts/lang_ratchet.py and python3 scripts/arch_fitness.py pass, and any baseline you lowered is in this commit. → §1 New identifiers, comments, messages and test names are English. User text goes through po::t / po::tf , with pt.po entries. No num . → §2 , §3.4 The code is in the right crate and layer, versions are only in the root, the library has no println! and doesn't re-run its own binary. → §4 Decisions are pure functions with unit tests, and I/O stays at the edges. → §4.3 New backends implement a port, with no provider-name matching. Adapters don't depend on adapters. → §5.1 Adapter/provider failures use a crate Error that converts into delonix_model::Error . → §5.2 If you touched proto/ : scripts/contract_gate.py passes, with one request per RPC, explicit identity, and a regenerated OpenAPI. → §5.4 No consumer name anywhere in crates/ , bins/ or proto/ , comments included. → §5.5 Nothing is accepted and then ignored. Errors state the fact and then the fix, and use the variant of the right class. → §6 , §3.8 Every unsafe block has a // SAFETY: comment. There is no raw fork in a multi-threaded process, temp files use write_private_temp , and secrets use write_atomic_mode . → §7 Record mutations go through update , and new record fields have #[serde(default)] . → §8 Tests use isolated roots, the regression test "},{"p":"13. Cloud native standards, layer by layer","h":"13. Cloud native standards, layer by layer","u":"13-cloud-native-standards.html#13-cloud-native-standards-layer-by-layer","t":"A container and microVM engine is not one specification. It is a stack of layers, and most layers have an open standard: some from the OCI (Open Container Initiative), some from Kubernetes SIGs and the CNCF (Cloud Native Computing Foundation), some from the Linux kernel, and a few that are de-facto interfaces with no standards body behind them. This page is organised by standard and by layer . For each one it answers four questions: what the standard is, what it requires of an implementation, how Delonix implements it today (crate, file, symbol), and what the evidence says about conformance, gaps included. If you search for \"OCI, CRI, CNI, CSI, CDI\", this is the page. Read 4. Cloud native primer first if namespaces, cgroups, overlayfs or the kubelet are new to you. This page assumes those basics and does not repeat them. Three rules for reading this page: \"Compatible\" never appears without a number, a date and a version. A conformance claim that nobody re-measures stops being a measurement and becomes a quotation. \"Not implemented\" is a first-class answer. Where the engine does not implement a standard, the section says so and links the decision (ADR) that explains why. Paths are relative to the repository root , and every symbol named here was read in the code. If a symbol has moved when you read this, trust the code and fix the page."},{"p":"13. Cloud native standards, layer by layer","h":"13.0 The layer map","u":"13-cloud-native-standards.html#130-the-layer-map","t":"Each arrow means \"is built on\". The kernel layer is not a CNCF standard; it is covered in 13.15 because every other layer depends on it."},{"p":"13. Cloud native standards, layer by layer","h":"13.1 OCI Runtime Specification","u":"13-cloud-native-standards.html#131-oci-runtime-specification","t":"What the standard is. The OCI Runtime Specification defines a filesystem bundle (a root filesystem plus a config.json ) and the lifecycle of a container created from it ( create , start , kill , delete , state ). It is what runc , crun and youki implement, and what containerd and CRI-O drive. What an implementation must do. Read a bundle: config.json with ociVersion , process , root , mounts , linux (namespaces, capabilities, cgroups, masked and read-only paths, seccomp). Expose the lifecycle operations and the state document, and run the hooks the config declares ( prestart , createRuntime , poststop , …). How Delonix implements it. Delonix is not an OCI runtime binary in the runc sense: it does not read a bundle and does not expose the create/start/state command line. It is its own runtime, and it relates to the specification in two ways: It produces bundles for OCI runtimes. delonix image export <image> <dir> writes <dir>/rootfs and <dir>/config.json so that runc run -b <dir> can run the image. - bins/delonix-runtime-bin/src/cmd/image.rs : cmd_export unpacks the rootfs through ImageStore::export_rootfs ( crates/adapters/delonix-oci/src/overlay.rs ) and builds the config with build_runtime_spec . - build_runtime_spec builds the config from the oci_spec::runtime types (the oci-spec crate, pinned in the root Cargo.toml ) instead of hand-written JSON. Its doc comment lists what an earlier hand-written bundle lacked (standard mounts, effective capabilities, masked/read-only pat"},{"p":"13. Cloud native standards, layer by layer","h":"13.2 OCI Image Specification","u":"13-cloud-native-standards.html#132-oci-image-specification","t":"What the standard is. The OCI Image Specification defines how an image is described: a manifest listing a config blob and ordered layer blobs, an image index for multiple platforms, content-addressed by digest, and the image layout directory format ( oci-layout , index.json , blobs/sha256/… ). What an implementation must do. Address all content by digest and verify it. Understand manifests, indexes (select the right platform) and layer media types (tar, gzip, zstd), and apply layers in order, including whiteouts. Read and write the image layout when exchanging images without a registry. How Delonix implements it. All in crates/adapters/delonix-oci : Content-addressed store : src/cas.rs ( Cas ), with ImageStore in src/image.rs . Layers as overlay lowers : ImageStore::prepare_overlay ( src/overlay.rs ) writes an overlay-lowers file next to the container's merged/ , and the container's own init mounts it. Layers are shared between containers, not copied (see the containers-share-layers notes in AGENTS.md and ADR-0037 for the mount API). Layer media type by magic number : the helper next to DOCKER_MANIFEST_MEDIA_TYPE in src/registry.rs detects gzip, zstd or plain tar. Writing the image layout : write_oci_archive ( src/save.rs ), used by delonix image save . It writes oci-layout , index.json , the blobs, and a legacy manifest.json so one archive reads in ctr images import , podman load , docker load and delonix image load . It sets both org.opencontainers.image.ref.name and io.con"},{"p":"13. Cloud native standards, layer by layer","h":"13.3 OCI Distribution Specification","u":"13-cloud-native-standards.html#133-oci-distribution-specification","t":"What the standard is. The OCI Distribution Specification defines the HTTP API of a registry: GET /v2/<name>/manifests/<reference> , blob fetch and upload, tag listing, and the token authentication flow most registries use. What an implementation must do. Negotiate manifest media types with Accept , follow the 401 → token → retry flow, fetch blobs and verify each against its digest . For a digest reference ( repo@sha256:… ), verify that the manifest itself hashes to that digest. Push blobs (with existence checks) and then the manifest. How Delonix implements it. crates/adapters/delonix-oci/src/registry.rs : parse_reference splits registry/repo:tag@digest (including the combined tag@digest form). pull_from_registry_with_creds / pull_from_registry_with_creds_full pull a multi-layer image; blobs already in the CAS are not downloaded again. verify_manifest_digest : for a digest-pinned pull, the manifest bytes must hash to the pinned digest, or the pull is refused. Without this, a compromised registry could serve a different, internally consistent manifest and the pin would be decorative. For a tag reference it is a no-op; TLS is the only integrity, as with docker pull . blob_with_progress_capped resumes an interrupted blob download with Range: . A 206 at an offset other than the one requested, or a 200 that ignored the range, restarts from zero instead of being stitched. The final digest check is what makes stitching safe. push_to_registry , build_manifest , list_remote_tags . OCI"},{"p":"13. Cloud native standards, layer by layer","h":"13.4 Kubernetes Container Runtime Interface (CRI)","u":"13-cloud-native-standards.html#134-kubernetes-container-runtime-interface-cri","t":"What the standard is. The CRI is the gRPC API ( runtime.v1 ) the kubelet uses to run pods: a RuntimeService (pod sandboxes, containers, exec/attach/port-forward, stats) and an ImageService . The kubelet connects to a runtime through --container-runtime-endpoint . What an implementation must do. Serve both services on a local socket; report RuntimeReady and NetworkReady in Status . Implement the pod sandbox model: shared network namespace, pod-level cgroup parent, container lifecycle inside the sandbox. Return URLs from Exec / Attach / PortForward and serve those streams over the Kubernetes remotecommand protocol (WebSocket or SPDY). Tell the kubelet its cgroup driver ( RuntimeConfig ), honour resource limits, report stats for eviction, and write logs in the CRI log format. How Delonix implements it. crates/interfaces/delonix-cri , binary delonix-cri (also delonix serve cri ): Contract : proto/api.proto declares package runtime.v1 with go_package = \"k8s.io/cri-api/pkg/apis/runtime/v1\" ; Version answers runtime_api_version: \"v1\" ( src/runtime_svc.rs ). Server : serve_blocking in src/lib.rs ; socket from --addr or DELONIX_CRI_ADDR , default unix:///run/delonix-cri.sock ( src/bin/delonix-cri.rs ). Lifecycle : src/runtime_svc.rs (the gRPC trait) delegates to src/runtime_svc/lifecycle.rs . Cgroup driver : runtime_config answers engine_cgroup_driver() , which is Cgroupfs . Its doc comment records the measurement that forced this (2026-09-15, k8s 1.36.4): answering nothing made the k"},{"p":"13. Cloud native standards, layer by layer","h":"13.5 Container Network Interface (CNI)","u":"13-cloud-native-standards.html#135-container-network-interface-cni","t":"What the standard is. The CNI specification defines how a runtime asks plugin binaries to configure a network namespace: a network configuration list ( /etc/cni/net.d/*.conflist ), plugin binaries in CNI_PATH (usually /opt/cni/bin ), and operations passed in CNI_COMMAND with the configuration on stdin. What an implementation must do. Load the configuration list, resolve each plugin in CNI_PATH , and set CNI_COMMAND , CNI_CONTAINERID , CNI_NETNS , CNI_IFNAME , CNI_PATH . For ADD , run plugins in order, passing each one the previous result as prevResult ; for DEL , run them in reverse. Parse results and structured errors; pass runtimeConfig (for example portMappings ) to the plugins that declare the capability. Newer spec versions add CHECK , GC and STATUS . How Delonix implements it. Two network providers exist, and CNI is one of them: Native SDN (the default for containers): the rootless holder netns, bridge, IPAM, nftables firewall — crates/adapters/delonix-sdn (see 4.5 and 5. Architecture ). CNI protocol layer : crates/adapters/delonix-sdn/src/cni.rs , pure and testable. list_conf_files , parse_config , load_default , resolve_plugin , add (chains prevResult ), del , plugin_dirs (from CNI_PATH ), readiness (config parses and every binary, ipam.type included, is in CNI_PATH ), attach_named_netns , detach_named_netns , set_netns_sysctls . Where CNI is used : CRI, root mode : the pod network is always the node's CNI chain, in the host, as in containerd. run_pod_sandbox ( crates"},{"p":"13. Cloud native standards, layer by layer","h":"13.6 Container Storage Interface (CSI)","u":"13-cloud-native-standards.html#136-container-storage-interface-csi","t":"What the standard is. The CSI specification is a gRPC API between an orchestrator and a storage driver: an Identity service, a Controller service (create/delete/publish volumes) and a Node service (stage/publish into a pod's mount namespace), registered with the kubelet. What an implementation must do. Run a Node plugin reachable on every node for the lifetime of the volumes it serves (usually a DaemonSet with registrar sidecars), and usually a Controller plugin as a long-running service. How Delonix implements it. It does not implement CSI. Storage is served by the engine's own kind: Volume : crates/adapters/delonix-volume/src/lib.rs : named volumes ( <root>/volumes/<name>/_data ) and bind mounts, both MS_BIND , Docker -v syntax. HostVolumes implements the compute context's StorageProvider port ( crates/contexts/delonix-compute/src/ports.rs ). Network storage (NFS, CIFS/SMB, WebDAV) mounted as a volume, and a storage provisioner against a NAS API ( ADR-0009 , crate crates/providers/delonix-truenas ). Conformance status / gaps. Not implemented, by decision: ADR-0034 (Proposed). A CSI Node plugin is a standing service, and the engine is daemonless by design. The ADR reopens only when a concrete need names the CSI protocol specifically and the daemon question has its own accepted ADR. It also records the practical path that needs no code here (an external NFS provisioner against the same server). Where to start reading. ADR-0034 → crates/adapters/delonix-volume/src/lib.rs → Sto"},{"p":"13. Cloud native standards, layer by layer","h":"13.7 Container Device Interface (CDI)","u":"13-cloud-native-standards.html#137-container-device-interface-cdi","t":"What the standard is. The Container Device Interface describes devices (GPUs, for example) as JSON or YAML specs in /etc/cdi and /var/run/cdi . A fully-qualified name such as nvidia.com/gpu=all resolves to container edits : device nodes, mounts, environment variables and hooks. What an implementation must do. Load specs from both directories with the defined precedence, resolve qualified names, and apply both the device-independent top-level containerEdits and the per-device ones, including hooks. How Delonix implements it. As a consumer of specs generated by a vendor tool ( nvidia-ctk cdi generate ), never as a driver-discovery tool: crates/adapters/delonix-linux/src/cdi.rs : is_cdi_qualified , ensure_cdi_available , resolve_cdi_device , expand_gpu_devices ; spec directories /etc/cdi then /var/run/cdi . HostDevices implements the DeviceResolver port ( crates/contexts/delonix-compute/src/ports.rs ), turning edits into the same mounts, device list and environment that -v and --device produce. The container's own init applies them before pivot_root ; no second process enters the container by PID. CLI surface: container run --gpus nvidia|all and --device nvidia.com/gpu=<name|all> . Without a spec or nvidia-ctk , the run is refused before anything is created. Conformance status / gaps. Hooks are not executed. A best-effort ldconfig -r <rootfs> replaces the usual createContainer hook, and a spec that declares hooks produces a visible warning. This is the one named cost of ADR-0033"},{"p":"13. Cloud native standards, layer by layer","h":"13.8 The workload API: own Kinds and the node contract","u":"13-cloud-native-standards.html#138-the-workload-api-own-kinds-and-the-node-contract","t":"What the standard is. There is no external standard here, on purpose. The engine exposes its own declarative Kinds in its own API groups ( core , compute , networking , gateway , storage , artifact , infrastructure ; list them with delonix api-resources ). The shape follows Kubernetes conventions ( apiVersion , kind , metadata , spec ) without claiming Kubernetes API compatibility. What an implementation must do (the engine's own rules): Publish the manifest schema generated from the code, not written by hand ( ADR-0007 ). Plan, apply and detect drift with a three-way diff, and never ignore a field silently. Serve one node contract over gRPC and HTTP/JSON on the same local socket, with the OpenAPI document generated from the protobuf ( ADR-0040 , Proposed; ADR-0041 ). How Delonix implements it. Kind table: crates/contexts/delonix-stack/src/kinds.rs (each Kind's api_version , domain, convergence, teardown). Reconciler: src/reconcile.rs . Published schema: docs/schema/v1/delonix.json . Node contract: proto/delonix/node/v1/*.proto , with docs/api/openapi.yaml generated and checked by scripts/contract_gate.py . Conformance status / gaps. Gated by CI (the schema test, contract_gate.py ), not by an external suite. See 5. Architecture and 7. System design . Where to start reading. crates/contexts/delonix-stack/src/kinds.rs → src/reconcile.rs → proto/delonix/node/v1/node.proto → scripts/contract_gate.py ."},{"p":"13. Cloud native standards, layer by layer","h":"13.9 Docker Engine API subset — a de-facto interface","u":"13-cloud-native-standards.html#139-docker-engine-api-subset-a-de-facto-interface","t":"What the standard is. The Docker Engine API is not a CNCF or OCI standard . It is one vendor's REST API that many tools speak ( docker CLI, compose, kind, test frameworks). It is included here because it is a real interoperability surface. What an implementation must do. Answer /_ping and version negotiation, then the routes a given tool calls, with Docker's JSON shapes and status codes. How Delonix implements it. bins/delonix-runtime-bin/src/cmd/dockerapi.rs , served by delonix serve docker-api [--addr unix://<socket>] : API_VERSION is \"1.43\" , MIN_API_VERSION is \"1.24\" ; version prefixes are stripped so /v<version>/... works. The coverage is a published table : API_MATRIX (served routes) and API_UNIMPLEMENTED (refused routes with a reason). delonix serve docker-api --matrix prints them, and a test fails if a dispatch arm exists without a matrix entry. Container lifecycle routes delegate to the same functions as the CLI. The socket is 0600 with SO_PEERCRED (same uid only). Conformance status / gaps. The module comment says it was checked against a real docker CLI 27.3.1 (undated). Refused today, with reasons in API_UNIMPLEMENTED : exec and attach (need HTTP hijacking), logs , events , build , networks, images/create (the pull most tools call first), images/{name}/json , stats , volumes. Read the table rather than trusting this list; the table is the contract. Docker's network model ( NetworkingConfig ) is not translated. Where to start reading. API_MATRIX and API_UNIMPLEMENT"},{"p":"13. Cloud native standards, layer by layer","h":"13.10 Compose Specification — a de-facto spec","u":"13-cloud-native-standards.html#1310-compose-specification-a-de-facto-spec","t":"What the standard is. The Compose Specification describes a multi-container application in YAML ( services , networks , volumes , secrets , configs ). It is an open specification maintained by the Compose project, not a CNCF or OCI standard . What an implementation must do. Parse the model, order services by depends_on (with its conditions), create networks and volumes, and not silently change the meaning of a file. How Delonix implements it. bins/delonix-runtime-bin/src/cmd/compose.rs ( delonix compose up|down|ps|logs|config ): a translator into RunOpts and the engine's own Image / Network / Volume documents, with membership derived from labels. Conformance status / gaps. Unknown keys are refused, not ignored : check_unsupported_fields checks the raw YAML against allowlists ( SUPPORTED_TOP , SUPPORTED_SERVICE , …) and denylists with reasons ( KNOWN_UNSUPPORTED_TOP , KNOWN_UNSUPPORTED_SERVICE ). Older notes elsewhere in the workspace say unknown keys were swallowed silently; the code no longer does that. include: is refused (use -f a.yml -f b.yml ). No Compose conformance suite has been run. Where to start reading. The module comment and check_unsupported_fields in compose.rs ."},{"p":"13. Cloud native standards, layer by layer","h":"13.11 OpenTelemetry","u":"13-cloud-native-standards.html#1311-opentelemetry","t":"What the standard is. OpenTelemetry is the CNCF project for traces, metrics and logs, with the OTLP wire protocol for exporting them to a collector. What an implementation must do. Emit spans with resource attributes (at least service.name ), export them over OTLP (gRPC or HTTP), and flush before the process exits. How Delonix implements it. crates/adapters/delonix-telemetry/src/telemetry.rs : Structured logs through tracing ( DELONIX_LOG , DELONIX_LOG_FORMAT=json ). Traces over OTLP/HTTP protobuf when DELONIX_OTLP_ENDPOINT is set ( build_otlp_layer , /v1/traces appended if missing), with a batch span processor on its own thread, so it works both in the async CRI server and in the synchronous CLI. service.name distinguishes the binaries. Conformance status / gaps (all stated in the module comment): Traces only. Metrics go through Prometheus exposition (13.12), not OTLP; there is no OTLP log export. Plain HTTP only. No TLS backend is compiled in, as a supply-chain decision; an https:// endpoint is not supported. The short-lived CLI does not flush on exit, so a fast invocation can lose its spans. The reliable path is the long-running delonix-cri . Where to start reading. Module comment, init and build_otlp_layer in crates/adapters/delonix-telemetry/src/telemetry.rs ."},{"p":"13. Cloud native standards, layer by layer","h":"13.12 Prometheus exposition format","u":"13-cloud-native-standards.html#1312-prometheus-exposition-format","t":"What the standard is. The Prometheus exposition format and its successor OpenMetrics define the text a scrape target serves on GET /metrics . What an implementation must do. Serve the text with the correct Content-Type , with stable metric names and types, fast enough for the scrape timeout. How Delonix implements it. One shared registry: crates/adapters/delonix-telemetry/src/metrics.rs ( encode , and setters such as set_containers , set_vms , set_memory , set_network , set_storage ), built on the prometheus-client crate. delonix-cri : an optional /metrics listener enabled by DELONIX_METRICS_ADDR ( src/lib.rs , metrics_handler ), separate from the gRPC socket. delonix-mgmt : /metrics on its local socket ( crates/interfaces/delonix-mgmt/src/lib.rs , metrics ). Cheap fields are computed per scrape; expensive ones (disk walk) are refreshed in the background so scrapes stay fast. Both handlers serve application/openmetrics-text; version=1.0.0; charset=utf-8 . Conformance status / gaps. No promtool check metrics run is recorded. The expensive gauges can be up to one refresh interval stale; the notes in AGENTS.md explain why that trade was chosen. Where to start reading. crates/adapters/delonix-telemetry/src/metrics.rs → metrics_handler in crates/interfaces/delonix-cri/src/lib.rs → metrics and src/dashstats.rs in delonix-mgmt ."},{"p":"13. Cloud native standards, layer by layer","h":"13.13 Supply chain: SBOM (SPDX), signatures, SLSA provenance","u":"13-cloud-native-standards.html#1313-supply-chain-sbom-spdx-signatures-slsa-provenance","t":"What the standards are. SPDX (ISO/IEC 5962) is a format for a software bill of materials. Sigstore cosign signs container images and stores the signature as an OCI artifact tagged sha256-<digest>.sig . SLSA defines build provenance: an attestation that says which source and which builder produced an artifact. What an implementation must do. Publish an SBOM whose packages, versions and checksums match the artifact; sign so that verification fails on tampering; bind provenance to the exact published files. How Delonix implements it. SBOM of the release binaries : scripts/sbom.py writes SPDX 2.3 from Cargo.lock . The release.yml step \"SBOM (SPDX 2.3, do Cargo.lock)\" writes delonix-sbom.spdx.json and adds it to SHA256SUMS , so the SBOM is covered by the signature. Release signature : release.yml signs SHA256SUMS with minisign and verifies it with the public key embedded in scripts/install.sh ( MINISIGN_PUBKEY ). If the key is configured and the secret is missing, the release fails instead of shipping unsigned. SLSA provenance : release.yml step \"Proveniência (SLSA) dos binários\" uses actions/attest-build-provenance on the published delonix , delonix-cri and delonix-mcp binaries. Verify with gh attestation verify <file> --repo angolardevops/delonix-runtime . Image signatures, cosign-compatible : crates/adapters/delonix-oci/src/sign.rs . sign_image (behind delonix image sign ) publishes an ECDSA P-256 simple-signing payload as the .sig artifact; verify_signature (behind image pull "},{"p":"13. Cloud native standards, layer by layer","h":"13.14 Virtual machines: KVM, virtio, UEFI, cloud-init NoCloud","u":"13-cloud-native-standards.html#1314-virtual-machines-kvm-virtio-uefi-cloud-init-nocloud","t":"What the standards are. KVM is the Linux kernel hypervisor interface ( /dev/kvm ). virtio (OASIS) is the paravirtual device standard: disk, network, 9p/fs and console. UEFI is the firmware interface cloud images boot through. cloud-init NoCloud is the datasource that reads user-data , meta-data and network-config from a local volume labelled cidata . What an implementation must do. Give the guest virtio devices and a firmware it can boot, and hand it a NoCloud seed whose network configuration matches its NIC. How Delonix implements it. crates/adapters/delonix-vm : Backends behind a port : trait VmBackend and register_backend in src/lib.rs . Cloud Hypervisor (Rust VMM on /dev/kvm ) and libvirt/QEMU are local; Proxmox VE is a remote provider ( crates/providers/delonix-proxmox ). See 9. Building microVMs . virtio : libvirt_domain_xml renders the main disk as bus='virtio' ( vda ), NICs as <model type='virtio'/> , and shared volumes as virtio-9p. UEFI firmware for Cloud Hypervisor : DEFAULT_CH_FIRMWARES prefers the EDK2 CLOUDHV.fd over hypervisor-fw . Its doc comment records why: with hypervisor-fw the project's images do not boot; with EDK2 they do (measured 2026-08-12 per AGENTS.md ). libvirt uses a pflash <loader> . cloud-init NoCloud : src/cloudinit.rs . build_user_data , build_network_config (DHCP on the primary NIC matched by MAC , from mac_for , because matching by name breaks NetworkManager guests), and generate_seed_iso , which packages the seed with cloud-localds . Remot"},{"p":"13. Cloud native standards, layer by layer","h":"13.15 Linux: cgroup v2 and systemd delegation","u":"13-cloud-native-standards.html#1315-linux-cgroup-v2-and-systemd-delegation","t":"What the standard is. Not CNCF: cgroup v2 is the kernel's resource control interface, and systemd's delegation contract defines who may write which part of the tree. Every container runtime depends on both. What an implementation must do. Write limits only in a subtree delegated to it, respect the \"no internal processes\" rule, and never assume that a controller listed at the root is available to the calling session. How Delonix implements it. crates/adapters/delonix-linux/src/lib.rs : Root mode: leaves under delonix.slice ( DELONIX_SLICE in delonix-runtime-core ). Rootless: user_service_base and try_delegated_base place containers under user@<uid>.service/dlx-containers . cgroup_limits_apply answers \"will limits apply here?\" without starting a container. Rootless, it probes the process's current cgroup ( delegated_base_usable ), not the host's root cgroup; root, it probes delonix.slice and creates it if missing ( root_slice_writable ). Under the kubelet, transient_scope_argv builds the StartTransientUnit call for a delegated scope under a pod slice (ADR-0038). Conformance status / gaps. A plain SSH session scope is not delegated; the remedy is systemd-run --user --scope -p Delegate=yes (measured 2026-08-04 per AGENTS.md ). Without delegation container run refuses -m / --cpus / --cpu-weight with exit 69 ( preflight_resource_limits in bins/delonix-runtime-bin/src/cmd/container.rs ; DELONIX_ALLOW_UNENFORCED_LIMITS=1 runs unenforced, with a warning). That probe covers only the me"},{"p":"13. Cloud native standards, layer by layer","h":"13.16 Model Context Protocol (MCP) — not a CNCF standard","u":"13-cloud-native-standards.html#1316-model-context-protocol-mcp-not-a-cncf-standard","t":"What the standard is. The Model Context Protocol is a JSON-RPC protocol through which an AI client calls tools and reads resources exposed by a server. It is not a CNCF, OCI or Kubernetes standard ; it appears here because it is one of the engine's interfaces. What an implementation must do. Serve JSON-RPC over a transport (stdio or HTTP), declare server capabilities, and describe tool inputs with JSON Schema. How Delonix implements it. crates/interfaces/delonix-mcp (binary delonix-mcp , also delonix mcp serve ), built on the rmcp crate: stdio only : a foreground child of the AI client, which exits when stdin closes. It is not a daemon ( ADR-0025 ). Tool inputs are typed and schema-validated; outputs are JSON text. Tools carry a risk class ( src/risk.rs ) and calls are audited ( src/audit.rs ). The only principal is the local uid, the same boundary as delonix-mgmt . Conformance status / gaps. No HTTP transport and no structured tool output, by decision (see the module comment). No MCP conformance tooling has been run. Where to start reading. Module comment in crates/interfaces/delonix-mcp/src/lib.rs → src/risk.rs → src/audit.rs ."},{"p":"13. Cloud native standards, layer by layer","h":"13.17 Summary table","u":"13-cloud-native-standards.html#1317-summary-table","t":"Status is implemented (the engine meets the core of the contract), partial (a documented subset, or a documented deviation) or not implemented . The evidence column is where you check the claim, not a promise. Standard Delonix component Status Evidence OCI Runtime Spec delonix-linux (native mechanisms); image export bundle via build_runtime_spec partial — produces bundles, is not a bundle-consuming runtime; no hooks 13.1, ADR-0033 OCI Image Spec delonix-oci ( cas , overlay , write_oci_archive ) partial — reads OCI and Docker manifests; writes Docker schema 2 manifests 13.2, src/registry.rs OCI Distribution Spec delonix-oci::registry ( verify_manifest_digest , resumable blobs, artifacts) implemented (client) — no pagination of tag lists; no conformance run 13.3 CRI ( runtime.v1 ) delonix-cri partial — critest v1.36.0: 79/103 passed, engine v0.63.1, 2026-08-25; kubelet 1.36.4 validated 2026-09-15 cri-conformance.md , ADR-0038 CNI delonix-sdn::cni ; CRI root mode, rootless opt-in DELONIX_CNI=1 partial — ADD / DEL ; no CHECK / GC / STATUS calls, no runtimeConfig 13.5 CSI none ( kind: Volume , delonix-volume , delonix-truenas ) not implemented — needs a daemon ADR-0034 CDI delonix-linux::cdi ( HostDevices ) partial — consumer; hooks not executed 13.7 Engine Kinds + node contract delonix-stack , proto/delonix/node/v1 implemented (own API) — CI-gated ADR-0040 , scripts/contract_gate.py Docker Engine API (de-facto) cmd/dockerapi.rs partial — published API_MATRIX / API_UNIMPLEMENTED d"},{"p":"14. Glossary","h":"14. Glossary","u":"14-glossary.html#14-glossary","t":"The words a newcomer meets in this repository, with the meaning they have in Delonix — which is sometimes narrower than the general cloud-native meaning. Each entry points to where the term is explained or implemented. Paths are relative to the repository root; file.rs::symbol names a symbol inside that file. Terms are in alphabetical order. For the general background (namespaces, cgroups, OCI, CRI, CNI, KVM), start with 04 — Cloud native primer . Adapter — A crate in crates/adapters/ that implements a port against a local mechanism: the kernel ( delonix-linux ), the network dataplane ( delonix-sdn ), the OCI store ( delonix-oci ), the local VM hypervisors ( delonix-vm ). An adapter may depend on foundation and context crates, never on an interface crate. See: 05 — Layers , scripts/arch_fitness.py::LAYERS . ADR (Architecture Decision Record) — One Markdown file per structural decision, in docs/adr/ , named NNNN-title.md , written in English, before the code. An accepted ADR is never rewritten; a new ADR supersedes it. See: docs/adr/README.md , 10 — When to write an ADR . Apply / plan / prune — The three verbs of declarative convergence. delonix plan shows what an apply would change and changes nothing ( --detailed-exitcode exits 2 when there are changes); delonix apply converges the manifest; --prune also removes what the stack owns and the manifest no longer declares, and never runs by default. See: crates/contexts/delonix-stack/src/reconcile.rs::plan , 04 — Declarative reco"},{"p":"15. Environment variables (DELONIX_*)","h":"15. Environment variables (DELONIX_*)","u":"15-environment-variables.html#15-environment-variables-delonix_","t":"This page lists every DELONIX_* name that appears in the engine's code, with where it is read, what it changes, and whether you should ever set it. It is a reference: read the section that matches what you are doing, not the whole page."},{"p":"15. Environment variables (DELONIX_*)","h":"How to read this page","u":"15-environment-variables.html#how-to-read-this-page","t":"Each row of the variable tables has five columns: Read by — the crate or binary and the path:symbol where the value is read (or written). Paths are relative to the repository root. Purpose — what the variable changes. Values / default — how the code parses it. When the code falls back silently on a value it cannot parse, the row says so. Notes — when to use it, and any warning. Most variables are read by the process that needs them, not passed on automatically. A few are set by the engine itself on the processes it starts; those are in Set by the engine itself and you should not set them. This table is checked by CI in both directions. python3 scripts/dev_docs.py --check (function env_var_problems in scripts/dev_docs.py ) extracts every DELONIX_* name from Rust string literals under crates/ and bins/ (including build.rs and env!() ) and from scripts/install.sh , and fails when a name is missing here or when this page lists a name the code no longer contains. A row counts only when its line starts with | `DELONIX_NAME` . The extraction is textual, so it also picks up a few names that are not environment variables (a Rust constant, test fixtures, a key written into a VM image); those are listed in their own sections so the check stays exact. Variables used only by scripts outside that scope — for example DELONIX_CHAOS_DIR and the DELONIX_CHAOS_TRUENAS_* family in scripts/chaos.sh — are documented in the header of each script and in 02 , not here."},{"p":"15. Environment variables (DELONIX_*)","h":"Precedence","u":"15-environment-variables.html#precedence","t":"Where the code has a precedence rule it is usually flag > environment > default ; the table shows each case as the code resolves it, including the ones with an extra level or no flag: Setting Order (first wins) Where CRI socket, capability ceiling and mode --addr / --cap-ceiling / --cap-ceiling-mode > DELONIX_CRI_ADDR / DELONIX_CRI_CAP_CEILING / DELONIX_CRI_CAP_CEILING_MODE > unix:///run/delonix-cri.sock / no ceiling / reject crates/interfaces/delonix-cri/src/bin/delonix-cri.rs:main Management API socket --addr > DELONIX_API_ADDR > unix:///run/delonix-mgmt.sock bins/delonix-mgmt-bin/src/main.rs:run Docker API socket --addr > DELONIX_DOCKER_ADDR > unix:///run/delonix-docker.sock bins/delonix-runtime-bin/src/cmd/dockerapi.rs:run Output language --l18n > DELONIX_L18N > English bins/delonix-runtime-bin/src/cmd/po.rs:peek_lang VM console escape key --escape > DELONIX_CONSOLE_ESCAPE > ^] bins/delonix-runtime-bin/src/cmd/vm.rs:resolve_escape VM backend --backend (or the image's HYPERVISOR ) > DELONIX_VM_BACKEND > vm default-backend --set > auto-detection crates/adapters/delonix-vm/src/lib.rs:standing_backend_choice Published port bind address the address in -p <ip>:<host>:<container> > DELONIX_PUBLISH_ADDR > 127.0.0.1 crates/adapters/delonix-sdn/src/lib.rs:publish_bind_addr CVE feed for image scan --update --feed > DELONIX_ADVISORY_FEED > error bins/delonix-runtime-bin/src/cmd/scan.rs:cmd_scan_update Log filter DELONIX_LOG > RUST_LOG > info crates/adapters/delonix-telemetry/src/tele"},{"p":"15. Environment variables (DELONIX_*)","h":"Isolating a development run","u":"15-environment-variables.html#isolating-a-development-run","t":"Before running anything beyond --help on a machine that also runs Delonix workloads, point both state locations at a scratch directory: export DELONIX_ROOT=$HOME/scratch/dlx/root # records, images, networks, IPAM, volumes, pidfiles export DELONIX_NET_RUNTIME_DIR=/tmp/dlx-run # the network holder's control and slirp sockets mkdir -p \"$DELONIX_ROOT\" \"$DELONIX_NET_RUNTIME_DIR\" ./target/debug/delonix system info # \"state root\" must show your scratch path Why both. The network infrastructure keeps its pidfiles under DELONIX_ROOT but its unix sockets in a separate runtime directory ( crates/adapters/delonix-sdn/src/infra.rs:runtime_dir ), because a socket path is limited to about 108 bytes and DELONIX_ROOT can be arbitrarily deep. Setting only one of the two can leave two state roots sharing one set of sockets. The full story, and how to tear the isolated infra down, is in 02 — Isolating the engine's state ."},{"p":"15. Environment variables (DELONIX_*)","h":"Everyday configuration","u":"15-environment-variables.html#everyday-configuration","t":"Variable Read by Purpose Values / default Notes DELONIX_ROOT every store and binary: bins/delonix-runtime-bin/src/cmd/util.rs:state_root , crates/adapters/delonix-oci/src/image.rs:ImageStore::default_root , crates/foundation/delonix-runtime-core/src/store.rs:Store::default_root , crates/adapters/delonix-sdn/src/infra.rs:base_root , crates/interfaces/delonix-cri/src/bin/delonix-cri.rs:main , bins/delonix-mgmt-bin/src/main.rs:run , crates/interfaces/delonix-mcp/src/lib.rs The engine's state root: container records, images, networks, IPAM, volumes, VMs, secrets. A directory path. Unset: $XDG_DATA_HOME/delonix (or ~/.local/share/delonix ) when not root, /var/lib/delonix as root. delonix-cri and delonix-mgmt default to /var/lib/delonix regardless , which is why delonix serve … passes the CLI's root explicitly ( cmd/serve.rs:exec_server ). The one variable to set when testing. The engine also sets it on every child it starts (re-exec passes, the network holder, servers, CRI lifecycle calls) so paths agree across user namespaces. DELONIX_NET_RUNTIME_DIR crates/adapters/delonix-sdn/src/infra.rs:runtime_dir ( RUNTIME_DIR_ENV ) Directory for the network infrastructure's unix sockets ( control.sock , slirp.sock ). A directory path; keep it short (socket paths over ~108 bytes fail with SUN_LEN ). Unset: /tmp/delonix-net-<uid> plus a suffix derived from a non-default DELONIX_ROOT . Set it together with DELONIX_ROOT when isolating. The engine also passes it to the holder and to --net <cust"},{"p":"15. Environment variables (DELONIX_*)","h":"Networking and security escape hatches","u":"15-environment-variables.html#networking-and-security-escape-hatches","t":"Every variable in this section lowers a boundary. Each one logs a SECURITY WARNING (or a warning) when it takes effect. They exist for debugging and for explicit, informed opt-outs; none of them belongs in a production configuration. Read the linked AGENTS.md section before using one. Variable Read by Purpose Values / default Notes DELONIX_FORWARD_POLICY crates/adapters/delonix-sdn/src/infra.rs (ingress ruleset builder) Reverts the holder netns forward chain from default-deny ( policy drop ) to default-allow. accept → default-allow; anything else → default-deny. Lowers isolation between networks. Logged as a security warning. See AGENTS.md , «Bloco 0 do plano 33 (v0.37.1)» . DELONIX_ALLOW_LINK_LOCAL crates/adapters/delonix-sdn/src/infra.rs ( fwguard chain) Removes the unconditional drop of 169.254.0.0/16 (cloud metadata) and 127.0.0.0/8 (host loopback) for container traffic. 1 → allowed; anything else → dropped. Exposes instance metadata credentials on a cloud host. Security warning. AGENTS.md , «Bloco 0 do plano 33 (v0.37.1)» (RF-NET-02). DELONIX_ALLOW_HOLDER_INGRESS crates/adapters/delonix-sdn/src/infra.rs ( dlxinput chain) Lets containers reach services the holder itself exposes (the L7 proxy, internal DNS) beyond the allowlist. 1 → allowed; anything else → new connections dropped. Through the proxy a container reaches any registered backend, in any namespace, past that backend's own ingress policy. Security warning. Rationale in docs/discovery/46_GAPS_ENCONTRADOS.md §4.2."},{"p":"15. Environment variables (DELONIX_*)","h":"Build and images","u":"15-environment-variables.html#build-and-images","t":"Variable Read by Purpose Values / default Notes DELONIX_INSECURE_REGISTRIES crates/adapters/delonix-oci/src/registry.rs Registries that are contacted over plain HTTP instead of HTTPS. Comma-separated host or host:port entries (case-insensitive). Unset → HTTPS everywhere except loopback registries, which already use HTTP. Traffic and credentials for those hosts travel without TLS. An explicit opt-in per host, never a range. When an HTTPS connection to a registry fails, the error suggests this variable with the host filled in. DELONIX_SCAN_ON_PULL bins/delonix-runtime-bin/src/cmd/scan.rs:admission_scan_on_pull CVE admission policy applied after every image pull. Unset/empty → off. warn → scan and report. low / medium / high / critical → remove the image and refuse when a vulnerability of at least that severity is found. An unknown value refuses the pull. A fail-closed gate: a typo does not turn it off. An image without an SBOM is admitted with a warning. DELONIX_ADVISORIES bins/delonix-runtime-bin/src/cmd/scan.rs:load_advisories Path to an advisory database file used by image scan . A file path. Used only when no synced database exists at <root>/advisories.json ; the synced one wins. Without either, the embedded placeholder is used and the scan says so. DELONIX_ADVISORY_FEED bins/delonix-runtime-bin/src/cmd/scan.rs:cmd_scan_update Source for delonix image scan --update . A URL or file (OSV or native format). --feed wins. Without either, --update fails with an error naming both."},{"p":"15. Environment variables (DELONIX_*)","h":"Cloud Hypervisor and VM resource tuning","u":"15-environment-variables.html#cloud-hypervisor-and-vm-resource-tuning","t":"The libvirt ceilings below are written into the generated domain XML ( crates/adapters/delonix-vm/src/lib.rs:libvirt_domain_xml ); they do not change a domain that already exists until it is booted again. Variable Read by Purpose Values / default Notes DELONIX_HYPERVISOR_FW crates/adapters/delonix-vm/src/lib.rs:default_ch_firmware Firmware Cloud Hypervisor boots when --firmware is not given. A file path, used only if it exists. Otherwise the first existing of DEFAULT_CH_FIRMWARES (EDK2 CLOUDHV.fd before hypervisor-fw ). See 09 for why the EDK2 build comes first. DELONIX_VM_RESERVE_MIB crates/adapters/delonix-vm/src/lib.rs:vm_admission_check Memory kept free for the host when admitting a VM: a VM is refused if its memory plus this reserve exceeds MemAvailable . MiB; default 2048 ; an unparsable value falls back to 2048. Lowering it risks the host OOM-killing things. DELONIX_VM_MEM_HARD_LIMIT crates/adapters/delonix-vm/src/lib.rs:mem_hard_limit_kib Whether libvirt domains get a <memtune><hard_limit> on the whole QEMU process. off → no hard limit; anything else → on. DELONIX_VM_MEM_OVERHEAD_PCT crates/adapters/delonix-vm/src/lib.rs:mem_hard_limit_kib Margin above guest memory allowed by the hard limit. Percent, accepted range 5–200; default 25 ; at least 1 GiB of margin. Out of range → 25. DELONIX_VM_CPU_QUOTA_CORES crates/adapters/delonix-vm/src/lib.rs:cpu_quota_micros CPU ceiling ( <cputune><quota> ) for a libvirt domain, in cores. Unset → vCPUs + 1 (the extra core is for QEMU"},{"p":"15. Environment variables (DELONIX_*)","h":"Container resource budget","u":"15-environment-variables.html#container-resource-budget","t":"These tune the defaults and the aggregate ceiling the engine applies to containers ( crates/adapters/delonix-linux/src/lib.rs ). Variable Read by Purpose Values / default Notes DELONIX_RESERVE_PCT crates/adapters/delonix-linux/src/lib.rs:host_reserve_pct Share of the host (memory and CPU) the engine's cgroup slice may use in total. Percent, accepted range 10–95; default 85 . Out of range → 85. Also the base of the per-workload defaults below. DELONIX_DEFAULT_PCT crates/adapters/delonix-linux/src/lib.rs:default_workload_pct Share of the engine's budget one workload may take when it declares no limits. Percent, accepted range 1–100; default 25 . Out of range → 25. Memory default is at least 64 MiB; CPU default is clamped between 0.25 and 1.0 cores. DELONIX_SWAP_MAX crates/adapters/delonix-linux/src/lib.rs:swap_max_value memory.swap.max of a container's cgroup. A cgroup value; default 0 (no swap). max restores unlimited swap. Swap turns a memory limit into a soft one. DELONIX_IO_MAX_BPS crates/adapters/delonix-linux/src/lib.rs:host_io_max_bps Aggregate disk read/write ceiling ( io.max ) of the engine slice. Bytes/s; default 500000000 (500 MB/s). 0 disables it. An unparsable value → default. A safety ceiling against one container saturating the disk, not fine QoS. Only applies where the io controller is available."},{"p":"15. Environment variables (DELONIX_*)","h":"Providers","u":"15-environment-variables.html#providers","t":""},{"p":"15. Environment variables (DELONIX_*)","h":"Proxmox VE","u":"15-environment-variables.html#proxmox-ve","t":"Read once at CLI start-up by bins/delonix-runtime-bin/src/cmd/vmbackends.rs:register_proxmox . Nothing is contacted until a VM command selects the proxmox backend. Blank values count as unset. A misconfiguration prints a warning and leaves the backend unregistered; it does not stop other commands. Background: docs/adr/0008-proxmox-vm-backend.md . Variable Read by Purpose Values / default Notes DELONIX_PROXMOX_URL cmd/vmbackends.rs:register_proxmox_with ; named in the error of crates/adapters/delonix-vm/src/lib.rs ( KNOWN_UNREGISTERED ) API endpoint of the node. Setting it is what enables the backend. https://<host>:8006 . Requires DELONIX_PROXMOX_NODE and a credential. DELONIX_PROXMOX_NODE cmd/vmbackends.rs:register_proxmox_with The node name to use (as GET /nodes reports it). e.g. pve . No default: the backend never picks a node for you. DELONIX_PROXMOX_SECRET cmd/vmbackends.rs:proxmox_auth Name of a kind: Secret holding the credential. Secret with tokenId + tokenSecret (preferred) or username + password . Checked first . Preferred over the variables below, which end up in shell history and ps . DELONIX_PROXMOX_TOKEN_ID cmd/vmbackends.rs:proxmox_auth API token id. user@realm!tokenname . Used with DELONIX_PROXMOX_TOKEN ; checked after the secret. DELONIX_PROXMOX_TOKEN cmd/vmbackends.rs:proxmox_auth API token secret. DELONIX_PROXMOX_USER cmd/vmbackends.rs:proxmox_auth Account for password authentication. root@pam , … Used with DELONIX_PROXMOX_PASSWORD ; checked last. DELONIX_P"},{"p":"15. Environment variables (DELONIX_*)","h":"TrueNAS","u":"15-environment-variables.html#truenas","t":"The TrueNAS provisioner ( kind: Volume with spec.provision.truenas ) takes its target from the manifest and a kind: Secret , not from environment variables. The only DELONIX_TRUENAS_* names are test settings — see Test-only ."},{"p":"15. Environment variables (DELONIX_*)","h":"Observability","u":"15-environment-variables.html#observability","t":"Variable Read by Purpose Values / default Notes DELONIX_OTLP_ENDPOINT crates/adapters/delonix-telemetry/src/telemetry.rs:build_otlp_layer Exports tracing spans over OTLP/HTTP (protobuf). A base URL such as http://localhost:4318 ; /v1/traces is appended if missing. Unset or blank → no exporter. A failure to build the exporter warns and continues with logs only. The service name is OTEL_SERVICE_NAME or the executable name. DELONIX_METRICS_ADDR crates/interfaces/delonix-cri/src/lib.rs (CRI server start-up) Enables a Prometheus /metrics HTTP listener in delonix-cri . host:port , e.g. 127.0.0.1:9100 . Unset → no listener. A TCP listener: bind it to loopback unless the metrics should be reachable from the network."},{"p":"15. Environment variables (DELONIX_*)","h":"Set by the engine itself / internal","u":"15-environment-variables.html#set-by-the-engine-itself-internal","t":"Do not set these. The engine writes them on processes it starts; setting them by hand makes a normal command behave like an internal pass. Variable Read by Purpose Values / default Notes DELONIX_BIN crates/foundation/delonix-runtime-core/src/dispatch.rs:cli_bin (used by delonix-cri , delonix-mgmt , delonix-mcp ) The delonix executable a server runs back for lifecycle operations. A path. Unset: the delonix next to the server's executable, then delonix on PATH . Set by delonix serve … / delonix mcp ( cmd/serve.rs:exec_server ) to the running CLI. Setting it by hand is legitimate only when you start a server binary directly and want it to call a specific CLI. scripts/cli-tree.sh and scripts/docs_cli_gate.py also read a variable of this name to choose the binary they inspect (see 02 ). DELONIX_DISPATCH_VERSION crates/foundation/delonix-runtime-core/src/dispatch.rs:check_version (in delonix-cri , delonix-mgmt , delonix-mcp ) The version the server must be; a server from another release refuses to start. Set by cmd/serve.rs:exec_server to the CLI's version. A server started directly (for example by a systemd unit) has no expectation and is not checked. DELONIX_REEXEC_ID bins/delonix-runtime-bin/src/cmd/container.rs ( cmd_run , reexec_env ) Marks the second pass of container run/start --net <custom> or --pod , re-executed inside the holder's namespaces, and carries the container id. Set by cmd/container.rs:reexec_env . Its presence skips checks the first pass already did (port owner"},{"p":"15. Environment variables (DELONIX_*)","h":"Build-time","u":"15-environment-variables.html#build-time","t":"Compile-time values: they are fixed when the binary is built and cannot be changed by setting a variable when the program runs. Variable Read by Purpose Values / default Notes DELONIX_GIT_HASH written by bins/delonix-runtime-bin/build.rs ; read with env! in bins/delonix-runtime-bin/src/main.rs Short commit hash shown by delonix --version . git rev-parse --short=9 HEAD , or unknown without git. DELONIX_GIT_SINCE written by bins/delonix-runtime-bin/build.rs ; read in src/main.rs Distance from the newest tag, shown as (+N commits since vX.Y.Z) . <count>\\|<tag> , empty at a tag or without git. This is how you tell two builds with the same version apart. DELONIX_BUILD_DATE written by bins/delonix-runtime-bin/build.rs ; read in src/main.rs and src/cmd/man.rs Build date in --version and in generated man pages. date -u +%Y-%m-%d , or unknown . Man pages use it instead of the clock so two generator runs on one build produce the same output. DELONIX_GIT_COMMIT read with option_env! in bins/delonix-runtime-bin/src/cmd/dockerapi.rs ( /version response, GitCommit ) Commit reported by the Docker API slice. Taken from the build environment if set; otherwise unknown . Nothing in the repository sets it (not build.rs , not the workflows), so released binaries report unknown . DELONIX_BPF_OBJECT written by crates/adapters/delonix-sdn/build.rs ; read with env! in crates/adapters/delonix-sdn/src/bpf.rs Path of the compiled eBPF flow-accounting object embedded in the binary. Set only when clang an"},{"p":"15. Environment variables (DELONIX_*)","h":"Test-only","u":"15-environment-variables.html#test-only","t":"These are read only by tests. Without them the live tests skip and print SKIP: … is not set (a live test that passes silently without its target would prove nothing). Variable Read by Purpose Values / default Notes DELONIX_PROXMOX_TEST_URL crates/providers/delonix-proxmox/tests/live.rs:target Proxmox node to run the live backend tests against. https://<host>:8006 . Enables the tests. They create and destroy one VM; run only against a node you own. DELONIX_PROXMOX_TEST_NODE crates/providers/delonix-proxmox/tests/live.rs:target Node name. Default pve . DELONIX_PROXMOX_TEST_USER crates/providers/delonix-proxmox/tests/live.rs:target Account for password authentication. e.g. root@pam . Required. TLS verification is disabled in these tests. DELONIX_PROXMOX_TEST_PASS crates/providers/delonix-proxmox/tests/live.rs:target Its password. Required. DELONIX_PROXMOX_TEST_STORAGE crates/providers/delonix-proxmox/tests/live.rs Storage for the test VM's disk. Default local-lvm . DELONIX_PROXMOX_TEST_AGENT_VMID crates/providers/delonix-proxmox/tests/live.rs:o_ip_vem_do_agente_de_um_convidado_a_serio An existing VM, with the QEMU guest agent running, whose IP the test reads. A VM id. Skipped when unset, even with the URL set. DELONIX_TRUENAS_TEST_URL crates/providers/delonix-truenas/tests/live.rs:target TrueNAS appliance to run the live provisioner tests against. https://<host> . Enables the tests. They create and destroy <pool>/dlxlive-<pid> . DELONIX_TRUENAS_TEST_POOL crates/providers/delonix"},{"p":"15. Environment variables (DELONIX_*)","h":"Names that look like variables but are not","u":"15-environment-variables.html#names-that-look-like-variables-but-are-not","t":"A few DELONIX_* names appear in the code without being read from any process environment. They are excluded from the check above by NOT_ENV in scripts/dev_docs.py , each with its reason, and you never need to set them: DELONIX_CRI_SOCKET — a Rust constant in bins/delonix-runtime-bin/src/cmd/cluster.rs , passed to kubeadm … --cri-socket= . The socket the CRI server listens on is DELONIX_CRI_ADDR . DELONIX_ROOTX , DELONIX_ROOT_BACKUP — test fixtures in crates/adapters/delonix-sdn/src/infra.rs , proving that reading a process environ matches whole names, not prefixes. DELONIX_IMAGE , DELONIX_DISTRO , DELONIX_RELEASE , DELONIX_BUILT_BY , DELONIX_BASE_IMAGE , DELONIX_BASE_SHA256 , DELONIX_K8S_VERSION , DELONIX_OFFLINE , DELONIX_NODE_EXPORTER , DELONIX_EXTRA_PACKAGES — keys of the provenance file /etc/delonix-image-release that image vm build writes inside a built VM image ( bins/delonix-runtime-bin/src/cmd/vmimage.rs ). Read them in the guest with cat /etc/delonix-image-release ; no process reads them."}];
