# Provider capability matrix

Generated by `delonix provider matrix` (engine 4.4.0, catalog 1.0.0). **Do not edit**: a test in `bins/delonix-runtime-bin` fails when this file differs from the output.

This is the DECLARED view — every host assumed complete. `delonix provider ls` measures the same declarations on a real host and turns a declared yes the host cannot honour into `unavailable-on-host`. States: `supported` (implemented and exercised — the evidence names a battery check `check:`, a section `e2e:`, a chaos scenario `chaos:`, a unit test `test:` or a live test `live:`), `partial` (implemented with a written limit, or without live proof), `unsupported-by-provider`, `requires-external-component`, `not-implemented`. See ADR-0050.

## compute

- **cloud-hypervisor**: 12 of 69 — 12 supported, 18 partial, 28 unsupported-by-provider, 2 requires-external-component, 9 not-implemented
- **libvirt**: 16 of 69 — 16 supported, 24 partial, 17 unsupported-by-provider, 2 requires-external-component, 10 not-implemented
- **proxmox**: 23 of 69 — 23 supported, 10 partial, 23 unsupported-by-provider, 3 requires-external-component, 10 not-implemented
- **linux**: 11 of 69 — 11 supported, 9 partial, 46 unsupported-by-provider, 3 not-implemented

| domain | capability | cloud-hypervisor | libvirt | proxmox | linux |
|---|---|---|---|---|---|
| inventory | `provider.availability` | partial — `available()` checks the binary; `provider ls` additionally probes /dev/kvm and the firmware | partial — `available()` checks the two binaries; `provider ls` additionally probes qemu:///system | partial — `available()` is always true once configured; reachability is learned on the first call | partial — the kernel is always there; what varies is rootless, subuid and cgroup delegation — each probed separately |
|  | `provider.readback` | supported — check:CH: ls nomeia-o | supported — check:o vm ls diz Paused (não Stopped) | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | supported — e2e:comandos de leitura (não destrutivos) |
|  | `provider.events` | not-implemented | not-implemented | not-implemented | partial — `system events` reads a persisted log the CLI writes on each action; no watch stream (`WatchEvents` unserved) |
|  | `provider.async-operations` | not-implemented | not-implemented | partial — every write waits on its UPID and is written to a per-VM task ledger before the wait (ADR-0049 slice 1); the engine exposes no job handle yet | not-implemented |
| vm-compute | `vm.create` | supported — e2e:vm: os mesmos snapshots no backend cloud-hypervisor | supported — e2e:vm: o snapshot sobrevive a um stop/start (precisa de hipervisor) | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.start` | supported — check:CH: vm start | supported — check:vm start | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.stop` | supported — check:CH: vm stop | supported — check:vm stop | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.destroy` | partial — terminates the VMM (pid + starttime guard) and removes the overlay; no named battery check | partial — `vm destroy` undefines the domain, releases the DHCP reservation and removes the overlay; the battery tears down without a named check | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.restart` | partial — stop-then-start; no battery check names it | partial — stop-then-start, always a real reboot; no battery check names it | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.pause` | partial — `PUT /api/v1/vm.pause` on the VMM socket; no battery check | supported — check:vm pause | supported — live:crates/providers/delonix-proxmox/tests/live.rs::power_operations_round_trip_through_the_node | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.resume` | partial — `vm.resume` on the VMM socket; no battery check | supported — check:vm unpause | supported — live:crates/providers/delonix-proxmox/tests/live.rs::power_operations_round_trip_through_the_node | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.resume-same-identity` | supported — check:CH: vm start | supported — check:vm start depois do stop de uma VM pausada | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.clone` | not-implemented | not-implemented | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_template_clone_gets_the_disk_size_asked_for | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.template` | not-implemented | not-implemented | partial — `POST …/template` is a client call (`mark_template`) the live case uses to make its clone source; no engine verb turns a VM into a template | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.resize.cold` | partial — `vm resize` rewrites vcpus/memory in the record and `vm start` rebuilds the vmm command line from it; no battery check yet | supported — check:o domínio arranca com 2 vCPU | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_stopped_vm_is_resized_and_the_node_reads_back_the_new_size | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.hotplug` | not-implemented | not-implemented | not-implemented | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.disks.extra` | unsupported-by-provider — the CH command line carries one root disk and the seed; `extraDisks` are refused for this backend | partial — `extraDisks` reach the domain XML (unit-tested target letters); never booted in the battery | supported — live:crates/providers/delonix-proxmox/tests/live.rs::extra_disks_and_nics_are_created_with_the_vm_and_go_with_it | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.nics.extra` | unsupported-by-provider — one tap on the SDN; `extraNics` are refused for this backend | partial — `extraNics` (network/bridge/user) reach the domain XML; never booted in the battery | supported — live:crates/providers/delonix-proxmox/tests/live.rs::extra_disks_and_nics_are_created_with_the_vm_and_go_with_it | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.disk.resize` | not-implemented | not-implemented | partial — `PUT …/resize` grows a template clone's boot disk to `diskSize` at create (live case); a shrink is refused by name; no engine verb resizes an existing VM | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.device.pci-passthrough` | partial — `--device path=<sysfs>` per validated address; no IOMMU host in the battery | partial — `<hostdev>` per validated PCI address; no IOMMU host in the battery | unsupported-by-provider — `devices` refused by name: the guest is on another machine | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.device.tpm` | unsupported-by-provider — no vTPM device is emitted for Cloud Hypervisor | partial — `<tpm model='tpm-crb'>` emulator; swtpm presence is not probed | unsupported-by-provider — refused by name: the node owns the QEMU knobs | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.cpu.model` | unsupported-by-provider — CH exposes the host CPU; no model selection is passed | partial — `cpuModel`/`cpuTopology` in the XML, default host-passthrough; not booted in the battery | unsupported-by-provider — refused by name: the node owns the QEMU knobs | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.cpu.pinning` | partial — `cpuAffinity` becomes `--cpus affinity=`; not booted in the battery | partial — `<cputune>` quota + `<vcpupin>`; unit-tested XML, dropped on qemu:///session | unsupported-by-provider — refused by name | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.memory.hugepages` | partial — `--memory hugepages=on`; unit-tested argument, pool not probed | partial — `<memoryBacking><hugepages/>`; the host's hugepage pool is not probed | unsupported-by-provider — refused by name | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.cloud-init` | partial — NoCloud seed ISO attached as a second disk; the battery never logs into a guest | partial — NoCloud seed on a virtio disk (#435); the battery never logs into a guest | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_stopped_vms_cloud_init_is_changed_and_the_node_renders_it | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.restart-policy.native` | unsupported-by-provider — the VMM has no crash policy; the engine's supervisor restarts it (`restart_policy_unsupervised`) | partial — `on_crash=restart` for always/on-failure; unit-tested, no guest crashed on purpose | unsupported-by-provider — the engine's supervisor is not on the node; no policy is set there | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.namespace-isolation` | supported — chaos:scen_namespace_isolation | unsupported-by-provider — a libvirt VM lives on virbr0 in the host netns, a different L2 the engine does not program; `--namespace` is refused by name | unsupported-by-provider — refused before any API call (`vm_namespace_supported`) | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.antispoof` | partial — the tap gets the same `iifname … ip saddr != <ip> drop` rule as a veth (auditoria #3); proven by rule inspection, not in the battery | supported — test:crates/adapters/delonix-vm/src/lib.rs::antispoof_live_defines_and_survives_the_domain | requires-external-component — the node's firewall (`…/firewall`), excluded as administration (ADR-0049 D3) | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.raw-definition` | unsupported-by-provider — no raw definition format exists for the CH command line | partial — `libvirtXml`/`libvirtXmlOverlay` are UNVALIDATED, trusted manifests only, local CLI only — never reachable from the node contract (ADR-0050) | unsupported-by-provider — no raw config passthrough (ADR-0049 D6) | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
| containers | `container.lifecycle` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — e2e:container: ciclo de vida + hot reconfig |
|  | `container.exec` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — check:container exec |
|  | `container.logs` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — check:container logs |
|  | `container.hot-reconfigure` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — check:update: mount visível DENTRO do container |
|  | `container.resource-limits` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — e2e:limites: o que se declara é IMPOSTO, não só escrito |
|  | `container.gpu.cdi` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | partial — consumes CDI specs generated by `nvidia-ctk`; never run on a GPU host in the battery |
|  | `container.seccomp.custom-profile` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — check:seccomp: a regra ERRNO continua a negar |
|  | `container.oom-detection` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | partial — `Dead (OOMKilled)` from `memory.events.local`, read by the supervisor; `scen_oom` kills but does not assert the reason yet |
|  | `pod.shared-network` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — chaos:scen_pod_namespace_isolation |
|  | `pod.shared-ipc-uts` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | partial — members `setns` into the first member's ipc/uts; no battery check reads them |
|  | `pod.shared-pid` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | not-implemented |
|  | `container.images` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — e2e:image |
| network | `vm.network.nat` | unsupported-by-provider — no hypervisor NAT network: the tap joins the engine's SDN, which does the NAT | supported — e2e:vm: o snapshot sobrevive a um stop/start (precisa de hipervisor) | unsupported-by-provider — the node has no NAT network of the engine's; `net0` is bridged | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.network.bridge` | unsupported-by-provider — the NIC is a tap on the SDN bridge inside the holder, never a host bridge | partial — `netMode: bridge` enslaves the NIC to a host bridge; validated live for ADR-0046 phase 2, no battery check | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.network.sdn` | supported — check:CH: vm start | unsupported-by-provider — the NIC is on virbr0 (host netns), not on the engine's SDN; `vm bridge` (root, experimental) is the only path across | unsupported-by-provider — the engine's SDN is on this host; a `network` that names one is refused by name (`refuse_unsupported`), the NIC is `net0` on the node's bridge | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.network.static-ip` | unsupported-by-provider — the address is derived from the MAC by the engine's DHCP; a fixed IP is not honoured | partial — `--ip` reserves a DHCP host entry (`net-update`), released on destroy; argv unit-tested, no battery | not-implemented | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
| storage | `storage.pools` | not-implemented | not-implemented | partial — `disk: <storage>:<gib>` names a node storage for a fresh disk; pools are not listed (ADR-0049: `storage` missing, not excluded) | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
| protection | `vm.snapshot.disk` | supported — check:CH: create com a VM parada | supported — check:snapshot create com a VM parada | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.snapshot.memory` | unsupported-by-provider — `vm.snapshot` of the VMM saves memory without the disk, which cannot be restored consistently; refused by design | supported — check:vm snapshot create | partial — `vmstate=1` on every snapshot of a running VM; the live case snapshots once, state not asserted | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.snapshot.restore` | supported — check:CH: restore | supported — check:vm snapshot restore depois do start | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.snapshot.delete` | supported — check:CH: rm com a VM parada | supported — check:vm snapshot rm | supported — live:crates/providers/delonix-proxmox/tests/live.rs::cria_arranca_e_destroi_contra_um_no_real | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.snapshot.persistent` | supported — check:CH: ls nomeia-o | supported — check:o libvirt volta a conhecer o snapshot | partial — snapshots live on the node; the live case lists `live1` back from the node right after taking it, but deletes it BEFORE the stop, so nothing asserts a snapshot is still there after a stop/start | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.backup.disk` | partial — `backup create vm` copies the overlay of a STOPPED VM; a running CH VM holds the qcow2 exclusively | partial — `backup create vm` takes a live external snapshot and block-commits it back; the battery backs up a container, not a VM | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_backup_lands_on_the_storage_and_comes_off_it | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.backup.quiesced` | unsupported-by-provider — no guest agent channel; only an offline copy is consistent | partial — `--quiesce` asks the guest agent to freeze; whether the guest has one is not probed | not-implemented | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.backup.restore` | partial — `backup restore` re-imports the disk; battery covers the container kind only | partial — `backup restore` re-imports the disk; battery covers the container kind only | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_deleted_vm_comes_back_from_its_own_backup | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `container.backup-restore` | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — a VM provider; containers are the Linux provider's | unsupported-by-provider — LXC needs its own boundary decision (ADR-0049 D4); containers are the Linux provider's | supported — check:backup restore --force pára, repõe e arranca |
| mobility | `vm.migration.cold` | partial — `vm migrate`: stop, copy over SSH, import, start; no battery | partial — `vm migrate`: stop, flatten, copy over SSH, import, start; no battery | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_stopped_vm_moves_to_another_node_and_the_cluster_lists_it_there | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.migration.live` | unsupported-by-provider — ADR-0031: CH migrates memory only, never the disk | unsupported-by-provider — ADR-0031: needs shared VM storage or a privileged libvirt daemon listening on the network; neither is a default of this engine | supported — live:crates/providers/delonix-proxmox/tests/live.rs::a_running_vm_moves_live_on_shared_storage_and_keeps_running | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.replication` | requires-external-component — shared or replicated VM storage outside the engine (ADR-0031) | requires-external-component — shared or replicated VM storage outside the engine (ADR-0031) | requires-external-component — cluster replication jobs (ADR-0049 D3: excluded as administration) | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.high-availability` | requires-external-component — a cluster manager with quorum and fencing | requires-external-component — a cluster manager with quorum and fencing; a single libvirt host has none | requires-external-component — cluster HA policy (ADR-0049 D3: excluded as administration) | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
| console | `vm.console.serial` | partial — `vm console` bridges the tty to the VMM's serial socket; no battery | partial — `vm console` runs `virsh console --force`; interactive, no battery | not-implemented | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.console.vnc` | unsupported-by-provider — no VNC device on the CH command line | partial — `vm vnc` reads `vncdisplay` of a `--vnc` domain; no battery | not-implemented | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.guest-agent` | not-implemented | not-implemented | partial — `agent=1` is set and `agent/network-get-interfaces` read; a guest without the agent answers `None` | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
|  | `vm.ip.observed` | unsupported-by-provider — the address is PREDICTED from the MAC (`ip_is_predicted`); `--wait` verifies it by ARP instead | partial — DHCP lease with a lease floor, then `domifaddr`; pure test only, the battery does not read the IP | supported — live:crates/providers/delonix-proxmox/tests/live.rs::o_ip_vem_do_agente_de_um_convidado_a_serio | unsupported-by-provider — a container provider; VMs are libvirt's, Cloud Hypervisor's or Proxmox's |
| metrics | `metrics.prometheus` | partial — `delonix_vms_running/total` only | partial — `delonix_vms_running/total` only; no per-domain stats | not-implemented | partial — `/metrics` on the CRI and management sockets; per-container gauges, no battery scrape |
|  | `metrics.network.per-workload` | partial — the tap's counters are readable in the holder; not exported per VM | not-implemented | not-implemented | partial — rx/tx per container read in its netns by the dashboard collector; not exported per container |
|  | `host.health` | supported — check:system info | supported — check:system info | not-implemented | supported — check:system info |
|  | `host.capacity` | not-implemented | not-implemented | not-implemented | not-implemented |
| access | `access.transport-verified` | partial — a unix socket per VMM, owned by the engine's uid | partial — local URIs only (qemu:///system\|session); a remote libvirt URI is never built | partial — TLS verified by default (webpki roots, or `ca_cert_pem`/`DELONIX_PROXMOX_CA_FILE` for an internal CA); `insecure_tls` is an explicit opt-out; 16 MiB response bound; `Debug` redacts the credential (ADR-0049 slice 1) | partial — unix sockets with SO_PEERCRED (CRI, management, docker-api); no TCP is ever bound |
|  | `access.credential-in-vault` | unsupported-by-provider — no credential exists: the VMM is a child of the engine | unsupported-by-provider — no credential exists: access is membership of the `libvirt` group | partial — `DELONIX_PROXMOX_SECRET` names a `kind: Secret`; the env-var form keeps the token in the environment | partial — `--secret` values are resolved from the vault at spawn and never persisted in the record |

## network

- **linux**: 7 of 20 — 7 supported, 10 partial, 2 unsupported-by-provider, 1 requires-external-component
- **proxmox**: 0 of 20 — 5 partial, 6 unsupported-by-provider, 9 not-implemented

| domain | capability | linux | proxmox |
|---|---|---|---|
| network | `net.bridge` | supported — e2e:network: ciclo de vida | partial — `kind: NetworkZone` creates a simple SDN zone and its VNets on the cluster and reloads the SDN (ADR-0049 addendum); a VNet is the node's bridge, not `network create` |
|  | `net.macvlan-ipvlan` | unsupported-by-provider — needs CAP_NET_ADMIN in the host's init netns; registered with Realized=False, never realised rootless | not-implemented |
|  | `net.vlan` | partial — `network vlan` puts an 802.1Q VLAN on a physical NIC — root only, and it says so on every run | not-implemented |
|  | `net.overlay.vxlan` | partial — VXLAN uplink + FDB peers inside the holder; inter-node forwarding needs a second node the battery does not have | not-implemented |
|  | `net.overlay.encrypted` | partial — WireGuard peer per overlay node; peer removal proven on one node, traffic across two nodes not measured | not-implemented |
|  | `net.ipam` | supported — chaos:scen_concurrent_attach | not-implemented |
|  | `net.static-ip` | partial — `--ip` reserves in the same per-prefix IPAM ledger; unit-tested, not attached live in the battery | unsupported-by-provider — a feature of the engine's own SDN on this host; a VM on a Proxmox node is not on it |
|  | `net.dns` | partial — `<name>.<ns>.delonix.internal` served by the holder; proven E2E in-session, no battery `nslookup` | not-implemented |
|  | `net.publish-ports` | supported — check:update: publish-add a quente | unsupported-by-provider — a feature of the engine's own SDN on this host; a VM on a Proxmox node is not on it |
|  | `net.routes` | supported — chaos:scen_stack_netroute | not-implemented |
|  | `net.namespace-isolation` | supported — chaos:scen_namespace_isolation | unsupported-by-provider — a feature of the engine's own SDN on this host; a VM on a Proxmox node is not on it |
|  | `net.l7-proxy` | partial — hyper reverse proxy in the holder, TLS termination, SIGHUP reload; proven E2E in-session, battery only lists routes | unsupported-by-provider — a feature of the engine's own SDN on this host; a VM on a Proxmox node is not on it |
|  | `net.tunnel` | requires-external-component — a tunnel provider (pinggy, ngrok, cloudflared) and its binary/account | unsupported-by-provider — a tunnel agent runs on this host, not on the node |
|  | `net.ipv6` | unsupported-by-provider — disabled in every container netns and dropped by `table ip6` in the holder: the firewall is `table ip` (v0.37.1); `DELONIX_ENABLE_IPV6=1` is a noisy escape, not a feature | not-implemented |
|  | `net.rate-limit` | supported — check:update: net-rate a quente | not-implemented |
|  | `net.capture` | partial — `net capture` on a container's SDN interface; no battery | unsupported-by-provider — a feature of the engine's own SDN on this host; a VM on a Proxmox node is not on it |
| firewall | `firewall.per-workload` | supported — e2e:container em rede custom: hot reconfig pelo ingress | partial — `NetworkPolicy` `scope: vm` replaces the engine's rules of one direction on the node's own firewall (ADR-0052); the live case reads rules, order and the three switches back — no guest traffic is measured |
|  | `firewall.default-deny` | partial — `policy deny` per direction with the conntrack prologue (2026-07-28); validated live, no battery check | partial — `defaultPolicy` becomes the VM's `policy_in`/`policy_out`, written after the rules; read back live, not measured on a packet |
|  | `firewall.source-filtering` | partial — `allow <port> --from <cidr>` works on published ports for routable sources (never for a loopback client); validated live, no battery | partial — `from`/`to` CIDRs become the rule's `source`/`dest`; `fromWorkload` is refused (an SDN address the VM is not on); read back live, not measured on a packet |
|  | `firewall.egress-policy` | partial — per-container and per-network egress policy; battery lists (`net egress ls`), does not block | partial — `direction: egress` writes `out` rules and `policy_out`; read back live, not measured on a packet |

## storage

- **linux**: 4 of 11 — 4 supported, 3 partial, 2 requires-external-component, 2 not-implemented

| domain | capability | linux |
|---|---|---|
| storage | `volume.local` | supported — e2e:volumes: ciclo de vida |
|  | `volume.bind` | supported — check:update: mount visível DENTRO do container |
|  | `volume.nfs` | partial — read and written from a container against a real NAS in-session; the battery has no NFS server |
|  | `volume.cifs` | partial — same mount path as nfs with `mount -t cifs`; no battery server |
|  | `volume.webdav` | partial — `mount -t davfs`; no battery server |
|  | `volume.quota` | supported — check:volume create --parent |
|  | `volume.snapshot` | supported — check:snapshot create |
|  | `volume.provision.nas` | requires-external-component — a TrueNAS SCALE 25.x API (`spec.provision.truenas`, ADR-0009); `scen_truenas_destroy` skips without one |
|  | `storage.lvm-thin` | not-implemented |
|  | `storage.zfs-btrfs` | not-implemented |
|  | `storage.ceph` | requires-external-component — a Ceph cluster and an RBD/CephFS driver the engine does not ship |

